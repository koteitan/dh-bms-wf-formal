[← Back](README.md)

# 原文忠実性の監査

論文の各節に 1 エージェントを割り当て、原文 PDF と Lean を突き合わせた結果。
21 エージェント、opus、effort=xhigh。各エージェントは他のメモを読まず、独立に監査した。

## 分類

| 分類 | 条件 | 監査時 | 再検討後 | 著者判定後 |
|---|---|---|---|---|
| **X** | 原文をそのまま形式化すると矛盾する。本質的、または規模が大きい | 0 | 0 | 0 |
| **Y** | 原文をそのまま形式化すると矛盾する。誤記の類 | 0 | 0 | 0 |
| **Z** | 原文に飛躍がある。飛躍が本質的で大きい | 0 | 0 | 0 |
| **W** | 原文に飛躍がある。形式化の範疇に収まる小さいもの | 22 | 23 | 32 |
| **S** | 迂回だが原文の主張との差が小さく、下流の内容を変えない | — | — | 1 |
| **R** | 原文の証明の一部が無くても証明が通る | 36 | 36 | 36 |
| **U** | 形式化の都合で原文どおりに書けず迂回している | 74 | 73 | 63（うち 45 件は解消済み、1 件は取り下げ、残り 17 件） |
| | **合計** | **132** | **132** | **132** |

**X・Y・Z はゼロ。** 21 エージェントは各自 1 節しか見ていないので、節をまたぐ誤りを
見落とす可能性があった。そこで横断的に読み直したが、**格上げに値するものは無かった**。
再検討での変更は U-7 → W の 1 件のみ。下記「監査後の再検討」を参照。

その後、著者が 1 件ずつ処遇を判定し、U のうち 10 件を **W**（形式化範疇の飛躍・誤差）へ、
1 件を **S**（軽微迂回）へ移した。移動したものは移動先の節に、旧番号を添えて置いてある。

| 旧 | 新 | | 旧 | 新 |
|---|---|---|---|---|
| U-6 | W-23 | | U-65 | W-28 |
| U-7 | W-24 | | U-66 | W-29 |
| U-12 | W-25 | | U-67 | W-30 |
| U-17 | W-26 | | U-70 | W-31 |
| U-64 | W-27 | | U-2 | W-32 |
| | | | U-22 | S-1 |

## 節ごとの件数（監査時のスナップショット）

下表は 21 エージェントが報告した時点の件数。その後の変更は「監査後の再検討」を参照。

| 節 | W | R | U | 計 |
|---|---|---|---|---|
| 01 §1, §2, §5 の定義 | 0 | 1 | 3 | 4 |
| 02 §2–§4 の補題 | 2 | 0 | 4 | 6 |
| 03 §5, §6 の設定 | 0 | 1 | 3 | 4 |
| 04 §5 展開との接続 | 0 | 1 | 1 | 2 |
| 05 §7 標準性 | 0 | 1 | 1 | 2 |
| 06 §6 コピー補題 | 0 | 4 | 3 | 7 |
| 07 論文外・試験用 | 0 | 0 | 1 | 1 |
| 08 §8 符号と充足コード | 0 | 2 | 8 | 10 |
| 09 §9 L のコード | 1 | 1 | 4 | 6 |
| 10 §10 KP 内の再帰 | 2 | 1 | 7 | 10 |
| 11 §9 L の階層 | 3 | 0 | 1 | 4 |
| 12 §11 内部許容性 | 4 | 0 | 4 | 8 |
| 13 §12 交代ブロック階層 | 2 | 3 | 5 | 10 |
| 14 §13 真理述語 | 1 | 2 | 7 | 10 |
| 15 §14 有限段 TV | 0 | 0 | 7 | 7 |
| 16 §15 St と Rel | 0 | 1 | 4 | 5 |
| 17 §16 ω₁ と初期対 | 1 | 3 | 3 | 7 |
| 18 §17 有限行反映 | 1 | 3 | 1 | 5 |
| 19 全体の道具（Lévy 階層） | 3 | 3 | 2 | 8 |
| 20 §18, §19 安定ラベルと降下 | 1 | 2 | 2 | 5 |
| 21 §20, §21, §22 | 1 | 7 | 3 | 11 |

## 監査後の再検討

各エージェントは担当節だけを見て判定した。そのため「原文どおりに書くと**別の節の主張が
偽になる**」型の誤りを見落としうる。この観点で横断的に読み直した結果を記す。

### 分類を変えたもの（1 件）

| 元 | 新 | 根拠 |
|---|---|---|
| U-7（現 W-24） | **W** | `anc_exit_point` は補題 6.6・6.7 の証明で実際に使われている。§6.5・§6.6 の「k-親鎖を左へたどる」という非形式的な一歩の形式化であり、迂回ではなく原文の飛躍の明示化 |

### 格上げを検討したが W に留めたもの（6 件）

いずれも「側条件を落として読むと偽になる」「引用先が主張していない」型で、一度は Y・Z を
検討した。しかし論文は §8・§10 で定義を文献（Barwise, Jech）に委ねており、そこで委ねた
**標準の定義を当てれば全て埋まる**。修正はいずれも一行で済む。よって W（形式化の範疇に
収まる小さい飛躍）が正しい。内容は [paper-corrections.md](../paper-corrections.md) に記載。

| 件 | 検討した内容 | W に留めた理由 |
|---|---|---|
| W-9 | スキーマインスタンスを全称閉包しないと ⟨d,∅⟩∈S が成立せず、KPTrue が充足不能。補題 11.2 の ⟸ が通らない | スキーマのインスタンスを全称閉包で取るのは標準の読み |
| W-10 | ∆0-収集に側条件 y ∉ FV(φ) が無い。φ = (j ∉ y) が反例でインスタンス自体が偽 | 側条件 y ∉ FV(φ) は教科書標準。§10 は公理を名前で挙げるだけなので標準のスキーマを指す |
| W-5 | 補題 10.5(1) の「補題 10.4(1) により ω × A^<ω が存在する」は誤引用。10.4(1) は直積を述べていない | 直積の存在自体は系 10.2 の証明中にある。引用先を直すだけ |
| W-4 | 系 10.2 は補題 10.1 の**主張**からは従わない。証明中の中間物（補助証人が d に入ること）に依存 | 論文は「補題 10.1 の**証人対収集**により」と証明中の構成を名指ししている。主張を強形にする一行で埋まる |
| W-19 | 補題 12.2(1) で付加変数が最外ブロックに既出だと定義 12.1 の「ブロック内相異」に反する。原文はこの場合に触れていない | 前置しないと規定する一行で埋まる |
| W-21 | §19 が宣言する不変量 (I1)(I2) では場合 (d) が閉じない。第三の不変量「G 上で f_q = f」が要る | 構成規則と f_0 = f↾c から自明に従う。不変量に一項足すだけ |

### 監査の記述そのものが誤っていたもの

| 件 | 訂正 |
|---|---|
| U-58 | 「(14.2) の同値は q = k+2 でしか証明されていない」は誤り。`StRelP.lean` の `tvqP_iff_elemHat_general` が `1 ≤ q` の一般形で証明しており、論文の (14.2)（q ≥ 1）そのもの。`tvqP_iff_elemHat` はその q = k+2 への特殊化にすぎない |
| U-40 | 「i,j,x,y の相異性も真理には不要」は誤り。`collAx φ i j x y = ∀x ((∀i∈x ∃j φ) → ∃y (∀i∈x ∃j∈y φ))` で、`ball i x` は i = x だと束縛が壊れ、そもそも Collection の形にならない。真に不要だったのは `x ∉ FV(φ)` だけで、そちらは削除した |
| U-11 | 引用する `Expand.lean:72 anc_expand_iff` と `Copy.lean` の `ParForm`/`AncForm` は既に削除済み |
| U-68 後半 | 「補題 12.2 の同値性の成立範囲が非空推移的領域に狭められている」は誤り。非空性・推移性を要求するのは `Defin.lean` の定義可能性クラスの道具立てであって補題 12.2 の主張ではない。12.2 に対応する補題は領域条件を課していない（`Fm.sat_subst` は条件なし、`BF.Sig.pad` は非空性のみ）。そこでの推移性は Δ₀ の有界量化を読むために不可欠で、論文も §13.2 の Δ₀-絶対性を推移的集合に限って主張している |
| U-34 後半 | 「許容順序数の定義自体が L θ ⊨ KP ではない」は現在は古い。U-36 で `isAdmissible_iff_kpModel` が両向き証明済み |

### 「Lean が論文より強い仮定を要求している」候補の判定

18 件あったうち、大きいものを個別に見た。いずれも**原文が正しく、Lean 側の選択**だった。

| 件 | 判定 |
|---|---|
| U-59（Π̂ を双対化だけで済ませている） | 原文は Σ̂_j の**両方向**を先に確立してから χ⊥ に適用しており、証明は完結している |
| U-43・U-46・U-51（定理 13.6 に論文に無い仮定） | 論文どおりの仮定で通る版が `StRelP.lean` の `GoodAsnQ` として存在する。指摘された 3 件は中間段階 |
| U-36・U-40・U-64（現 W-27）（許容性の定義） | Lean が「L_θ ⊨ KP」を 3 条件で代用し、認識する公理集合も狭めている。いずれも Lean が弱い側 |
| U-31（補題 10.3 の F を外から与える） | 「KP ⊢ …」を形式化していないことの帰結。原文の誤りではない |
| U-66（現 W-29）（ω<Λ と Λ 極限を追加要求） | 論文は補題 13.1 から得ている情報を、Lean は `IsAdmissible` の定義に持つため仮定として要る |
| U-54・U-60・U-61・U-69（相対化の副条件） | 論文は非有界量化だけを M に制限しており、それ自体 Δ0 で正しい。全量化子を制限したのは Lean の選択 |

### U 68 件の再検討（14 エージェント、全件判定、格上げ 0 件）

論文の節ごとに 1 エージェントを割り当て、**「原文の主張を字義どおり形式化したら偽になるか／
証明不能になるか」の一問だけ**を問うた。修正済みを除く 68 件すべてに判定が返り、
**格上げは 0 件**。エージェントには上記 6 件の飛躍を「同型のものを探す手がかり」として渡し、
格上げ方向にバイアスをかけたうえでこの結果である。確信度は high 65 件、medium 3 件
（U-19, U-33, U-40）。

主な確認内容:

| 件 | 確認された内容 |
|---|---|
| U-1, U-4, U-5, U-8, U-11, U-13 | 論文は Pos(A)・k<r・コピー 0..N に制限した定義で完結しており矛盾しない。Lean はその真の拡張を証明しているだけ。特に U-5 の「補題 3.1 では足りず anc_congr が要る」は成立しない。§6.6 も §19 も接頭部分が literally 同一の配列なので補題 3.1 を二度使えば足りる |
| U-12（現 W-25） | 備考 7.2 の「コピー式は原規則の書き直し」は外部実装への言及なので形式化の対象にならない。代わりに定義 5.1 を字義どおり実装して参照実装（`bms -v4`）と突き合わせ、`lean/test/cases.txt` 58 件と乱択 4 系列 1149 ステップで完全一致を確認した |
| U-13 | (C6) の `b < N` は必須（`b = N` だと `D_j^(b+1)` が A[N] に存在しない）。Lean も (C3) については `k < m₀` を保っており、`k = m₀` では (C4) により実際に偽になる |
| U-17（現 W-26）, U-18, U-25, U-38, U-56 | 論文の Cantor 対 π による自然数符号化の下では `∃s ∈ ω` は文字どおり有界量化で、固定パラメータも ω 一つで足りる。Lean で h = L ω が増えたのは符号化媒体を HF 集合に替えた帰結 |
| U-19, U-41, U-42, U-68 | (8.3) の DerSeq/Trace スキーマは列挙された全操作を実際に覆う。Trace は τ に多段パスを詰められるので「局所再帰規則では足りない操作」は挙がらない |
| U-21 | むしろ論文の方が正しい。`Form(e)` の連言項が無いと `Asn_A` が非コードにも成立し、定義 8.3 条件 2 の正準性が壊れる。Lean が落とせるのは `Free` を `¬NotFreeW` で書いた内部事情 |
| U-37 | Lean の `infAx`（∈-極大元を持たない非空集合の存在）は論文の Infinity より真に弱い。弱い公理を採る側なので原文の誤りではない |
| U-49 | ∆0 は否定で構文的に閉じているので、双対化で生じる ¬δ もそのまま ∆0 コード。符号層は不要で、Lean 側の設計 |
| U-58 | 「(14.2) は q = k+2 でしか証明されていない」は誤り。q ≥ 1 一般版が `StRelP.lean:328 tvqP_iff_elemHat_general` にある |
| U-61 | むしろ原文 (15.4) の `Ord(ξ) ∧ Ord(η) ∧ ξ<η` の方が正しい。これがあることで `Rel_k` が順序数上の関係になり `Rel_k ∈ Σ̂₁` も保たれる |

なお U-11 の記述（`Expand.lean:72 anc_expand_iff`）と U-10・U-15 の `ParForm`/`AncForm` は
現在のツリーには存在しない。下記「解消した U」で削除済み。

## 解消した U（main、ビルド緑・公理クリーン）

45 件解消。`sorry` は 0、公理は `[propext, Classical.choice, Quot.sound]` のみ。

### 根本 A: `Arr` の設計（6 件）

論文の定義 2.1 は `A ∈ (ℕ^r)^ℓ` とし、`A_i(k)` を `i ∈ Pos(A) = {0,…,ℓ-1}` と `k < r` で
のみ定義する。Lean は `col : ℕ→ℕ→ℕ` を全域にして側条件を落としていた。これを入れた。

| 件 | 対応 |
|---|---|
| U-1・U-4 | `parentRel` の末尾に `∧ k < r ∧ i < A.len` を追加。`parent` / `anc` / `cand` が定義 2.1 の側条件を持つようになった。読み出しは `parent_row_lt` / `parent_target_lt` |
| U-5 | **補題 3.1 が実質的な主張になった。** 以前の `anc_prefix` は `fun _ _ _ => rfl` で自明だったが、いまは `anc_prefix (hm : m ≤ A.len) (hi : i < m)` で、`anc_congr`（`k` についての帰納法）を経由して証明される。論文の仮定 `m ≤ ℓ` と「二位置が接頭部分に属する」が両方使われる |
| U-8 | `BadRoot` に `N : ℕ` を追加し、`tA = ⟨p + (N+1)*s, tildeCol A p m s⟩` とした。論文の `Ã = A[N]`（コピー N+1 個、長さ p+(N+1)s）そのもの。`expand_eq : expand A N = (toBadRoot h N).tA` |
| U-11・U-13 | 定理 6.3 の主張に論文どおりのコピー範囲が入った。(C1)(C2)(C3) は `∀ q ≤ N`、(C6) は `c' + 1 ≤ N`。行の範囲 `k < r` は関係の側条件から自動で付く |

`Label.lean` の `anc_Aq_iff` も、`A[q]` が `A[N]` の接頭部分であることを使う補題 3.1 の
適用（`q ≤ N` と `x < (A[q]).len` が要る）になった。

### 根本 D: §14・§15 の緩いルートを削除（4 件）

Part III には §13・§14・§15 が**二重に形式化**されていた。`Truth.lean` → `TrCorrect.lean` →
`TV.lean` → `StRel.lean`（∅ パディング無しの定義 13.4 に基づく緩い版）と、
`BlkP.lean` → `TrP.lean` → `StRelP.lean` → `StKP.lean`（論文どおりの版）である。

主定理が前者を使っていないことを **sorry 注入**で確定させた（`trSigPiS_correct` と
`trSigPiS_correct_V` を `sorry` にしてビルドしても、`terminates_unconditional` /
`R_wf_unconditional` / `relKP_iff` の `#print axioms` に `sorryAx` が出ない）。
そこで §14・§15 側の緩いルートを削除した。`TV.lean` は全体、`StRel.lean` は後半
（`tvBody_of_tvConj` 以降）を削除し、唯一必要だった `delta0_nonemptyMem` だけを移した。

| 件 | 対応 |
|---|---|
| U-54 | `TVq` の 4 連言（M 推移的・非空・h∈M・w∈M）が消えた。残る `TVqP` ではそれらは連言ではなく**含意の仮定**で、これは [paper-corrections.md](../paper-corrections.md) W-20 に記載済みの読み替え |
| U-57 | `TVBody` は緩い符号認識子 `IsSigCodeW` を使っていた。残る `TVBodyP` は定義 12.1 どおりの `IsSigCodeWD`（ブロック内相異性つき）を使う |
| U-61 | `RelK` には (15.4) の `Ord(ξ) ∧ Ord(η) ∧ ξ<η` が無かった。削除により、これを持つ `RelKP` だけが残った |
| U-63 | 補題 15.3 の二重形式化を解消。`stK_iff` を削除し `stKP_iff` だけにした |

副次的に `IsSigCodeW` / `IsPiCodeW`（U-44 の緩い認識子）も、`BFCodeD.lean` が忠実版
`IsSigCodeWD` を導くための内部足場だけになった。

### 根本 E: §12・§13 の緩いルートを削除（4 件）

同じく二重化していた §12・§13 も、緩い側（∅ パディング無しの `IsBlkUpd`、それに基づく
定義 13.4 の `TrSigS`/`TrPiS`、全ブロック `Nodup` を要求する `GoodAsn`/`GoodAsnP`）を削除した。
残ったのは論文どおりの `IsBlkUpdP`（§12 の BlkUpd、∅ パディング付き）と `TrSigP`/`TrPiP`
（定義 13.4）と `GoodAsnQ`（定理 13.6 の仮定、ブロック内相異のみ）だけである。

| 件 | 対応 |
|---|---|
| U-43 | `IsBlkUpd` を削除。∅ パディングを持つ `IsBlkUpdP` だけが残り、存在定理に論文に無い仮定（ブロック変数が既に dom(a) にある）は要らなくなった |
| U-46 | 定義 13.4 が padding 版一本になった。定理 13.6 から「ブロック変数が既に代入の定義域にある」という論文に無い仮定が消えた |
| U-51 | 全ブロック連結の `Nodup` を要求する `GoodAsn` / `GoodAsnP` を削除。残るのは定義 12.1 どおりのブロック内相異 `b.NodupBlocks` を使う `GoodAsnQ` だけ |
| U-52 | 定理 13.6 の最後の文「同じ同値は外部宇宙 V でも成立する」を padding 版で新たに証明した（`Bm4/SetTheory/TrPV.lean`、`GoodAsnVQ` / `trSigPiQ_correct_V` / `trSigP_correct_ext` / `trPiP_correct_ext`）。以前は緩い版にしか無かった |

§12–§15 の緩いルート削除で Lean は差し引き約 1100 行減った。`sorry` は 0、公理は不変。

### 個別の修正（5 件、章ごとに 1 エージェントで並列）

| 件 | 対応 |
|---|---|
| U-16 | `Compute.lean` の `expandC` が先頭列の長さから `r` を推定するのをやめ、論文どおり `r` を外部パラメータにした。G 部分も長さ `r` に正規化し、「この操作では行数 `r` を変更しない」を `length_mem_expandC` として明示的に証明。`lean/test/cases.txt` 58 件で振る舞い不変を確認 |
| U-28 | 補題 9.2 の一般版 `lcode_sound_isOrdinal` を追加。`IsOrdinal` を満たす任意の `ZFSet` について成り立つ形で、外部順序数の像に限られない。`IsOrdinal` は定義 9.1 の条件 1 に含まれるので追加仮定は不要 |
| U-39 | 論文に対応物の無い第二の定義 11.1（`AdmP` / `CollTrue`）を削除。`AdmP.lean`（414 行）と `AdmPOK.lean` を丸ごと消した。下流は論文どおりの `AdmKP` を使っている |
| U-45 | `IsVarsBody` に先頭タグの検査を足した。`(e = ⟨natZ 1, ⟨ν,d⟩⟩) ∨ (e = ⟨natZ 2, ⟨ν,d⟩⟩)` となり、∆0 ブロック符号 `⟨natZ 0, ⟨sg,d⟩⟩` の符号ビットを「Vars」として返さなくなった。定義 12.1 の Vars/Body に一致 |
| U-53 | `ElemHat` を定義 14.1 どおりにした。`1 ≤ q ∧ M 推移的 ∧ N 推移的 ∧ M ⊆ N ∧ ∀ j, 1 ≤ j ≤ q → …`。j = 0（∆0）の一致は推移的 M ⊆ N の絶対性から `ElemHat.delta0` として導く |

### 論文の言明そのものに寄せた 2 件

| 件 | 対応 |
|---|---|
| U-3 | 論文の BM4 は全ての `r` にわたる和集合。`Elts := Σ r : ℕ, Elt r` を定義 1.1 として置き、その上の一段関係 `R'` と、定理 1.2・命題 22.1 の BM4 全体版（`terminates_all`, `R'_wf`, および無仮定版 `terminates_all_unconditional`, `R'_wf_unconditional`）を追加した。展開は行数を変えないので `R'`-鎖は一つの `r` に留まり、整礎性は `R_wf` に帰着する |
| U-21 | `IsAsn`（(8.4) の `Asn_A`）に論文の `Form(e)` の連言を戻した。`IsSeqA w A a ∧ (自由変数の被覆) ∧ IsCodeW h w e`。`IsCodeW` は ∆0 なので `delta0_isAsn` も通る。従来は `¬NotFreeW` の否定形が非コードで dom(a) ⊇ ω を要求することからコード性が導けたが、論文どおり明示した |

### 原文の言明に寄せた第 2 弾（8 件、7 エージェントで並列）

**原文忠実を優先し、形式化の主張が弱くなってもよい**という方針で実施した。

| 件 | 対応 |
|---|---|
| U-9 | 有効性を課さない `IntCand`（`Cand^I_k`）を追加。論文 (6.5) をその記法で述べた系 `intCand_bridge` を (C3) の証明に置き、実際に使われる状態にした |
| U-14 | 区間を `I = [lo, hi)` の両端で表す形に変更。補題 6.2 に `X ∈ I` の仮定と「親が `I` に属する」という結論を入れ、右端の冗長性も同値補題として明示した |
| U-23 | `satCode_correct` から論文に無い仮定 `a ∈ U` を削除。定義 8.3 条件 1 から `seq_mem_of_puCl` で導ける。主張が (8.6) どおりになった |
| U-24 | `IsPadOf` とその補題を `SatCode.lean`（§8）から `Blk.lean`（§12–13 の準備）へ移した。§8 の「padding」は式コードに対する別の操作 |
| U-27 | `a_x` の一意存在 `existsUnique_consSeq` と、`∃ ax ∈ U` / 無束縛 / `∀` の 3 形が同値であることを補題化。(9.1) を「`a_x` を項として読む」形で述べた系も追加 |
| U-37 | 無限公理を論文どおり「帰納的集合の存在」に強めた。`satIn_infAx` は `ωZ` を証人に書き直し、`omega_lt_of_kpTrue` は逆に簡単になった |
| U-55 | (14.1) の前件 `IsSeqA w M a` を論文の `Asn_M(e,a)` に差し替え。`IsAsn` は `Fm` コード用で `BF.code` には使えないため、ブロック式コード用の `NotFreeBFW` / `IsAsnBF` を新設した |
| U-59 | `elemHat_of_downward` を論文どおりの筋（`Σ̂_j` の**両方向**を先に確立してから `χ⊥` に適用）に書き直し、`sat_alls_anti` を使わなくした |

**U-48 は差し戻した。** 補題 13.3 に論文の仮定「a が適切な代入」を足すと `BaseCorrect` の
適用点 4 箇所（`trMSig_correct` など）が壊れる。下流の `GoodAsnQ` は自由変数の被覆を持たない
設計なので、埋めるには「`∅` 詰めで `Tr⁺`/`Tr⁻` が変わらない」という補題が要る。
担当エージェントがその補題を書いて緑まで確認した検証済みの断片を残している。

### §8・§11・§13・§22 の個別修正（4 件）

| 件 | 対応 |
|---|---|
| U-20 | `ValAt a i x` を論文の `Val(a,i,x)`（`i ∈ dom(a)` かつ `a(i) = x`）どおり `⟨i,x⟩ ∈ a` にした。定義域外で `x = ∅` を真にする全域版の分岐を削除。`SatInL.lean` の 18 箇所を追随させた |
| U-36 | 許容順序数を論文の定義 11.1 そのもの、すなわち `L_θ ⊨ KP` で特徴づけた。`KPModel θ := ∀ φ, KPAxCode (L ω) ωZ φ.code → SatIn (L θ) _ φ` を置き、`isAdmissible_iff_kpModel : IsAdmissible θ ↔ KPModel θ` を証明。従来の 3 条件（`ω < θ`・極限・Δ₀-Collection）は同値な特徴づけとして残る |
| U-47 | 定義 13.2 の行列を論文どおりにした。`TrD0Mat = A 推移的 ∧ Asn_A(e,a) ∧ a ∈ U ∧ SatCode(A,U,T) ∧ ⟨e,a⟩ ∈ T`。論文に無い連言 `∅ ∈ A` と、∅-パディング `b` の有界存在量化が消え、判定される対が `⟨e,b⟩` から `⟨e,a⟩` に戻った。使われていなかった `trD0P_imp_trD0N_of_unique` も削除 |
| U-73 | §22 の「各配列は有限データなので自然数で符号化でき、BM4 全体は集合である」を回復した。BM4 の元は初期配列 `E_r` からの展開回数の有限列で決まるので、`ofList : ℕ → List ℕ → Arr r` と全射性 `exists_ofList` を作り、`List ℕ` の可算性から BM4 の可算性を得る。ただし命題 22.1 の「最小コードの元を選ぶ ω-再帰」そのものは無く、`wellFounded_iff_isEmpty_descending_chain` のままである |

### 部分的に解消（U-18）

(8.2) の「導出列は**最後の項が** `e`」を実装した。`IsCodeW h w e` は
`∃ s ∈ h, DerSeqW w s ∧ IsLastW w s e` になり、

```
IsLastW w s e := ∃ d ∈ w, IsDom s d ∧ ∃ k ∈ d, (∀ k' ∈ d, k' ∈ k ∨ k' = k) ∧ ⟨k,e⟩ ∈ s
```

すなわち「`d` は `s` の定義域、`k` はその最大元（最終添字）で `s(k) = e`」である。
`∃ d ∈ w`・`∃ k ∈ d`・`∀ k' ∈ d` はすべて有界なので `delta0_isCodeW` はそのまま通る。
以前は「どこかの項が `e`」だった。`isCodeW_iff` は変わらず
`IsCodeW (L ω) ωZ e ↔ ∃ φ : Fm, e = φ.code`。U-18 に残るのは有界化の枠が `ω` でなく
`L ω` である点（根本 B）だけになった。

### 要忠実化修正（著者判定 F の 13 件、4 グループ並列）

著者が 1 件ずつ処遇を判定し、13 件を「要忠実化修正」とした。根が同じものを 4 グループに
まとめ、偵察 → グループ逐次実装 → 検証で処理した。ビルド緑（8538 jobs）、`sorry` 0、
主定理の公理は `[propext, Classical.choice, Quot.sound]` のまま。新規 7 ファイル 2,486 行。

**解消（4 件）**

| 件 | 対応 |
|---|---|
| U-69 | 相対化 `Fm.relTo` を論文どおり「**非有界**量化だけを M に制限」にした。`Fm` は ⊥, =, ∈, →, ∀ の 5 構成子で、有界量化 `∀u ∈ a. ψ` は `∀u (u ∈ a → ψ)` の糖衣である。その形かどうかを構文で判定する述語 `isBallBody` を入れ、`relTo m (∀i φ) = if isBallBody i φ then ∀i (relTo m φ) else ∀i (i ∈ m → relTo m φ)` とした。結果 `relTo m (∀u ∈ a. ψ) = ∀u ∈ a. relTo m ψ` が定理 `relTo_ball` として成り立つ |
| U-60 | 定義 15.4 の `Rel_k(ξ,η)` を字面どおりにした。定義 15.2 の固定公式を `stFm k` として名づけ、`stRelFm k := Fm.relTo 3 (stFm k)` を論文の `St_k(ξ)^M` そのものとして置き、`RelKP := Ord(ξ) ∧ Ord(η) ∧ ξ ∈ η ∧ ∃M ∃c (LCode(η,M,c) ∧ Sat D (stVal h w ξ M) (stRelFm k))` にした。M の推移性・非空性・パラメータ所属は定義から消え、橋渡し補題 `sat_stRelFm_iff` の仮定へ移って `M = L_η` から解消される。Δ₀ 性 `delta0Def_stRelFm` は**側条件なし**で出る（補題 15.5(2) の無条件主張と一致） |
| U-42 | 補題 12.2(1) の代入を新設した。`Subst.lean` に `φ[x := y]` と `sat_subst`（領域に条件が一切要らない）、`SynSubst.lean` に符号上のグラフ `SubstW` と `delta0_substW`（グラフが Δ₀）・`substW_iff`（正しさ）。(8.3) のスキーマ `GraphW` のインスタンスとして書いた。`TraceW` の `all` 規則が子のコードを渡さないので、trace の値を `⟨入力コード, 出力コード⟩` の対にして解決した |
| U-31 | 補題 10.3 の 2 つの欠陥を両方直した。前半（仮定が広い）: 上界 `β` を導入し `sigma1_recursion_le` として仮定・結論を `∀ ξ < β` / `∀ ξ ≤ β` に狭め、`sigma1_recursion_existsUnique` を `β := η+1` の系にして「∀ ξ ≤ η、dom = η+1」という論文の字面に一致させた。後半（F を外から与える）: 論文の `Φ(ξ,g,y,p̄)` に当たる `StepRel` を置き、`recFun` を順序数上の整礎再帰で作って、F を仮定でなく**結論側の存在物**にした |

**一部を解消（5 件）**

| 件 | できたこと / 残り |
|---|---|
| U-41 | 補題 12.2 の 8 操作のうち **6 つ**（変数改名・代入・相対化・交代ブロックの付加・双対化・padding）の符号上グラフを作り Δ₀ 性と正しさを証明。12.2(2)(3) は完全、12.2(1) は 5 項目中 3 項目。残るのは束縛変数の衝突回避と有限連言・選言の結合で、どちらも新変数を `i ↦ i + K` でずらす操作を符号上で書く必要があり、ω 上の加法の Δ₀ グラフが無いことで止まっている |
| U-68 | 前半（グラフの Δ₀ 性が無い）は U-41 の 6 操作で大部分が解消。**後半は監査の誤り**（下記） |
| U-30 | 上界つき Σ₁-再帰に `β := ω` を代入して ω-再帰の定理を 2 通り作った（`sigma1_recursion_omega`, `sigma1_recursion_paper_omega`）。論文の「同様に自然数段階の ω-再帰も存在する」に対応する定理ができた。ただし**まだ使っていない**。論文が ω-再帰を使う箇所は今も ℕ 上の外部帰納法のまま |
| U-32 | 補題 10.4(2) の推移閉包 `exists_tc` を新設し、10.4(3) を `exists_puCl_superset` として立て、論文が「10.4(3) を … に適用」と書く 3 箇所すべてで実際に使うようにした。補助集合 U が具体的な階層段から抽象的な推移的 PUCl 集合になった。残るのは 10.4(3) の**証明の筋**（論文の第二の ω-再帰でなく極限段で作っている） |
| U-56 | (14.1) の外側の量化を論文どおり `∀e ∈ ω ∀a ∈ M` の形にした（`∀ e ∈ M` → `∀ e ∈ h`）。残るのは固定パラメータが 2 個であること（U-25 と同根）と、定義 14.2 に残る M の推移性等の前提（U-60 の §15 版と同型で、約 100 行で直せる見込み） |

**解消せず（4 件）** — U-25・U-38（媒体の ℕ 化。実測で新規 3,000〜5,000 行、書き換え対象 23 ファイル 14,473 行）、U-33（主張でなく証明の筋の差。800〜1,200 行で得られる定理は現状と同一）、U-34（KP ⊢ 化。証明体系 + 導出への帰納法で 1,500〜2,500 行、内部導出化が 3,000 行超）。

### U-48 は U-47 の副産物として解消していた（1 件）

補題 13.3 の Lean 版が論文の仮定「`a` が適切な代入」を落としている、という指摘だったが、
U-47（定義 13.2 の行列から ∅-パディングを除いて論文どおりの `Asn_A(e,a)` に戻す修正）に伴って
`BaseCorrect` と `BaseCorrectV` の両方に `∀ k ∈ fv φ, k ∈ dom(a)` が入っている。
以前は覆わない変数を ∅ と評価する規約だったので覆う条件が要らなかったが、
いまは `Asn` が覆いを要求するのでこの仮定が必要になり、実際に置かれている。
もう一つの指摘「`e ∈ W` を仮定しない」は差異ではない。`e` は外部の標準式の符号で、
符号は `L ω ⊆ W` の元なので `e ∈ W` は自動である。詳細は [diff/P-11.md](../diff/P-11.md)。

### U-68 後半は監査の誤りだった

「補題 12.2 の同値性の成立範囲が非空推移的領域に狭められている」という指摘は誤り。
非空性・推移性を要求しているのは `Defin.lean` の定義可能性クラスの道具立てであって、
補題 12.2 の主張ではない。今回作った補題も領域条件を課していない
（`Fm.sat_subst` は領域に条件が一切無く、`BF.sat_dual` も条件なし、`BF.Sig.pad` は
領域が非空であることだけ）。しかもそこでの推移性は Δ₀ の有界量化 `ball i j` を
「∀x ∈ v j」と読むために不可欠で、論文も §13.2 の Δ₀-絶対性を推移的集合に限って主張している。

### その他（6 件）

| 件 | 対応 |
|---|---|
| U-10・U-15 | 論文に対応物の無い閉形式 `ParForm` / `AncForm` と、それによる完全特徴付け `anc_tA_iff` を削除。命題 7.1 は (C1)(C2)(C3) から直接示す 2 補題（`hasParent_tA_of_hasParent_A`, `hasParent_tA_lead`）に置き換えた。未使用だった `anc_expand_iff`・`parForm_of_parent_A`・`hasParent_tA_of_parForm` も削除 |
| U-71 | `anc_tA_cases` を定理 6.3 経由でなく `lt_pos_cases`（純粋に位置の三分）から証明。§19 場合 (b) がコピー補題に依存しなくなり、備考 19.2 の表と一致した |
| U-72 | 論文どおり BM4 上の関係にした。`Elt r := {A : Arr r // Reachable r A}` を定義し、`R r A B := 0 < B.len ∧ ∃ n, A = B[n]`（`Reachable` の連言を除去） |
| U-74 | 未使用かつ論文に対応物の無い `E_len` と `seq_succ_shift` を削除 |
| U-40 | `KPAxCode` から `NotFreeW h w x c`（領域変数 x が φ に自由でない）を削除。標準の KP の ∆0-分出・∆0-収集は領域変数をパラメータに使うインスタンスを許すので、これは論文より狭い制限だった。真理側 `satIn_sepAx` / `satIn_collAx` は元から `y ∉ FV(φ)` しか使っておらず、削除は素直に通った。**残る i,j,x,y の相異性は「不要」ではない**: `ball i x` は i = x だと束縛が壊れ、そもそも Collection の形にならない。相異な変数名を取ることは常にできるので公理集合は狭まらない |

## W 原文の飛躍（形式化の範疇）（32 件）

### W-1. (3)(4) の論文証明は各一行だが、Lean では鎖の最終ステップ抽出 (anc_last_step) と TransGen 上の帰納法が必要で、しかも (4)→(3) と依存順序が論文と逆になっている。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

補題 2.2 の証明末尾：「(3) は親が高々一つであることから従い、(4) は同じ一本の鎖上では列番号が小さい方が遠い祖先であることから従う。」

##### lean

lean/Bm4/Basic.lean:126-133 parent_unique、135-143 anc_last_step、145-163 anc_of_anc_of_anc_of_lt、165-196 ancEq_single_finite_chain

#### 判定

論文: (3) を「親が高々一つ」から導き、(4) を「(3) の一本の鎖上での位置関係」から導く。すなわち (3) → (4)。
Lean: parent_unique を示したうえで、まず anc_last_step（anc A k j i なら ∃ b, ancEq A k j b ∧ parent A k b i）を取り、
anc_of_anc_of_anc_of_lt（= 2.2(4)）を TransGen に関する帰納法で直接証明する。
そのうえで ancEq_single_finite_chain（= 2.2(3)）の (b) を anc_of_anc_of_anc_of_lt から組み立てる。すなわち (4) → (3)。
論文の「同じ一本の鎖上では列番号が小さい方が遠い祖先」という一行は、この帰納法（鎖を末尾から剥がして親の一意性を使う）を要約したもので、実質は書かれていない。
飛躍は形式化の範疇に収まる小さいものなので W。

### W-2. z_0 の段で「その間に別の構造的候補はない」と言うとき、q_{a+1} の構造的候補と z_0 の構造的候補が区別されておらず、j > 0 の段でのみ与えられる推移性の議論が j = 0 にも必要。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

補題 4.1 の証明、(4.3) の直後：「従って q_a は z_0 の構造的かつ有効な候補であり、その間に別の構造的候補はないので z_0 の直接の k-親である。」

##### lean

lean/Bm4/Basic.lean:312-339 anc_of_parent_of_between（w に関する強帰納法。z_0 を特別扱いしない）

#### 判定

z_0 の直接の k-親が q_a であるためには、q_a < y < z_0 なる y が z_0 の構造的 k-候補でないことが要る。
しかし z_0 は「(q_a, q_{a+1}) 内の q_{a+1} の構造的候補の最小元」として取られている。y が z_0 の候補であることから y が q_{a+1} の候補であることを出すには、
k > 0 では y ≺_{k−1} z_0 ≺_{k−1} q_{a+1} という (k−1)-祖先関係の推移性が要る。この議論は 3 行後の j > 0 の段でしか述べられていない。
（k = 0 でも「位置関係から」という同型の一言が要る。）
Lean はこの区別を回避して z_j の列挙をやめ、w に関する強帰納法にしている：w の親 z を exists_parent_of_valid で取り、u ≤ z を示し、
z が v の構造的候補であることを cand の推移性から出して帰納法の仮定を z に適用する。z_0 の特別扱いは現れない。
飛躍は小さく形式化の範疇なので W。

### W-3. 指定変数を v0 に正規化する一般性の損失の無さを、論文は束縛変数改名規則で片付けるが、実際に要るのは自由変数 v_i ↔ v_0 の互換である。

**節**: 09 §9 L のコード

#### 差異

##### 原文

§9.1：「e は指定自由変数 v0 と有限個のパラメータ変数だけを持ち…束縛変数改名規則を固定しているので、この配置に一般性の損失はない。」

##### lean

lean/Bm4/SetTheory/LCode.lean:897 swap0、899-929 swap0_involutive / swap0_injective / swap0_eq_zero_iff / swap0_apply_self、981 exists_defInp_of_definableOver（`Fm.rename (swap0 i) φ` と fv_rename, sat_rename を使用）。

#### 判定

Def(A)（L.lean:17 DefinableOver）は指定変数を任意の v_i として許すので、DefInp が v0 固定であることと突き合わせるには v_i と v_0 を入れ替える置換が要る。
Lean は互換 swap0 i を全変数に適用する rename を作り、その単射性から fv と充足が保たれることを示して初めて exists_defInp_of_definableOver（(9.1) の「定義可能部分集合は必ず入力で名指される」側）を得る。
論文はこの構成を書かず、「束縛変数改名規則を固定しているので一般性の損失はない」の一行で済ませている。束縛変数の改名は捕獲回避には要るが、必要な主張そのもの（自由変数の互換）ではない。飛躍は形式化の範疇に収まる大きさなので W とした。

### W-4. 系 10.2 は補題 10.1 の主張ではなく証明内部の集合を使っており、Lean は補題 10.1 を強形に述べて埋めている

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

系 10.2 の証明冒頭：「補題 10.1 の証人対収集により、全ての候補値 y と補助証人を含む一つの集合 B を得る」

##### lean

Recur.lean:155 `sigma1_collection`（結論が `ExsIn b l`、すなわち証人ブロック l の全変数の値が b に入る）

#### 判定

補題 10.1 の主張は ∀x∈a ∃y∈b ψ(x,y,p̄) のみで、Σ1 の補助証人 z が b に入ることは述べていない。ところが系 10.2 の証明は F を ∆0 条件の Separation で切り出すため「ψ の補助証人も B に有界化」する必要があり、補題 10.1 の主張ではなくその証明の中間物（d = ⋃⋃c）に依存している。Lean は `sigma1_collection` の結論を強形（証人ブロック l の全変数が b に入る `ExsIn b l`）で述べており、この飛躍を主張レベルで埋めている。飛躍自体は小さく形式化の範疇。

### W-5. 直積 ω × A^<ω の存在を補題 10.4(1) に帰しているが、10.4(1) は直積について何も述べていない

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.5(1) の証明冒頭「補題 10.4(1) により A^<ω と ω × A^<ω が存在する」

##### lean

SatInL.lean:587 `kprod_mem_L`（546 `kprod_singleton_mem_L` と Σ1-Collection を使う独立の証明）

#### 判定

補題 10.4(1) の主張は「任意の A に対して A^<ω が存在する」だけで、直積の存在は含まれない。KP における a × b の存在は別の議論（∆0-Collection を 2 回使う）を要し、論文ではそれが系 10.2 の証明中に傍論として一行で触れられているのみで、番号付きの主張にはなっていない。Lean はこれを独立の補題 `kprod_mem_L` として、まず {x} × Y を Separation で作り（`kprod_singleton_mem_L`）、次に x ∈ X 上の Σ1-Collection で束ねる、という 60 行余りの証明で与えている。飛躍は小さく形式化の範疇。

### W-6. L_θ（θ 極限）が対・和集合・Δ0-分離で閉じることを Lean は極限性だけから証明するが、論文にはこの主張も議論も無い。

**節**: 11 §9 L の階層

#### 差異

##### 原文

§10 冒頭の KP 公理列挙と許容順序数の定義（p.19）。L_θ が Pairing・Union・Δ0-Separation を満たすことは、あくまで L_θ |= KP から来るものとして扱われる。

##### lean

Adm.lean:47-95 `pair_mem_L_succ`/`pair_mem_L_of_limit`/`singleton_mem_L_of_limit`/`kpair_mem_L_of_limit`、Adm.lean:98-126 `sUnion_mem_L_succ`/`sUnion_mem_L_of_limit`、Adm.lean:129-211 `sep_mem_L_of_limit`

#### 判定

論文は L_θ の閉包性を独立には一切述べず、必要なときは L_θ |= KP から取り出す
（補題 13.1 の証明「Pairing と Union を L_θ 内で用いると ξ+1 = ξ ∪ {ξ} ∈ L_θ」）。
Lean は逆に、KP を一切使わず θ の極限性だけから {a,b} ∈ L θ、⋃a ∈ L θ、
Δ0-分離集合 ∈ L θ を証明している（Adm.lean:78, 122, 129）。
これは論文の定義に飛躍があることも示す：θ>w 極限であれば Δ0-Collection 以外の
KP 公理は L_θ で自動的に成り立つので、§10 の定義に列挙された 7 公理は
許容性の実質的な制約になっていない。論文はこの点に触れていない。
なお補題 13.1 の上記一文は、KP の Pairing/Union が「上位集合の存在」の弱形である
ため実際には Δ0-Separation を 2 回使う必要があり、論文はそこを省いている
（Lean 側の該当証明は AdmKP.isSuccLimit_of_kpTrue で、分離を明示的に使う）。
形式化の範疇に収まる小さい飛躍なので W とした。

### W-7. 論文は Def と L 階層を定義せず文献に委ねているため、Lean が具体版を確定させており、指定自由変数とパラメータ列の扱いが論文の DefInp と形が異なる。

**節**: 11 §9 L の階層

#### 差異

##### 原文

§9 全体。Def(X) と L_ξ そのものの定義は与えられず、補題 9.2 の証明中で「H(ξ) 上で有限個のパラメータを用いて一階定義可能な部分集合全体」「H(ξ+1) = Def(H(ξ))」「H(λ) = ∪ L_ξ = L_λ」と使われるだけ（§8 冒頭で Barwise[3]・Jech[4] に委ねている）。

##### lean

L.lean:17-19 `DefinableOver`、L.lean:22-23 `Def`、L.lean:56-58 `L`、L.lean:60-69 `L_zero`/`L_succ`/`L_limit`

#### 判定

論文 §9.1 の DefInp_X(e,a) は「e は指定自由変数 v0 と有限個のパラメータ変数だけを
持ち、a(j) が第 j パラメータ変数の値、全ての値は X に属する」と、v0 を固定し
パラメータを有限列 a で与える形。
Lean の DefinableOver M X は、指定変数 i を存在量化し（v0 に固定しない）、代入は
全域関数 v : N → ZFSet で、制約は (fv φ).erase i 上のみ。
また論文 §8 の式構成子 (8.1) は Eq, In, Neg, Conj, Ex だが、Lean の Fm の原始構成子は
falsum, eq, mem, imp, all（Fm.lean:13-18）で、or/and/ex/ball/bex は派生。
いずれも相互定義可能で Def(M) の値は一致するが、論文は Def / L のどの版を採るかを
明示していないので、形式化がここを埋めている。
極限段は Ordinal.limitRecOn の非零極限ケースで (9.3) と一致、L 0 = ∅ は H(0) = ∅ と一致。

### W-8. 「L_o に属する順序数はちょうど o 未満の順序数」という事実を Lean は L.lean で証明しているが、論文には主張としても証明としても存在しない。

**節**: 11 §9 L の階層

#### 差異

##### 原文

§9・§10 には対応項目が無い。「L_α ∩ Ord = α」は定理 17.1 の証明中（p.29 付近）で証明抜きに使われるだけ。

##### lean

L.lean:202-239 `toZFSet_mem_L_iff`、およびその補助 L.lean:123-140 `rank_Def_le`/`rank_L_le`/`rank_lt_of_mem_L`、L.lean:143-199 `transF`/`ordF`/`satV_ordF`

#### 判定

論文で L_α ∩ Ord = α が現れるのは定理 17.1 の証明の一行だけで、番号付き項目としては
述べられていない。§9 は L 階層の定義自体を置いていないので当然その基本性質も無い。
Lean は L.lean:202 で toZFSet_mem_L_iff : ξ.toZFSet ∈ L o ↔ ξ < o を証明し、
これをリポジトリ内 15 箇所で使っている（Adm.lean:39 の w ∈ L θ も含む）。
そのために rank_L_le（rank(L o) ≤ o）と、Δ0 式 ordF（推移的かつ元が全て推移的）を
用意している。ordF は Foundation を前提とした順序数の特徴づけで、L.lean 内でのみ使用。
論文が文献に委ねた部分を形式化が埋めている小さな飛躍なので W とした。

### W-9. Lean はスキーマの「インスタンスの全称閉包」だけを認識するが、論文は「一インスタンス」としか書かず閉包を要求していない。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

§11 冒頭「KPAx(d) を、d が KP の公理または公理スキーマの一インスタンスのコードであることを表す ∆0 述語とする」＋定義 11.1 の ⟨d,∅⟩∈S

##### lean

KPAx.lean:699 KPAxCode（スキーマ枝は AllsW h w q d ∧ ∀k∈w NotFreeW h w k d を要求）、KPAx.lean:898 fv_eq_empty_of_kpAxCode

#### 判定

論文の字義どおり開いたインスタンス（例: 自由なパラメータ変数を持つ ∆0-Separation の実例）を KPAx が認識すると、定義 8.3 の第 2 条件により T の元は Asn_A(e,a) を満たす対だけなので、空代入 ∅ は自由変数を覆わず ⟨d,∅⟩∈S は決して成り立たない。よって KPTrue(M,U,S) は充足不能となり、補題 11.2 の ⟸ 方向が偽になる。Lean はこれを避けるため d を alls l ψ の形（かつ d は閉）に限定し、fv_eq_empty_of_kpAxCode で「認識されるコードは文のコード」を証明している。論文には閉包を取るという言明が無く、この読み替えは形式化側の補いである。

### W-10. 証人変数 y が φ に自由に現れないという側条件が論文に無いが、これが無いとスキーマのインスタンスが偽になる。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

§11 冒頭「公理スキーマの一インスタンス」（側条件の記述は §8 の「公理スキーマのインスタンス判定は有限構文木上の決定的な再帰で定まる」だけ）

##### lean

KPAx.lean:699 KPAxCode（NotFreeW h w y c を要求）、KPSat.lean:147 satIn_sepAx / 184 satIn_collAx（hyfv : y ∉ Fm.fv φ）

#### 判定

Lean は sepAx φ i j x y, collAx φ i j x y のインスタンスに y ∉ FV(φ) を課す。この条件が無い場合、例えば φ = ¬(j∈y) を取ると Collection の前件「各 i∈x に対しパラメータ y の外の j が存在する」は真だが、後件は j∈y ∧ j∉y を要求するので偽になり、そのインスタンスは Lη で成り立たない。論文は側条件を一切述べていないので、字義どおりの KPAx を取ると KPTrue が偽になり補題 11.2 の ⟸ が成り立たない。形式化が補った条件である。

### W-11. 補題 11.2 の右辺が「Lη |= KP」ではなく「ω<η ∧ η 極限 ∧ Lη で ∆0-Collection」に置き換わり、その同値性の証明が §11 の外から持ち込まれている。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

補題 11.2 の右辺「Lη |= KP」（許容順序数の定義は §10 の「Lθ |= KP」）

##### lean

AdmKP.lean:317 admKP_iff（右辺は IsAdmissible η）、Adm.lean:27 IsAdmissible = ω<θ ∧ 極限 ∧ Delta0Collection (L θ)、AdmKP.lean:172 omega_lt_of_kpTrue, 255 isSuccLimit_of_kpTrue、KPSat.lean:258 satIn_kpAx, 309 isAdmissible_of_kpTrue

#### 判定

Lean の IsAdmissible は意味論的な Lη |= KP ではなく 3 条件の連言。両者の同値性のうち、(a) ω<η と η の極限性を KP 公理の真理から導く部分（AdmKP.lean:150-300、Infinity から階数で ω<η、Pairing/Union/∆0-Sep で後続閉性）は論文では補題 13.1 に相当し §11 には無い、(b) 任意の極限 η>ω で Lη が Extensionality, Empty Set, Pairing, Union, Infinity, ∆0-Separation, Set Induction を満たすこと（KPSat.lean の satIn_* 群）は論文では §15 の Θ=ω1V の場合にしか書かれていない。§11 だけを読むとこの橋渡しは与えられていない。

### W-12. ⟸ 方向で必要な「充足コードの真理部分が正しい」（補題 8.4）の引用が論文の証明に無い。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

補題 11.2 の証明「右から左は系 10.6 と Lθ 内の充足コードの存在による」

##### lean

AdmKP.lean:317 admKP_iff の ⟸ 分岐（lcode_exists_in_L と satCode_exists_in_L のあとに satCode_correct と satIn_kpAx を使う）

#### 判定

Lη |= KP から KPTrue(Lη,U,S) を出すには、得られた S が実際に Lη の真理関係を表していること（補題 8.4 の正しさの部分）を使って、各 KP 公理コード d について ⟨d,∅⟩∈S に翻訳する必要がある。論文はこの一歩を ⟹ 方向でのみ 8.4 を引いており、⟸ 方向では「系 10.6 と充足コードの存在」しか挙げていない。Lean は satCode_correct（SatCode.lean:560）と satIn_kpAx を明示的に使っている。小さな飛躍。

### W-13. 論文が定義の前提として置く ν の条件を、Lean は BlkUpd 関係の連言に取り込み、そのため関係が余分な引数 w(=ω) を取る。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

BlkUpd の定義の前提「相異なる変数番号の有限列 ν」。§13 では t についてのみ「t が ν と同じ長さの有限列でない場合、BlkUpd は偽である」と述べる

##### lean

Bm4/SetTheory/BlkP.lean:23 def IsBlkUpdP の末尾 2 連言（(∃ n ∈ w, IsDom ν n) と nuInj）、:37 nuSeq、:41 nuInj

#### 判定

論文は ν が退化した場合（値に重複がある、有限列でない）の BlkUpd の真偽を規定しない。
t については §13 で「偽である」と明示するが、ν については何も言わない。
Lean は IsBlkUpdP の定義自体に「ν は w の元を定義域とする有限列」と
「ν の値は相異なる」を入れて偽にし、この穴を埋めている。
結果として関係が 4 引数でなく 5 引数 (w a ν t b) になる。
付随して、論文は Δ₀ 性の限量域に ω を挙げるが、Blk.lean:631 delta0_isBlkUpd は
自由変数 {a, nu, t, b} だけで Δ₀ であり、ω は「ν が有限列」の側条件にしか効かない。

### W-14. Lean 側に論文に無い仮定 ∅ ∈ A が付いている。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

BlkUpd の定義（padding の値として ∅ を使う）

##### lean

Bm4/SetTheory/BlkP.lean:191 exists_blkUpdP の仮定 hemp : ∅ ∈ A、および :116 seqVal_of_isBlkUpdP、Blk.lean:385 exists_padSeq

#### 判定

論文は padding の値を ∅ と決めるだけで、代入の値域について何も条件を付けない。
しかし §13 の Asn_A は「値域が A に含まれる」ことを要求するので、
padding された b が再び A 値の有限代入であるためには ∅ ∈ A が必要になる。
Lean はこれを exists_blkUpdP の仮定 hemp : ∅ ∈ A として明示している。
論文はこの点に触れていない（推移的で空でない A なら自動だが、明言は無い）。

### W-15. 補題 13.1 は担当ファイルに対応物が無く（Lean は θ > ω と極限性を IsAdmissible の定義に組み込む）、証明の「Pairing と Union を用いると ξ+1 = ξ ∪ {ξ} ∈ L_θ」は ∆0-Separation の使用が省かれている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

補題 13.1（θ 許容 ⟹ θ > ω、θ は極限、ω ∈ L_θ）とその証明

##### lean

担当 4 ファイルに対応物なし。内容は lean/Bm4/SetTheory/Adm.lean:26-39 (IsAdmissible, omega_lt, isSuccLimit, omega_mem) と lean/Bm4/SetTheory/AdmKP.lean:172, 255 (omega_lt_of_kpTrue, isSuccLimit_of_kpTrue)

#### 判定

Lean の IsAdmissible θ は「ω < θ ∧ θ は極限 ∧ L θ が ∆0-Collection を満たす」で、補題 13.1 の 2 つの結論は定義そのもの、ω ∈ L_θ は IsAdmissible.omega_mem。L_θ |= KP から導く版は AdmKP.lean にあるが担当外で、そこでの極限性の証明は Pairing→∆0-Separation→Pairing・Union→∆0-Separation の 4 段を踏む。KP の Pairing/Union を弱形（∃z (a ∈ z ∧ b ∈ z) 等）で取ると、論文の「Pairing と Union を用いると ξ ∪ {ξ} ∈ L_θ」だけでは正確な対と和が取れず ∆0-Separation が要る。論文は §10 冒頭で公理を名前で挙げるだけで形を固定していないため、強形を意図しているなら飛躍ではない。飛躍としても 1 行で埋まる小さいものなので W とした（ω ∈ L_θ の「最小の帰納的集合」の方は §10 冒頭で根拠が与えられているので飛躍ではない）。

### W-16. Λ < Θ と h(γ) < Θ に必要な ℵ1 の正則性（cf(ω1) > ω）の使用が論文では明示されていない。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 の主張 (16.1) は Λ < Θ を要求するが、証明は「Λ = sup_{s<w} γ_s とする」と置くだけで Λ < Θ（Λ が可算であること）を明示的に正当化していない。同様に h(γ) < Θ も「取れる」で済ませている。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Skolem.lean:205 Lam_lt_omega1、/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Skolem.lean:115 nextStage_lt

#### 判定

論文が ω1^V の正則性に言及するのは Δ0-Collection の段だけで、
第2段の h(γ) < Θ と、結論に必要な Λ = sup_{s<w} γ_s < Θ については
根拠を書いていない。
Lean は両方で cof(ω1) = ℵ1 と Ordinal.iSup_lt_of_lt_cof を明示的に使う
（nextStage_lt は可算個の witStage の上限、Lam_lt_omega1 は ω 個の chain の上限）。
数学的には標準的な事実なので飛躍は小さいが、(16.1) の Λ < Θ という結論そのものが
論文の証明本文では一度も述べられていない点は形式化で補われている。

### W-17. 補題 11.2 / 15.5(2) を適用するのに必要な副条件（y'_i が順序数、x < y'_i、y'_i < y'_j）の根拠が論文では明示されていない

**節**: 18 §17 有限行反映

#### 差異

##### 原文

証明末尾「L_α |= Adm(y'_i) と補題 11.2 から各 y'_i は外部でも許容順序数である。また y'_i ∈ L_α ∩ Ord = α なので y'_i < α」および「L_α 内の Rel_k 条件と補題 15.5(2) から (17.2), (17.3)」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Reflect.lean:374-389 (hFord, hFrank, hFlt, hFadm)、411-421 (hle, hXlt)、428-429, 439-440

#### 判定

補題 11.2 は「θ 許容、η < θ」を前提とするが、論文は L_α |= Adm(y'_i) から先に「y'_i は許容順序数」と結論し、
そのあとで y'_i ∈ L_α ∩ Ord = α と述べる。反映で得た証人は L_α の任意の元であり、それが順序数であることは
Adm の定義 11.1 に含まれる LCode(η, M, c) から取り出す必要があるが、論文はその一歩を書いていない。
Lean は hFord で AdmKP の LCode 成分から IsOrdinal を取り出し（374-378）、次に hFlt で y'_i < α、
最後に admKP_iff（補題 11.2 相当）で許容性、という順に並べ替えている。
また補題 15.5(2) は ξ < η < θ を要求するので (17.2) には x < y'_i、(17.3) には y'_i < y'_j が要るが、
論文はこれらが (17.1) と順序条件から出ることを書かない。Lean は hle・hXlt から hlt を作って明示している（428, 439）。
いずれも一行で埋まる小さな飛躍。

### W-18. Lean は有界量化の束縛変数 i と限界変数 j が相異なることを全ての有界量化補題で要求するが、原文にこの副条件は無い。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

§8.2 p.16, §12 p.23, §13 などの「全量化を e ∈ ω, a ∈ U, … に有界化して書けるため ∆0 である」という記述全般

##### lean

Defin.lean:107 Delta0Def.ball, 130 Delta0Def.bex, 361 SigmaDef.bex, 392 PiDef.ball（いずれも仮定 hij : i ≠ j）

#### 判定

Fm.ball i j φ = all i (imp (mem i j) φ) という定義のため、i = j だと限界そのものが束縛されてしまい ∆0 性も意味も壊れる。そこで Fm.lean:225 の IsDelta0.ball が i ≠ j を課し、それが Delta0Def.ball/bex、SigmaDef.bex、PiDef.ball へ伝播する。
論文は ∀u ∈ a, ∃u ∈ a を暗黙に u ≠ a と読んでいるだけで、この条件を明示していない。
実害は無く形式化の範疇に収まる小さい飛躍。分類 W。

### W-19. 付加する変数が既に最外ブロックに現れている場合の扱いが原文に無く、Lean は場合分けして領域の非空性で処理している。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

補題 12.2 (1)「交代ブロックの付加」および定義 12.1「各 x⃗i は相異なる変数からなる空でない有限ブロック」（p.23）

##### lean

Defin.lean:202 SigmaDef.ex / Defin.lean:278 PiDef.all（i ∈ l の分岐）

#### 判定

SigmaDef.ex は φ = exs l ψ（IsSigma (q+1) φ）に ∃x_i を前置して Σ̂q を保つ、という補題 12.2 (1) の操作にあたる。
しかし i ∈ l のとき前置すると同一ブロック内に同じ変数が二度現れ、定義 12.1 の「ブロック内は相異なる変数」に反する。Lean はこの分岐で式をそのまま返し、i ∉ fv (exs l ψ) と GoodDom の非空性 (∃x₀, D x₀) を使って ∃x (D x ∧ P D (update v i x)) ↔ P D v を正当化する（Lean 側のコメントも「prefixing it would violate Definition 12.1」と明記）。
原文はこの衝突可能性に触れず、12.2 (1) の「束縛変数の衝突回避」で暗黙に処理しているとみられる。
分類 W。

### W-20. 相対化の ∆0 性と正しさに Lean は m ∉ vars φ / m ∉ s、M の推移性・非空性、パラメータ ∈ M という副条件を要するが、原文はどれも述べていない。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

§14 p.27「固定公式 TrΣ̂j の全ての非有界量化を M に制限した相対化を TrΣ̂j^M(e,a) と書く。これは M をパラメータとする ∆0 公式である」／§15 p.28「Stk(ξ)^M を全非有界量化を M に制限した相対化とする。これは ∆0 である」／定義 15.4 (15.4)

##### lean

Rel.lean:141 delta0Def_relTo, 149 delta0Def_relTo', 160 SigmaDef.relativize, 177 PiDef.relativize

#### 判定

delta0Def_relTo は m ∉ vars φ を要求する。delta0Def_relTo' が exists_avoid で束縛変数を改名して m ∉ fv φ まで緩めるが、原文は §14/§15 の相対化の箇所でこの改名の必要性に触れていない（補題 12.2 (1) の一項目としてのみ存在）。
SigmaDef.relativize / PiDef.relativize は m ∉ s（相対化に使う変数がパラメータ変数と異なる）を要求し、正しさ Q D v ↔ P (· ∈ v m) v には GoodDom (· ∈ v m)（v m が推移的かつ非空）と ValD (· ∈ v m) s v（全パラメータが M の元）を要求する。
原文の (15.4) には Stk(ξ)^M に対する M の推移性も ξ ∈ M も書かれておらず、補題 15.5 の証明で「補題 9.2 により M = Lη」と分かってから初めて回収される。
分類 W（形式化の範疇に収まる小さい飛躍）。

### W-21. 論文は不変量を (I1)(I2) の 2 つとするが、場合 (d) は「G 上で f_q = f」という第三の不変量を使っており、Lean はそれを `onG` として明示的に追加している。

**節**: 20 §18, §19 安定ラベルと降下

#### 差異

##### 原文

§19 の不変量 (I1)(I2)、および場合 (d)「G のラベルと新しいコピーのラベルは元の f と同じなので、元の安定性をそのまま適用できる」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Label.lean:93-97 `BadRoot.Inv` の `onG`（使用は :272-276 の場合 (d)）

#### 判定

論文が帰納構成で保つと宣言する不変量は (I1)「最後のコピー B_q の第 i 列のラベルは y_i」と
(I2)「B_q より前の全ラベルは α 未満」の 2 つだけ。
しかし場合 (d)（祖先が G、対象が新コピー）は、E ∈ G のラベルが元の f(E) のままであることを
使って f の安定性 E ≺^A_k D_j^{(0)} ⟹ f(E) ◁_k y_j を移す。(I2) は「α 未満」しか言わないので
これは (I1)(I2) からは出ない。構成規則 1 と f_0 = f↾c から帰納的に従う事実だが、
不変量としては述べられていない。
Lean は `Inv` に onG : ∀ x < p, g x = f x を第三の成分として持ち、:274 で `hg.onG` を使う。
飛躍は小さく形式化の範囲に収まるので W。

### W-22. 論文は E_r から A_0 までの中間配列が全て非空であることを言わずに命題 19.1 を順次適用している。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

定理 21.1 の証明「補題 20.1 と命題 19.1 を E_r から A_0 までの有限展開列に順に適用し、A_0 の安定ラベル f_0 を得る」

##### lean

lean/Bm4/Main.lean:38-48 `reachable_stable`（`expand_of_len_zero` による空配列の除外）

#### 判定

命題 19.1 は「A が非空」かつ「A[N] が非空」を仮定する。E_r から A_0 までの有限列に順に適用するには、途中の配列が全て非空であることが要る。
論文は A_t の非空性を無限列側でしか仮定しておらず、E_r → A_0 の中間項の非空性には触れない。
Lean は帰納の step で `A.len = 0` の場合を分け、定義 5.1 の ℓ=0 節（`expand_of_len_zero`）から `expand A N = A` なので結論の非空性 h0 と矛盾する、として明示的に潰している。
埋めるのは容易なので W。

### W-23. 「一本の有限鎖をなす」という非形式的な文言を、Lean は 3 つの連言（ReflTransGen との一致・任意の二元の比較可能性・有限性）に読み替えている。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

補題 2.2(3)：「固定した i, k に対し、i の非狭義 k-祖先全体は一本の有限鎖をなす。」

##### lean

lean/Bm4/Basic.lean:165-196 ancEq_single_finite_chain

#### 判定

論文の文言は数学的命題としては未確定。Lean は次の 3 連言に固定した。
(a) ∀ u, ancEq A k u i ↔ ReflTransGen (parent A k) u i（鎖であること）
(b) ∀ u v, ancEq A k u i → ancEq A k v i → u < v → anc A k u v（一本、分岐しないこと）
(c) {u | ancEq A k u i}.Finite（有限であること）
読み替え自体は妥当だが、原文の主張とこの 3 連言が同値であることは論文には書かれていない。
なお (b) は論文 2.2(4) の非狭義版で、論文 2.2(4)（狭義祖先どうし）より仮定が弱い分だけ強い形になっている。
原文どおりに書けないための迂回なので U。

（監査時は U-6。著者判定 **W（形式化範疇の誤差）** により移した。上の「…ので U」は監査時の判定文である。）

### W-24. Lean には §2–4 のどの番号付き主張にも対応しない補助補題 anc_exit_point（祖先鎖の脱出点）が置かれている。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

§2–4 に対応物が無い（補題 2.2、3.1、4.1 のいずれでもない）。

##### lean

lean/Bm4/Basic.lean:296-308 anc_exit_point

#### 判定

anc_exit_point: y < p ≤ v かつ y ≺_k v なら、∃ u y' で p ≤ u、u ≼_k v、y' が u の k-親、y' < p、y ≼_k y'。
すなわち v から左へ辿る k-親鎖が境界 p を跨ぐ最初の一歩を取り出す補題。
§2–4 にはこれに当たる番号付き主張は無い（後続の §6 以降で「親鎖を左へたどり…」と非形式的に使われる推論に相当すると思われる）。
担当範囲の §2–4 に対応物が無い、という事実のみ報告する。
形式化の都合で明示化された補助補題と読めるので U。

（監査時は U-7。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-25. Lean の「公開規則」expandOfficial は原実装ではなく定義 5.1 のコピー式 tildeCol と本稿の parent をそのまま使っており、備考 7.2 の主張のうちコピー式が原規則の書き直しであるという部分に対応する形式的主張が無い。

**節**: 05 §7 標準性

#### 差異

##### 原文

備考 7.2。「式 (7.2), (7.3) により、この末尾判定と最大行の選択は、定義 5.1 の『どの行にも親を持たないか』と『最大親行 m₀』に一致する。残るコピー式は、原規則を親・祖先記法で書き直したものが定義 5.1 である。従って、標準初期配列から到達可能な配列上では、本稿の展開と BM4 ver. 4 の原規則が一致する。」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Standard.lean:169 expandOfficial、:179 expandOfficial_eq_expand

#### 判定

備考 7.2 の内容は (a) 末尾判定 A_c(0)=0 ⟺ どの行にも親を持たない、(b) 正成分を持つ最大行 = m₀、(c) 残るコピー式が原規則の書き直し、の 3 つ。論文は (a)(b) を (7.2),(7.3) で論証するが、(c) は論証せず断言している。
Lean の expandOfficial は (a)(b) だけを成分ベースに差し替えた定義であり、コピー部分は定義 5.1 と同じ tildeCol A p m s、親も本稿の parent（HasParent A m (A.len-1) の Classical.choose）を使う。
そのため expandOfficial_eq_expand は「定義 5.1 = 定義 5.1 の末尾判定と最大行を成分版に置換したもの」という主張になり、(7.2),(7.3) を代入した後は rfl で閉じる（185-193 行）。
原実装 [1,2] の規則そのものは Lean に定義が無いので、備考 7.2 の結論「本稿の展開と BM4 ver. 4 の原規則が一致する」に対応する形式的主張は存在しない。
外部実装が対象で数学的対象として書けないため U に分類したが、論文側の未証明の断言と見れば Z 寄りとも言える。

（監査時は U-12。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-26. 対象言語の構成子集合とタグ番号、符号化の媒体（自然数 vs 遺伝的有限集合）が論文と Lean で異なる。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

(8.1)：構成子を Eq(i,j)=π(0,π(i,j)), In(i,j)=π(1,·), Neg(e)=π(2,e), Conj(e,d)=π(3,·), Ex(i,e)=π(4,·) と固定し、対象言語は {=,∈,¬,∧,∃}。符号は Cantor 対関数 π による自然数。

##### lean

Code.lean:14-20 `Fm.code`（および Fm.lean:13-18 `inductive Fm`）

#### 判定

論文の言語は ¬, ∧, ∃ を原始子とし ⊥ を持たない。Lean は ⊥, →, ∀ を原始子とし ¬, ∧, ∃ は略記（Fm.lean:26-33）。
タグも一つずれる：論文 Eq=0,In=1,Neg=2,Conj=3,Ex=4、Lean falsum=0,eq=1,mem=2,imp=3,all=4。
さらに論文は π による自然数符号だが、Lean は Kuratowski 対による遺伝的有限集合（ZFSet）符号。
表現力は同じで下流の議論も対応が付くが、(8.1) の式そのものは Lean に存在しない。
形式化の都合による書き換えなので U と判断した。

（監査時は U-17。著者判定 **W（形式化範疇の誤差）** により移した。上の「…ので U」は監査時の判定文である。）

### W-27. Lean の「許容」は L_θ ⊨ KP ではなく「ω<θ ∧ 極限 ∧ Δ0-Collection」で代用され、KP の残り 7 公理の充足は形式化されていない。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

16 節冒頭・補題 16.2 の証明第1段：「まず L_Θ ⊨ KP を示す」として Extensionality, Empty Set, Pairing, Union, Infinity, Set Induction, Δ0-Separation, Δ0-Collection の 8 項目を順に検証する。許容順序数の定義は p.19 の「θ 許容 :⟺ L_θ ⊨ KP」。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Omega1.lean:105 isAdmissible_omega1（定義は Adm.lean:26 IsAdmissible）

#### 判定

論文: 許容 = L_θ ⊨ KP（KP は 8 公理）。補題 16.2 の第1段はその 8 項目を全部検証する。
Lean: IsAdmissible θ := ω < θ ∧ Order.IsSuccLimit θ ∧ Delta0Collection (L θ)。
isAdmissible_omega1 が示すのはこの 3 条件だけで、Extensionality, Empty Set, Pairing,
Union, Infinity, Set Induction, Δ0-Separation が L ω1 で成り立つという
「充足」の主張は形式化されていない（Adm.lean には pair_mem_L_of_limit,
sUnion_mem_L_of_limit, sep_mem_L_of_limit という閉包性の補題があるだけで、
∈-構造の充足としては述べられていない）。両者の同値は Adm.lean のコメントで
主張されるだけで証明されていない。分類は形式化の都合による代用と見て U とした
が、結果として論文第1段の 7 項目の検証には Lean 側の対応物が無い（R 的側面もある）。

（監査時は U-64。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-28. 大域的に固定した Skolem 関数の代わりに、段階 γ ごとの列挙 enumL γ に依存する証人段階 witStage を使っている。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 証明第2段：「(L_Θ,∈) の全一階式に対する Skolem 関数を外部で固定する。可算 γ < Θ に対し、L_γ の有限パラメータへ全 Skolem 関数を適用した値全体は可算である。従って、それらを含む L_{h(γ)} をある h(γ) < Θ に対して取れる」。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Skolem.lean:48 Idx / 96 enumL / 62 witStage / 109 nextStage / 135 witness_in_nextStage

#### 判定

論文は Skolem 関数 f_φ を Θ について一度だけ固定し、h(γ) を「f_φ が L_γ の有限
パラメータに取る値を含む段階」として定める。
Lean は各 γ について L γ の全射列挙 enumL γ（補題 16.1 による可算性を使う）を取り、
Idx = Fm × N × List N（式・変数・パラメータの添字列）ごとに証人を選び、その段階の
上限として nextStage γ を定める。証人の選び方は γ ごとに変わり得るので、
大域的な Skolem 関数の族にはなっていない。Tarski–Vaught test には十分だが、
論文どおりの「関数の固定」ではなく形式化上の迂回である。

（監査時は U-65。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-29. 文レベルの elementarity で一行で済む論文の議論が、Lean では Collection 文の符号化と Δ0 絶対性を経由する構成に置き換わり、さらに ω<Λ と Λ 極限を追加仮定として要求する。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 証明の最後：「完全 elementarity から L_Λ ⊨ KP でもある。」（L_Λ ≺ L_Θ と L_Θ ⊨ KP から一行で結論）

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/AdmTrans.lean:17 sat_collFm、/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/AdmTrans.lean:72 isAdmissible_of_elemFull

#### 判定

論文では KP が文の集合なので L_Λ ≺ L_Θ から直ちに L_Λ ⊨ KP となる。
Lean の Delta0Collection は意味論的述語 P : Pred で書かれているため、そのままでは
elementarity を適用できない。isAdmissible_of_elemFull は
(1) P の Δ0 絶対性で仮説を L Λ から L θ へ移し、
(2) L θ で Collection を使い、
(3) その結論を式 ∃b ∀x∈a ∃y∈b φ（sat_collFm）に符号化して L Λ へ反映し、
(4) 再び絶対性で述語に戻す、という 4 段階を踏む。
また IsAdmissible が ω<Λ と Order.IsSuccLimit Λ を含むため、Lean 側はこの 2 つを
仮定として要求する（論文では L_Λ ⊨ KP から補題 13.1 で従う側の情報）。

（監査時は U-66。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-30. Lean は §15.1・§16.2・§17.1 を LabelSystem 構造体に束ね、§1.2/§22.1 の無仮定版も同じファイルに置いている

**節**: 18 §17 有限行反映

#### 差異

##### 原文

§17 には対応物なし（LabelSystem という抽象インターフェースは論文に無い。定理 1.2・命題 22.1 は §22 の主張）

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Reflect.lean:450-458 (bm4LabelSystem)、461-463 (terminates_unconditional)、466-467 (R_wf_unconditional)

#### 判定

論文 §17 は定理 17.1 だけで閉じるが、Lean の Reflect.lean は末尾で bm4LabelSystem を定義し、
rel_mono（補題 15.1(1)）、rel_trans（補題 15.1(2)）、init（補題 16.2）、reflect（定理 17.1）を一つの構造体にまとめる。
この LabelSystem という抽象化は論文に対応物が無く、第 IV・V 部を集合論から切り離すための形式化上の都合。
さらに terminates_unconditional（定理 1.2）と R_wf_unconditional（命題 22.1）をこのファイルで宣言しており、
論文の章立て（§22）とファイル配置が一致しない。数学的内容の差ではない。
同様に setBlk / sigmaDef_blk / sigmaDef_bigAnd / SigmaDef.imp_const も、論文が自明として書かない
「有限連言をまとめる」「∃ ブロックを付ける」「外部で真な条件だけを連言に入れる」を補うための補助定義である。

（監査時は U-67。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-31. 定義 18.1 の 3 条件のうち条件 1（許容順序数）が Lean の `Stable` には現れず、抽象型 `LabelSystem.Lab` に吸収されている。

**節**: 20 §18, §19 安定ラベルと降下

#### 差異

##### 原文

定義 18.1 条件 1「各 f(i) は許容順序数である」、および「空配列には空関数を割り当てる」「A が非空なら ht(f)=f(ℓ−1)」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Label.lean:42-47 `BM4.Stable` / `BM4.ht`、:16-37 `BM4.LabelSystem`

#### 判定

論文の安定ラベルは f : {0,…,ℓ−1} → Ord で 1.許容性 2.狭義増加 3.祖先 ⟹ ◁_k の 3 条件。
Lean の `Stable` は 2 と 3 の 2 条件のみで、1 に対応する述語が無い。許容性は `LabelSystem.Lab`
を「許容順序数の型」とみなす抽象化に吸収されている（Part III のインターフェイス化）。
また Lean の f は ℕ → Lab の全域関数で、論文の定義域 {0,…,ℓ−1} の外の値は無制約
（`Stable` は i<j<A.len に限定して量化しているので実害は無い）。
「空配列には空関数」に対応する規定は無く、`ht A f = f (A.len − 1)` は len=0 でも f 0 として
定義される。§18/§19 では 0<len でしか使わないので結論には影響しない。
原文どおりに書けないための迂回なので U と分類した。

（監査時は U-70。著者判定 **W（形式化範疇の飛躍）** により移した。上の「…ので U」は監査時の判定文である。）

### W-32. 規約 5.2 に対応する Lean 側の構成が無い。Lean は常に生の ℕ 列番号で書くため、列出現の名前付けと文脈依存の読み替えが不要になっている。

**節**: 01 §1, §2, §5 の定義

#### 差異

##### 原文

規約 5.2: 列出現 (H,i) と列番号 ind_H(H,i)=i を区別し、X ≺^H_k Y :⟺ ind_H(X) ≺^H_k ind_H(Y)、X(k)=H_{ind_H(X)}(k) と略記する。さらに Ã=A[N] では D_j^{(q)}, P^{(q)} の列番号は p+qs+j, p+qs であり、上付き A を持つ関係式中の D_j^{(q)} は元配列の位置 p+j を表す、と読み替えを定める。

##### lean

Defs.lean:52-58 tildeCol、76-82 expand（対応する構成なし）

#### 判定

論文: D_j^{(q)} という「列出現の名前」を Ã の関係式では p+qs+j、上付き A の関係式では p+j と読み替える、という文脈依存の上書き規約を明示的に導入している。
Lean: tildeCol は位置 i から q=(i-p)/s、j=(i-p)%s を復元し、上昇条件を ancEq A k p (p+j)（元配列 A の位置 p+j）、新配列側の位置を i そのものとして書く。名前と読み替えの機構は存在しない。
すなわち規約 5.2 は Lean では形式化されず、初めから列番号だけで記述することで不要化されている。純粋に記法上の規約であり数学的内容の差ではないので U とした。

（監査時は U-2。著者判定 **W** により移した。上の「…ので U」は監査時の判定文である。）

## S 軽微迂回（1 件）

迂回だが原文の主張との差が小さく、下流の内容を変えないもの。

### S-1. Lean の SatCode は論文に無い ⊥ 節を持ち、¬・∧ 節の代わりに → 節を、∃ 節の代わりに全称形の ∀ 節を持つ。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

定義 8.3 の条件 4（¬ と ∧ の Tarski 節）および条件 5（∃ の節：⟨⌜∃v_iφ⌝,a⟩∈T ⟺ ∃x∈A ∃b∈U (Update(a,i,x,b) ∧ ⟨⌜φ⌝,b⟩∈T)）。

##### lean

SatCode.lean:330-352 `structure SatCode` の `falsum` / `imp` / `all` フィールド

#### 判定

falsum : ∀ a ∈ U, IsSeqA w A a → ⟨⌜⊥⌝,a⟩ ∉ T は論文に対応物が無い（論文の言語に ⊥ が無いため）。
imp 節は論文の条件 4 の二節をまとめたもので、¬φ = φ→⊥ を経由して論文の ¬ 節が導かれる。
all 節は ⟨⌜∀v_i e⌝,a⟩∈T ⟺ ∀x∈A ∀b∈U (IsUpdSeq a i x b → ⟨e,b⟩∈T) で、論文の ∃ 節の双対。
補題 8.2 が必要になる向きも双対に入れ替わる（∃ 側は b の存在、∀ 側は全ての更新代入が U 内にあること）。
さらに構造体は h を追加パラメータとして持つ（(8.2) の所見と同根）。言語の選び方に由来する迂回なので U。

（監査時は U-22。著者判定 **S（軽微迂回）** により移した。上の「…ので U」は監査時の判定文である。）

## R 原文の証明が無くても通る（36 件）

### R-1. 定理 1.2 の「空配列に到達した後は空配列のまま」という但し書きは定義 5.1 の ℓ=0 節から自動的に従うので冗長。

**節**: 01 §1, §2, §5 の定義

#### 差異

##### 原文

定理 1.2: 「A_0=A, A_{t+1}=A_t[n_t] と定め、空配列に到達した後は空配列のままとする。」

##### lean

Defs.lean:95-97 seq（および 76-82 expand の A.len = 0 分岐）

#### 判定

論文: 定義 5.1 は既に「ℓ=0 なら A[N]=A」と定めている。したがって A_t=∅ なら A_{t+1}=∅[n_t]=∅ が定義から出るため、定理 1.2 で改めて規約を置く必要がない。
Lean: seq A n 0 = A、seq A n (t+1) = expand (seq A n t) (n t) だけで定義し、対応する但し書きは持たない。空配列が保存されることは expand の `if A.len = 0 then A` から従い、Main.lean の seq_len_zero_of_le が補題として導出している。
原文の記述が無くても議論が通る（冗長）ため R とした。誤りではない。

### R-2. Lean の BadRoot は m が「最大」親行であることも m<r も要求せず、単に p が c の m-親であるとしか仮定しない。

**節**: 03 §5, §6 の設定

#### 差異

##### 原文

定義 5.1（p.4）「c が親を持つとき、その最大親行を m₀、c の m₀-親を p、その列を P=A_p とする」。行は k<r（定義 2.1）。

##### lean

lean/Bm4/Copy.lean:21-26 structure BadRoot（p, m, hpar : parent A m p (A.len-1)）、行 22 のコメント "No maximality of m is needed for the structural copy lemma."

#### 判定

論文: m₀ = max{k<r : c は k-親を持つ}、p = c の m₀-親。
Lean: hpar : parent A m p (A.len-1) のみ。m の最大性も m<r も無い。
したがって Copy.lean 以下で証明される内容は論文の主張より真に一般（任意の親行 m に対する版）。
実際に論文でも §6 の局所証明で m₀ の「行としての最大性」は一度も使われず、使われるのは「P が row m₀ の有効候補のうち最後」という p の最大性（補題 6.9 の (6.17)、補題 6.10 の (6.20)）だけで、これは Lean では parent_max（parentRel の第 4 成分）として hpar に含まれている。m₀ の最大性が現れるのは備考 7.2 と §19 のみ。
最大性を課す設定の記述が §6 の範囲では不要、という意味で R に分類したが、「Lean のほうが一般」という一般性の差でもあり分類は迷う。
なお実際の展開との接続（m := m₀）は Defs.lean の m₀/badRoot と Expand.lean の toBadRoot で別途行われている。

### R-3. Lean の bad root データ `BadRoot` は「m が c の親行である」ことしか要求せず、定義 5.1 が課す m の最大性を落としている。

**節**: 04 §5 展開との接続

#### 差異

##### 原文

定義 5.1: 「c が親を持つとき、その最大親行を m₀、c の m₀-親を p …」。§6 のコピー補題 (C1)–(C6) はこの m₀（最大親行）を固定して述べられている。

##### lean

Bm4/Copy.lean:23-26 `structure BadRoot`（p, m, hpar : parent A m p (A.len-1)）、Bm4/Expand.lean:37-38 `toBadRoot`

#### 判定

`BadRoot` のフィールドは hpar : parent A m p (A.len - 1) だけで、∀ k < r, HasParent A k c → k ≤ m にあたる条件が無い。Copy.lean:22-23 のコメントも「No maximality of `m` is needed for the structural copy lemma」と明言している。展開そのものは Defs.lean:81 で m₀ A を渡し、Expand.lean:41 `toBadRoot_m` が (toBadRoot h).m = m₀ A を与えるので定義 5.1 に忠実であり、最大性は `m₀_hasParent`/`le_m₀`(Expand.lean:19,27) として別に保持されている。つまり論文が §6 全体で仮定している m₀ の最大性は、コピー補題の証明には不要であることを Lean 側が示している（最大性が実際に使われるのは §7 の原規則との一致と、それ以降のラベル降下）。定義の仮定であって証明ステップではないため R と W の中間だが、余分な仮定という点で R に寄せた。

### R-4. 論文は元成分 D_j(k) の正負で場合分けするが、Lean は上昇条件 Asc で場合分けし、上昇列では帰納法の仮定も成分の正値性も使わずに親を直接構成している。

**節**: 05 §7 標準性

#### 差異

##### 原文

命題 7.1 の証明、c が親を持つ場合の帰納段。「接頭部分 G⌢B₀ = A↾c に属する列については、やはり補題 3.1 と帰納法の仮定から主張が成立する」／「まず D_j^(q)(k) > 0 と仮定する。元の成分 D_j(k) が正なら、帰納法の仮定により…(C2)/(C1)」／「次に D_j(k) = 0 とする…もし j > 0 なら第 2 の関係は狭義であり、補題 2.2(1) から 0 ≤ P(k) < D_j(k) = 0 となって矛盾する」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Standard.lean:47 standard_invariant（帰納段は 60-115 行、場合分けは 86-101 行）

#### 判定

論文の分岐は (i) D_j(k)>0 → 帰納法の仮定で A の k-親 R を取り R∈G なら (C2)、R=D_i∈B₀ なら (C1)、(ii) D_j(k)=0 → 増加条件 (7.4) が必要、j>0 なら補題 2.2(1) で矛盾、よって j=0 で (C3) を i=0 に適用、の 2 分岐。
Lean は 86 行で by_cases hasc : b.Asc k j（= k<m ∧ p ≼_k p+j）により 4 分岐する。
・Asc ∧ j>0（98-101 行）: ancEq から anc A k p (p+j) を取り exists_parent_of_valid で A の k-親を直接構成。仮定 hpos も帰納法の仮定 ih も使わない。
・Asc ∧ j=0 ∧ q>0（95-96 行）: exists_parent_c で「C の k-親が B₀ にある」ことだけを使う。hpos も ih も使わない。
・Asc ∧ j=0 ∧ q=0（90-93 行）と ¬Asc（102-107 行）の 2 分岐だけが ih を使う。
また論文は q=0 の列（B₀、位置 p..c-1）を補題 3.1 で接頭部分として一括処理するが、Lean が接頭不変性（col_lt + parent_congr_iff）を使うのは i<p のみで、B₀ は q≥1 と同じコピー機構（ParForm/anc_tA_iff）で扱う。
すなわち上昇列については、論文の (i) の帰納法の仮定の使用と (ii) の j>0 の矛盾論法は無くても証明が通る。

### R-5. Lean は m が最大親行であることを仮定せず、任意の親行 m に対して §6 全体を証明している。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

§5 定義 5.1 と §6 冒頭。「c が親を持つとき、その最大親行を m0、c の m0-親を p、その列を P = A_p とする」。§6 全体はこの最大親行 m0 に対して述べられる。

##### lean

Bm4/Copy.lean:23-26 `structure BadRoot (A : Arr r) where p, m, hpar : parent A m p (A.len - 1)`（コメント「No maximality of `m` is needed for the structural copy lemma.」）。担当範囲の全ファイルはこの `b : BadRoot A` に対する主張（Interval.lean:124-150 Claim1–Claim6 ほか）。

#### 判定

論文は m0 = max{k : c が k-親を持つ} と定める。Lean が要求するのは parent A m p (c) のみで、最大性は BadRoot に無い。
§6 の局所補題を追うと最大性が使われる箇所は無い。補題 6.9・6.10 の「P の最大性」は行 m0 ではなく列位置 p の最大性（最後の有効候補）であり、Δ_k>0 も 補題 2.2(2) から k ≤ m0 で従うだけである。
実際の適用（Bm4/Expand.lean:39 `toBadRoot h := ⟨badRoot A, m₀ A, _⟩`）では m = m₀ A を代入するので、下流の結論の強さは変わらない。
すなわち論文の仮定のうち「m0 が最大」は §6 では余分である。仮定の冗長という意味で R とした（「Lean のほうが一般」という観点でも読める）。

### R-6. 補題 6.2(3) と内部親 IntPar は形式化されているが、局所補題 6.4–6.10 の Lean 証明はそれを使わず別の筋（最終ステップ分解＋強帰納法）で通している。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

補題 6.2(3)「Y, X ∈ I について Y ≺_k X であることは、X から内部親を反復する鎖が I を出る前に Y に達することと同値である」。使用箇所：補題 6.4 末尾「補題 6.2(3) により内部親鎖を有限回反復して (C1)_k を得る」、補題 6.5 末尾「補題 6.2(3) を区間 I_q と I_0 に適用して」、補題 6.6 第 3 段階「補題 6.2(3) から直接親も B_q 内にある」。

##### lean

Bm4/CopyPaper/Interval.lean:62 `IntPar`、:66 `intPar_unique`、:74 `intPar_of_parent`、:84 `parent_eq_intPar`、:90 `parent_of_intPar`、:101 `anc_iff_intPar_transGen`（6.2(3) の形式化）— いずれも Interval.lean 外から一度も参照されない。実際の祖先鎖の扱いは L1.lean:144 `anc_copy_iff`、L3.lean:120 `anc_bridge`、L5.lean:151 `lemma_6_6` が `anc_last_step` による直接の帰納で行う。

#### 判定

Lean が実際に使う 6.2 は (1) の存在部分 `exists_parent_of_intECand`（L1.lean:112、L5.lean:189、L2.lean:53 経由）と (2) `parent_lt_of_no_intECand`（L4.lean:65）だけである。
(1) の同定部分（直接親が IntPar に等しい）と (3) の同値は証明済みだが未使用で、(C1)_k・(C3)_k・(C5)_k は内部親鎖の言語を経ずに証明されている。
つまり論文が 6.2(3) に委ねている「有限親鎖の反復として正当化する」部分は、形式化上は不要である。
分類は「原文の議論の一部が無くても通る」という意味で R とした（「別の筋で証明している」観点にも当たる）。

### R-7. 式 (6.11) は Lean の証明に現れず、実際 論文の後続の議論でも使われていない。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

補題 6.6 の証明中、(6.11) 「Q は直接親なので Q(k) < D_j^(q)(k) = D_j(k) ≤ P(k) ≤ P^(q)(k)」。

##### lean

Bm4/CopyPaper/L5.lean:151-227 `lemma_6_6`。対応する不等式鎖は作られていない（`hstep3`＝(6.10)、`htgt`＝(6.9)、`hancQ`＝(6.12)、最後に `anc_val_lt hfin` で矛盾）。

#### 判定

(6.12) の証明は補題 4.1（凸性）と位置関係だけを使い、(6.11) を参照しない。第 4 段階の矛盾も「P^(0)(k) = P(k)」と (6.9), (6.10) と 補題 2.2(1) から出る。
Lean 側でも Q(k) と P^(q)(k) を結ぶ不等式は一切構成せず、`convex` で Q ≺_k P^(q) を得たあと直接 P ≺_k D_j^(q) を作って `anc_val_lt` で矛盾させている。
したがって (6.11) は原文の冗長な補助不等式である。R。

### R-8. 論文が順位関数 rk と辞書式順序で正当化する部分を、Lean は ℕ 上の強帰納法 2 本と場合分けで置き換えており、rk は形式化されていない。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

命題 6.11 の証明「より形式的には、低行 k<m0 の命題へ rk((C1)_k)=(0,k,0), rk((C3)_k)=(0,k,1), rk((C5)_k)=(0,k,2), rk((C2)_k)=(0,k,3), rk((C6)_k)=(0,k,4) を与え、(C4) に (1,0,0)、高行の (C1),(C5),(C2),(C6) にそれぞれ (2,k,0),(2,k,1),(2,k,2),(2,k,3) を与える。…整礎帰納として完全に正当化される」。

##### lean

Bm4/CopyPaper/Assemble.lean:40 `stage1`（k<m の強帰納法）、:60 `claim4`（m=0/m>0 の場合分け）、:70 `stage3`（全 k の強帰納法）、:88 `theorem_6_3`。

#### 判定

論文は依存表の全前提が辞書式順序で結論より小さいことを確かめて整礎帰納とする。Lean は第 1 段階（k<m0 の 5 命題）、第 2 段階（(C4)）、第 3 段階（全行の 4 命題）をそれぞれ独立の定理とし、各段階内は k に関する Nat.strong_induction_on で回す。
段階の順序が辞書式順序の第 1 成分に対応するので内容は同じだが、rk の定義や整礎性の検証は形式化に不要である。
局所補題の仮定は §6.9 の依存表 9 行と 1 対 1 に一致していることを確認した（lemma_6_4/6_5/6_6/6_7/6_8/6_9/6_10 の引数）。R。

### R-9. 7 つのうち Concat と Append の ∆0 性は Lean で証明されておらず、Cons も §8 のファイルには無い。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

§8.1：「Seq, Len, Val, Cons, Append, Concat, Update は全て ω を固定パラメータとする ∆0 関係である」。

##### lean

SatCode.lean:450 `IsConcatSeq`（∆0 証明なし）、Append には述語自体が無い、Cons は担当範囲外の LCode.lean:110 `IsConsSeq`

#### 判定

Lean が ∆0 を証明しているのは Seq（delta0_isSeqA, 736）、Len（HF.lean:362 delta0_isDom）、
Val（delta0_valAt, 663）、Update（delta0_isUpdSeq, 787）の 4 つ。
IsConcatSeq は ∃ m : ℕ … natZ (m+j) とメタレベルの自然数を使って書かれており、そのままでは ∆0 式にならない。
実際 IsConcatSeq は補題 8.2 の系（concat/cons/append_mem_of_puCl, 528-543）でしか使われず、∆0 性は下流で要求されていない。
Cons は §9 の DefInp で必要になるため LCode.lean で別途 IsConsSeq として ∆0 が証明されている。
論文の主張のうち Concat・Append の ∆0 性は使われずに済んでいるので R。

### R-10. Lean は T を一回の分出で、U を V_γ（フォン・ノイマン階層）で作っており、論文の深さ再帰も TC 閉包構成も使っていない。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

補題 8.4 の存在部分の証明：W_A^pr を ω × A^<ω から Separation で切り出し、構文深さに沿った累積集合 T_n を ω-再帰（Replacement 併用）で作って T = ⋃_{n<ω} T_n とし、U_0 = TC({A,ω,T}), U_{n+1} = TC(U_n ∪ Pair(U_n) ∪ Un(U_n)), U = ⋃_n U_n と構成する。

##### lean

SatCode.lean:1091 `TruthSet`、1228-1252 `satCode_exists`

#### 判定

T は ZFSet.sep で「∃φ a, z = ⟨φ.code,a⟩ ∧ … ∧ SatIn A (SeqVal a) φ」を一発で切り出している。
Mathlib の ZFSet.sep は任意の Lean 述語を受け付ける（ZFC の分出図式より強い）ため、
論文が慎重に組んだ深さ再帰 T_n と Replacement による ω-再帰は Lean では不要になっている。
U も TC 閉包の列ではなく U = V_γ, γ = max(rank A, rank T, rank ωZ, rank (L ω)) + ω を取り、
推移性・Pairing 閉包・Union 閉包を rank 計算で直接示している。
W_A^pr に相当する集合も Lean には無い。論文の構成は（KP 内で行う §10.5 では要るとしても）ここでは冗長なので R。

### R-11. (9.4) の ∆0 性に「H(ξ),S(ξ),D(ξ) ∈ U」は不要で、Lean は H, S, D 自身とその元で量化を抑えている。

**節**: 09 §9 L のコード

#### 差異

##### 原文

式 (9.4) の直前：「さらに H, S, D ∈ U と推移性から、それらの関数値 H(ξ), S(ξ), D(ξ) は U に属する。…関数、正確な定義域、値、範囲、後続・極限条件は全て η, U, H, S, D への有界量化で展開できる。」

##### lean

lean/Bm4/SetTheory/LCode.lean:597 ball_val_iff, 605 bex_val_iff, 634 satClause_iff_bounded, 645 defClause_iff_bounded, 659 succClause_iff_bounded, 671 limClause_iff_bounded, 334 isDefEnum_iff_bounded。

#### 判定

Lean の有界化はすべて `∀ p ∈ H, ∀ q ∈ p, ∀ Hx ∈ q, p = ⟨ξ,Hx⟩ → …` の形で、段の値 H(ξ), S(ξ), D(ξ) を H, S, D の元の元として捕まえる。U が量化の枠として使われるのは条件 4 の a（定義の一部）と (9.1) の連結代入 ax だけである。
つまり論文が (9.4) の論拠に挙げる「H,S,D ∈ U と推移性から関数値も U に属する」という一段は、∆0 性を得るためには不要。
（U を枠に使う書き方でも ∆0 にはなるので、論文の議論が誤りというわけではなく、冗長という意味で R とした。）

### R-12. 補題 10.1 の証明の最後の ∆0-Separation は不要で、d 自体がそのまま Collection 集合になる

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.1 の証明：∆0-Collection で c を得て d = ⋃⋃c と置き、b = {y ∈ d : ∃x∈a ∃w∈c ∃z∈d (w = ⟨y,z⟩ ∧ δ(x,y,z,p̄))} を ∆0-Separation で切り出す

##### lean

Recur.lean:155 `sigma1_collection`（証明末尾で `b := ZFSet.sUnion b₀` を返すだけ）

#### 判定

論文は d = ⋃⋃c を作った後、さらに ∆0-Separation で b ⊆ d を切り出している。しかし結論は ∀x∈a ∃y∈b ψ(x,y,p̄) だけなので、b := d でそのまま成立する（c に集められた証人対の第一成分 y は d に属し、ψ(x,y,p̄) が成り立つ）。Lean の `sigma1_collection` は ∆0-Collection で得た b₀ に対し b = ⋃b₀ を返すだけで、Separation を一切使っていない。したがって論文の Separation 段落は証明に不要な冗長部分。

### R-13. Lean は選言の場合を双対化 2 回で導いており、論文が与える ∨ と ∀ の直接論証は無くても証明が通る。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

補題 12.2 の証明「? ∈ {∧,∨} について (12.1) を取る」「選言と全称量化の組合せで逆向きを示すとき、片方が全称的に真でないなら反例値を一つ固定し、他方が全ての値で真であることを得る」

##### lean

Bm4/SetTheory/Fm.lean:914 IsSigma.or_exists、:926 IsPi.or_exists（docstring「Obtained from the conjunction case by dualizing twice」）

#### 判定

論文は (12.1) を ? ∈ {∧,∨} 両方について同時に主張し、
∨ と ∀ を組み合わせた場合の逆向きを反例値の固定で議論している。
Lean が (12.1) に沿って直接証明するのは ∧ の場合だけ
(IsSigma.and_exists:851, IsPi.and_exists:879, sat_exs_and_exs:601, sat_alls_and_alls:643)。
∨ は exists_neg → and_exists → exists_neg の 3 段で機械的に得ている。
論文の ∨ 側の議論は冗長。

### R-14. Lean が取る新変数は「自由変数に現れない」だけで、論文の「式に現れない」より弱いが、それで十分。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

補題 12.2 の証明「padding では式に現れない変数を量化する vacuous block を最内側へ必要な個数だけ付ける」

##### lean

Bm4/SetTheory/Fm.lean:457 def fresh (s : Finset ℕ)、:473 IsSigma.pad_succ、:483 IsPi.pad_succ

#### 判定

Lean は fresh (fv φ) = max(fv φ)+1 を取る。これは φ の束縛変数や
外側ブロックの変数と一致しうる（例: fv δ = {6} で外側ブロックが [7] のとき
exs [7] (all 7 δ) が生じる）。
それでも Sat は fv にしか依存しない (sat_ex_of_notMem) ので同値性は保たれ、
定義 12.1 も（ブロック間の変数の disjoint 性は要求していないので）破られない。
論文の「式に現れない変数」という要求は過剰。

### R-15. 論文の定義的拡大の枠組みは Lean に対応物が無く、最初から Δ₀ グラフの関係形で扱うことで丸ごと不要になっている。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

定義 12.1 後半の段落全体（原始再帰関数を Δ₀ 定義可能関数記号として加えた定義的拡大、一意な Δ₀ グラフによる消去、「以後の Σ̂q, Π̂q と ≺*q はこの拡大におけるものとする」、「純粋言語での完全 elementarity はこの拡大での有限段 elementarity を含意する」、最後に全関数記号を消去して {∈} の ZFC 証明へ戻す）

##### lean

Bm4/SetTheory/BFCode.lean:757 def IsVarsBody（Δ₀ 関係）、:759 delta0_isVarsBody。定義的拡大の形式化は Bm4/ に存在しない

#### 判定

論文は Vars(e), Body(e) を項のように書くために言語を拡大し、
その拡大が消去可能であること、拡大での有限段 elementarity と
純粋言語での完全 elementarity の関係まで議論している。
Lean は Vars/Body を関数記号にせず、Δ₀ 関係 IsVarsBody e ν d のまま使い、
下流でも符号の対の形 e = ⟨natZ 1, ⟨ν, d⟩⟩ を直接書く。
したがって拡大の導入・消去の議論も、拡大と純粋言語の elementarity の
対応の議論も一度も必要にならない。論文のこの段落は形式化に現れない。

### R-16. ρ に課した ω + 1 < ρ と ρ + 1 < θ は不要で、Lean は 0 < ξ < θ かつ a ∈ L ξ だけで同じ議論を通している。

**節**: 14 §13 真理述語

#### 差異

##### 原文

補題 13.3 の証明「a とその値域を含み、かつ ω + 1 < ρ < θ、ρ + 1 < θ となる ρ を取る」

##### lean

lean/Bm4/SetTheory/BaseOK.lean:22 (isSeqA_stage)、同 42 (baseCorrect_L)

#### 判定

isSeqA_stage は a ∈ L θ から ξ = max ζ 1 を取り、0 < ξ、ξ < θ、IsSeqA ωZ (L ξ) a、a ∈ L ξ だけを出す。0 < ξ は ∅ ∈ L ξ を得るためで、ω + 1 < ρ は使っていない（充足コードの側で ω を A に入れる必要が無い。h = L ω, w = ωZ は大域パラメータ）。また ρ + 1 < θ は θ が極限であること（補題 13.1）から ρ < θ に自動的に従うので、条件として書く必要が無い。結論は変わらないので、論文の議論のうちこの部分は冗長。

### R-17. t, b ∈ W を出すのに KP は要らず、Lean は「W が ∅ を含み insert で閉じる」だけを使い、L θ についてはそれを θ が極限であることのみから得ている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定理 13.6 の証明「W |= KP は Pairing と Union を持つので、有限個の W-元からなる標準値列 t と更新代入 b は W の元として存在する」

##### lean

lean/Bm4/SetTheory/TrCorrect.lean:21 (WClosed)、同 73 (wClosed_L)、同 345-422 (trSig_step/trPi_step)

#### 判定

trSig_step/trPi_step が使うのは GoodAsn の最後の節 WClosed W（∅ ∈ W かつ x, y ∈ W → insert x y ∈ W）だけで、そこから seqOfNats/seqOfVals の元性と exists_blkUpd_W が出る。wClosed_L は θ が Order.IsSuccLimit であることだけから WClosed (L θ) を証明しており、Δ0-Collection（=許容性）も KP の公理も使わない。したがって論文が定理 13.6 の証明でここに「W |= KP の Pairing と Union」を持ち出すのは不要である。

### R-18. 論文が h < k を仮定する単調性を、Lean は h ≤ k で証明している（狭義不等号は不要）。

**節**: 16 §15 St と Rel

#### 差異

##### 原文

補題 15.1(1)：「h < k かつ α ◁_k β なら α ◁_h β」。証明も h<k から h+2<k+2 を使う。

##### lean

lean/Bm4/SetTheory/Stable.lean:34 `relAdm_mono (hhk : h ≤ k)`

#### 判定

論文: h < k を仮定し、h+2 < k+2 から Σ̂_j, Π̂_j 式の範囲 1≤j≤h+2 が 1≤j≤k+2 の部分であることを使う。
Lean: `relAdm_mono` は hhk : h ≤ k のみを仮定し、`ElemHat.mono (h+2 ≤ k+2)` を適用する。
実際 h = k の場合も同値の反射性で成立するので、論文の狭義不等号は使われていない。
差は些細で、Lean のほうが一般的。分類は「原文の仮定の一部が無くても通る」という意味で R としたが、W とも読める。

### R-19. Lean の証明は述語の Δ0 性（hP）も i≠j（hij）もパラメータ条件（hv）も使わず、任意の述語で成立する。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 証明第1段の Δ0-Collection：「L_Θ ⊨ ∀x∈a ∃y δ(x,y,p), δ ∈ Δ0 とする」として Δ0 式に限定して議論している。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Omega1.lean:105-137 isAdmissible_omega1 の Δ0-Collection 部分

#### 判定

Lean の証明本体は `intro s P hP i j hij v hv a ha H` の後、hP, hij, hv を一度も
使わない。使うのは ha（a ∈ L ω1）と H（各 x の証人が L ω1 に存在）だけで、
そこから countable_of_mem_L と ℵ1 の正則性（lsub_lt_ord_of_isRegular）で結論する。
つまり L ω1 では Δ0 に限らない完全な Collection が同じ議論で出る。
論文が δ ∈ Δ0 と書くのは KP の公理型としては当然だが、この箇所の議論では
Δ0 性は使われていない（Lean のほうが一般的）。

### R-20. 論文が要求する証人・Skolem 関数の <_L-最小性は Lean では使われず、外部の選択公理で任意に取っている。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 証明：Δ0-Collection で「各 x ∈ a に対して <_L-最小証人 y_x を取り」、第2段で「(L_Θ,∈) の全一階式に対する <_L-最小 Skolem 関数を外部で固定する」。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Omega1.lean:118-124（choose y hyL hyP / choose o ho hyo）、/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Skolem.lean:62 witStage（Classical.choose）

#### 判定

論文は 2 箇所で「<_L-最小」を明示的に要求する（Collection の証人、Skolem 関数）。
Lean はどちらも Classical.choose / choose で任意の証人を取り、<_L（構成可能順序）に
一切言及しない。議論は外部宇宙 V（ZFC）で行われるので選択公理が使え、最小性は
結論に不要である。すなわち論文の <_L-最小性の要求はこの証明では冗長。
（もし内部定義可能性を要求するなら最小性が要るが、§16 の使い方は外部固定のみ。）

### R-21. Lean の鎖は極限順序数から始まらず（γ0 = ω+1）、各項が極限であることも要求しない。

**節**: 17 §16 ω₁ と初期対

#### 差異

##### 原文

補題 16.2 証明第2段：「可算極限 γ0 > ω から始め、γ_{s+1} > max{γ_s, h(γ_s)} となる可算極限を取り、Λ = sup_{s<w} γ_s とする」。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Skolem.lean:173 chain / 109 nextStage / 260 exists_elemFull_pair（γ0 = ω+1）

#### 判定

論文は γ0 を「可算極限 > ω」に取り、各 γ_{s+1} も可算極限に取ると指定する。
Lean は chain γ0 0 = γ0, chain γ0 (s+1) = nextStage (chain γ0 s) で
nextStage γ = max (γ+1) (sup witStage)。極限性はどの項にも課さず、
実際に使う初期値は Lam (Ordinal.omega0 + 1)、すなわち γ0 = ω+1（後続順序数）。
極限性が証明されるのは Λ 自身についてのみ（Lam_isSuccLimit, Skolem.lean:216）。
ω < Λ は γ0 = ω+1 ≤ chain 0 ≤ Λ から出るので、論文の極限性要求は結論に不要。
また論文は γ_{s+1} > h(γ_s) と真の大小を課すが、Lean は witStage ≤ nextStage の
非真な不等号で足りている。

### R-22. 論文 (17.1) の ω 部分が Lean には無いが、許容順序数であることから自動的に従うため冗長

**節**: 18 §17 有限行反映

#### 差異

##### 原文

(17.1) max(X ∪ {ω}) < y'_0、および証明の条件群 1 の「ω < u_0」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Reflect.lean:158 (reflect_pattern の結論第3連言 `∀ x ∈ X, x < y' 0`)、および 241-250 の hbody（ω に関する連言なし）

#### 判定

論文は (17.1) を max(X ∪ {ω}) < y'_0 と書き、証明でも条件群 1 に ω < u_0 を課す。
Lean は結論を ∀ x ∈ X, x < y' 0 のみとし、内部式 hbody にも ω < u_0 に当たる連言を入れない。
論文の補題 13.1 が「θ が許容順序数なら θ > ω」を与え、Lean も IsAdmissible θ := ω < θ ∧ … (Adm.lean:27-28) を採るので、
y'_0 が許容順序数である以上 ω < y'_0 は自動。∪{ω} は X = ∅ のとき max を定義するためだけの装置になっている。
従って論文の ω 連言・ω < u_0 条件は無くても定理は通り、実際 §19 の (19.5) の使い方も X < y'_0 の部分しか使っていない。

### R-23. 仮定 n < r は定理 17.1 の証明で一度も使われない

**節**: 18 §17 有限行反映

#### 差異

##### 原文

定理 17.1 の仮定「n < r」

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Reflect.lean:150 (`reflect_pattern` の引数 `hnr : n < r`)

#### 判定

論文は r, n ∈ N, n < r を仮定に置くが、証明中で n < r が働く箇所は無い。
(17.2), (17.3) は k < r、(17.4) と St_m の複雑度は m < n（m+2 ≤ n+1）しか使わず、両者は独立。
反映も α ◁_n β（L_α ≺*_{n+2} L_β）のみに依存する。
Lean 側も同じ仮定 hnr を持つが、150 行以降の証明本体に hnr は現れず、束縛されたまま未使用である。
（Lean が仮定を落としていないので論文との差ではないが、論文の仮定が冗長であることが形式化で確認できる。）

### R-24. 論文は Π̂_{n+1} を経由し n = 0 を別扱いするが、Lean は全連言を直接 Σ̂_{n+2} に揃える別経路で証明している

**節**: 18 §17 有限行反映

#### 差異

##### 原文

証明の複雑度計算：n = 0 と n > 0 の場合分け、Σ̂_1 ≡ ∀w∃z⃗(w = w ∧ δ) による Π̂_2 化、有限連言を Π̂_{n+1} へまとめる手順

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/SetTheory/Reflect.lean:22-40 (PiDef.toSigma)、115-145 (sigmaDef_blk)、241-322 (hbody と ∃ ブロック付加)

#### 判定

論文：Σ̂_1（Adm, Rel_k）を ∀w∃z⃗(w=w ∧ δ) で Π̂_2 にしてから Π̂_{n+1} へ padding、St_m は Π̂_{m+2} → Π̂_{n+1}、
全体を一つの Π̂_{n+1} にまとめ、外側に ∃u_0…∃u_{s-1} を付けて Σ̂_{n+2}。Π̂_{n+1} = Π̂_1 になる n = 0 はこの経路が使えず、
補題 12.2 を引いて別途 Σ̂_1 → Σ̂_2 と論じている。
Lean：Δ0 は .sigma (n+2)、Σ̂_1 は .pad で Σ̂_{n+2}、Π̂_{m+2} は PiDef.toSigma（ダミー ∃）で Σ̂_{m+3} → .pad で Σ̂_{n+2}。
SigmaDef.and で連言をとり、sigmaDef_blk（SigmaDef.ex、1 ≤ n+2 のみ必要）で ∃ ブロックを付ける。n の場合分けは無い。
数値条件は一致（m < n ⟺ m+3 ≤ n+2 ⟺ m+2 ≤ n+1）。論文の Π̂_{n+1} 正規化と n = 0 の場合分けは無くても証明が通る。

### R-25. 論文が導入する「∆0 関数記号による定義的拡大とその消去」は Lean では一度も使われず、最初から純粋言語 {∈} だけで通っている。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

§12 p.23「本稿では、原始再帰関数を ∆0-定義可能な関数記号として加えた定義的拡大を用いる。この拡大は純粋言語 {∈} へ一意な ∆0 グラフを用いて消去でき、通常の Lévy 複雑度を変えない。…全ての許容階層は同じ ω を持つため関数記号の解釈も一致し、純粋言語での完全 elementarity はこの拡大での有限段 elementarity を含意する」

##### lean

Defin.lean:39,44,49（Delta0Def/SigmaDef/PiDef は Fm 上に定義され、Fm は falsum/eq/mem/imp/all のみ）

#### 判定

Lean の Fm には関数記号が無く、Defin.lean の三つの複雑度クラスは純粋言語 {∈} の式だけを対象にする。
論文が Vars(e), Body(e) を項として書くために導入する定義的拡大は、Lean では追加の量化変数と ∆0 グラフとして直接書かれる（TrP.lean の TrSigP/TrPiP は ∃ν∃d(e = ⟨1,⟨ν,d⟩⟩ ∧ …) / ∀ν∀d(e = ⟨2,⟨ν,d⟩⟩ → …) の形で、射影を最外ブロックへ併合している）。
したがって「拡大は ∆0 グラフで消去できる」「Lévy 複雑度を変えない」「許容階層は同じ ω を持つので関数記号の解釈も一致する」「純粋言語の完全 elementarity が拡大での有限段 elementarity を含意する」という一連の議論は、証明を通すためには不要である。
分類 R（記述上の便宜であって誤りではないが、原文の議論としては冗長）。

### R-26. 選言の結合は補題 12.2 (2) の双対化と連言の場合から従うので、原文が選言専用に置いている議論は不要。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

補題 12.2 (4) と式 (12.1) の証明（p.24）「選言と全称量化の組合せで逆向きを示すとき、片方が全称的に真でないなら反例値を一つ固定し、他方が全ての値で真であることを得る」

##### lean

Defin.lean:342 SigmaDef.or / Defin.lean:346 PiDef.or（および Fm.lean:914 IsSigma.or_exists, 928 IsPi.or_exists）

#### 判定

Lean は SigmaDef.or を (hP.not.and hQ.not).not.congr で得る。すなわち ¬P, ¬Q を Π̂q にし、連言をとり、もう一度双対化して P ∨ Q を得る。PiDef.or も同型。
Fm.lean:912-914 のコメントも「Obtained from the conjunction case by dualizing twice」と明記する。
論文は (12.1) で ⋆ ∈ {∧,∨} を一括して扱い、そのうえで ∨ と ∀ の組合せに対して反例値を固定する別議論を置いているが、補題 12.2 (2) を認めればこの部分は不要。
分類 R。

### R-27. 12.2 (1) が挙げる「代入」に相当する構文操作は Lean のどこにも無く、以後の議論でも使われていない。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

補題 12.2 (1)（p.24）「変数改名、代入、束縛変数の衝突回避、相対化、交代ブロックの付加」のうちの「代入」

##### lean

対応物なし（Rel.lean:14 relTo, 86 avoidMap, 126 exists_avoid、Defin.lean:202/278/313/328 のブロック付加、Fm.lean:175 rename はあるが、式への代入は無い）

#### 判定

Rel.lean は相対化 (relTo) と束縛変数の衝突回避 (avoidMap/exists_avoid)、Defin.lean は交代ブロックの付加、Fm.lean は変数改名を持つが、項や値を式へ代入する操作は存在しない。
§13 以降の部分真理述語は BlkUpd（有限代入の更新、意味論側の操作）だけを使い、構文的代入は定義 13.4・補題 13.5・定義 14.2・定義 15.2/15.4 のどこにも現れない。
従って補題 12.2 (1) の「代入」は述べられているが使われない項目である。
分類 R。なお論文の「代入」が assignment（有限代入）の意であれば話は別だが、その語は同じ列の「変数改名」「束縛変数の衝突回避」と並ぶ構文操作として書かれているため substitution と読んだ。

### R-28. (19.5) のうち ω < y'_0 の部分は §19 で一度も使われず、Lean の `reflect` も X に関する部分しか持たない。

**節**: 20 §18, §19 安定ラベルと降下

#### 差異

##### 原文

(19.5) max(X ∪ {ω}) < y'_0

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Label.lean:32 `LabelSystem.reflect` の結論 `(∀ x ∈ X, x < y' 0)`

#### 判定

論文は定理 17.1 の (17.1) をそのまま (19.5) として引くが、§19 での使用箇所は
「B_q より前の最後のラベルは (19.5) により y'_0 より小さい」の一箇所だけで、
必要なのは max X < y'_0 のみ。ω < y'_0 は §19 では使われない。
Lean の `LabelSystem.reflect` は (17.1) を `∀ x ∈ X, x < y' 0` とだけ書き、ω に相当する要素は
`Lab` に存在しない。inv_succ は :203 の `hXy'` でこの弱い形だけを使う。
原文の (19.5) の半分が無くても命題 19.1 の証明が通るので R と分類した。
ただし「Lab に ω を書けない」という点では U とも言える（迷ったので併記する）。

### R-29. 命題 19.1 の帰納段階は m₀ の最大性を一切使っておらず、Lean は任意の親行 m に対して同じ構成と結論を証明している。

**節**: 20 §18, §19 安定ラベルと降下

#### 差異

##### 原文

§19 場合 2「最大親行 m₀ が存在する。C の m₀-親の位置を p …」以降、証明全体を m₀ で通す

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Copy.lean:21-26 `BadRoot`（最大性を要求しない）、/home/koteitan/proofs/dh-bms-term/lean/Bm4/Label.lean:99-330 `inv_zero`/`inv_succ`/`inv_exists`、:341-344 で `toBadRoot` により m₀ を代入

#### 判定

論文は場合 2 を「最大親行 m₀」で始め、以後すべて m₀ という記号で議論する。
Lean の `BadRoot` は「p が最後の列 c の m-親である」というデータだけを持ち、m の最大性を
課さない（Copy.lean のコメントにも「No maximality of m is needed」とある）。
`inv_zero`/`inv_succ`/`inv_exists` は任意の `BadRoot` で成立し、実際に使うのは m < r（:142 の hm）と
α ◁_m β（:155）と系 6.12 が返す k < m だけ。
最大性が効くのは `descent`(:341) で b := toBadRoot h と置き、`expand`（定義 5.1）が
この b の A_q と一致することを言う箇所のみ。
したがって帰納段階に関する限り「最大」は不要な仮定であり、Lean のほうが一般。R と分類した。

### R-30. 論文は r ≥ 1 を仮定するが、Lean は仮定なしで全ての r について示している。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

補題 20.1「r ≥ 1 なら E_r は安定ラベルを持つ」の仮定 r ≥ 1

##### lean

lean/Bm4/Main.lean:20 `theorem stable_E : ∃ f, Stable S (E r) f`

#### 判定

論文: r ≥ 1 が仮定。
Lean: `{r : ℕ}` は無制限で、r = 0 でも成立を証明。
r = 0 では配列成分が無いので祖先関係が生じず（Lean では Stable の祖先条項が `∀ k, k < r → …` で空虚）、単調性だけ満たせばよく、f(0)=Λ, f(1)=Θ がそのまま働く。
よって r ≥ 1 は不要な仮定であり、Lean のほうが一般。分類は R（原文の余分な仮定・議論）とした。

### R-31. 論文は零列が一列の k-親であることを帰納法で示すが、安定ラベルの定義は含意なので不要。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

補題 20.1 の証明「零列が一列の k-親であることを k < r に関して帰納的に示す」

##### lean

lean/Bm4/Main.lean:29-35（stable_E の祖先条項）

#### 判定

定義 18.1 の条件 3 は「i ≺_k j ならば f(i) ◁_k f(j)」という含意である。
E_r の位置は 0,1 のみなので、祖先対は (0,1) しかありえず、補題 16.2 の Λ ◁_k Θ（(16.2)）から結論は直ちに従う。
Lean は `anc_lt h` で i<j、j<2 から i=0, j=1 を出し、`hΛΘ k` を適用するだけで、親関係そのものは一切証明していない。
論文の k に関する帰納法（唯一の有効候補であることの議論）は補題 20.1 には不要。

### R-32. 論文は r = 0 を別扱いするが、Lean は r で場合分けせず一様に処理している。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

定理 21.1 の証明冒頭「r = 0 では親となる行がなく、非空配列の展開は最後の列を一つ削除するので、有限回で空になる」

##### lean

lean/Bm4/Main.lean:88 `terminates`（r による場合分けなし）

#### 判定

論文は r = 0 と r ≥ 1 に分ける。分岐が必要なのは補題 20.1 が r ≥ 1 を仮定しているからだけである。
上の指摘のとおり補題 20.1 は r = 0 でも成り立ち、命題 19.1 も r ≥ 1 を要求しない。
Lean の `terminates` は r を場合分けせず、`reachable_stable`（補題 20.1 + 命題 19.1）と `descent` だけで r = 0 も含めて示す（r=0 では `LastHasParent` が偽なので `descent` は dropLast 側に落ちる）。
したがって論文の r = 0 の別議論は冗長。

### R-33. 論文が (21.2) の集合 S を作って整列するのに対し、Lean は S を使わず選択関数で直接 ω-再帰している。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

定理 21.1 の証明の (21.2)：S = ⋃_{ℓ<ω}(β₀+1)^ℓ に候補が属すること、Choice で S を整列し各段階で最小候補を選ぶこと

##### lean

lean/Bm4/Main.lean:64-68 `chain`（Nat.rec + Classical.choose）

#### 判定

論文: 候補が全て S に入ることを示し、Choice で S を整列して「最小候補」を選ぶことで ω-再帰を正当化する。
Lean: `descent`（命題 19.1）が返す存在命題に `Classical.choose` を当て、`Nat.rec` で反復するだけ。(21.2) に対応する定義・補題は Lean のどこにも無い。
論文が S を導入する理由は ZFC で選択公理を集合に対して適用するためだが、Lean の大域的選択作用素ではその制限が要らない。
「原文の議論が Lean では不要」という意味で R としたが、ZFC 内で原文どおりに書くなら S に相当する制限が必要なので U とも読める（分類に迷う）。

### R-34. 定理 21.1 は既に「E_r から到達可能な任意の配列を始点とする」と述べているので、有限列の連結は不要。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

定理 1.2 の証明（§22）「A から始まる無限非空展開列があれば、その前に E_r から A までの有限列を連結し、定理 21.1 に反する」

##### lean

lean/Bm4/Main.lean:88 `terminates`（そのまま定理 1.2、Reflect.lean:461 で無条件化）

#### 判定

定理 21.1 の主張は「E_r から有限回の展開で得られる任意の配列を始点とする無限非空展開列は存在しない」である。
A ∈ BM4 はまさにその条件を満たすので、A から始まる列にそのまま定理 21.1 を適用すれば済み、E_r から A までの有限列を前置して連結する操作は要らない。
Lean 側では定理 21.1 と定理 1.2 は同一の命題 `terminates (A) (hA : Reachable r A) (n)` であり、連結に対応する補題は存在しない（`seq_succ_shift` は使われていない）。

### R-35. 論文は規約として置くが、定義 5.1 の ℓ=0 節から従う定理であり、規約は冗長。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

定理 1.2 の主張文「空配列に到達した後は空配列のままとする」

##### lean

lean/Bm4/Main.lean:106 `seq_len_zero_of_le`

#### 判定

定義 5.1 は「ℓ = 0 なら A[N] = A」と定めている。したがって A_T = ∅ なら以後の A_t も空であり、定理 1.2 の中で改めて規約として置く必要はない。
Lean は規約を置かず、`seq_len_zero_of_le` として `expand_of_len_zero` から証明している。
矛盾ではなく重複なので R。

### R-36. R⁺ の整礎性は一般定理（整礎関係の推移閉包は整礎）で一行で出るので、論文の個別議論は冗長。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

命題 22.1 の後半「R⁺ が整礎的でなければ…各辺を実現する正の有限長 R-経路のうち最小コードのものを選び、順に連結すると無限 R-降下列を得て前半に反する」

##### lean

lean/Bm4/Main.lean:142-143 `transGen_R_wf := (R_wf S r).transGen`

#### 判定

論文は R⁺ の整礎性のために、各 R⁺-辺を実現する有限 R-経路を最小コードで選んで連結する、という個別の議論を書いている。
Lean は Mathlib の `WellFounded.transGen`（整礎関係の推移閉包は整礎）を適用するだけで、経路の選択・連結に対応する構成は無い。
BM4 に固有の性質を使わない一般論なので、原文の議論は冗長。

修正作業そのものの記録（74 件全部の状態と、修正しない理由）は
[fidelity-audit-fix.md](fidelity-audit-fix.md) にある。

## 残り 17 件の内訳

解消済み 45 件、監査ミスとして取り下げた 1 件（U-58）、著者判定で W へ移した 10 件と
S へ移した 1 件を除いた残りを、手が付くかで分類する。🔶 は一部を解消したもの。
1 件ごとの原文・Lean・差異は [diff/](../diff/README.md) にある。1 件ごとの差異と対応は
[fidelity-audit-fix.md](fidelity-audit-fix.md) にある。

| 区分 | 件数 | 該当 | 状況 |
|---|---|---|---|
| 根本 B：符号化の媒体 | 4 | U-18, 25, 38, 🔶56 | 論文は Cantor 対 π による自然数符号、Lean は Kuratowski 対の遺伝的有限集合。直すには §8〜§17 を書き直し ℕ 上の ∆0 算術を一から作る必要がある |
| 根本 C：「KP ⊢」の不在 | 8 | U-26, 29, 🔶30, 🔶32, 33, 34, 35, 62 | §10 は全部「KP は次を証明する」という構文的主張。Lean は許容 θ に対する L θ 内の意味論的主張で代用している。直すには KP の証明体系（導出・健全性）の形式化が要る |
| 構文操作のグラフが ∆0 | 3 | U-19, 🔶41, 🔶68 | (8.3) の DerSeq/Trace スキーマと補題 12.2。論文は「符号上の構文操作のグラフが ∆0」を一般に主張するが、Lean は述語ごとの個別証明と、帰納型 `Fm`/`BF` 上のメタ定理に置き換えている。根本 B と同根 |
| その他 | 1 | U-50 | 関数記号を項として使う枠が無い（外延は一致） |
| **未着手** | **1** | U-49 | ∆0 行列の符号ビット層 |

## U 形式化の都合で迂回（63 件）

### U-1. Lean の Arr は列も行も無限（col : ℕ→ℕ→ℕ が全域）で、定義 2.1 の側条件 i ∈ Pos(A) と k<r が親・祖先関係に入っていない。

**節**: 01 §1, §2, §5 の定義

#### 差異

##### 原文

§2 冒頭と定義 2.1: 配列は A ∈ (N^r)^ℓ で、A_i(k) は i ∈ Pos(A)={0,…,ℓ-1} と k<r でのみ定義される。定義 2.1 も「i ∈ Pos(A)、k<r とする」と側条件を置く。

##### lean

Defs.lean:14-16 structure Arr、23-25 parentRel、29-36 anc/cand、39 parent

#### 判定

論文: A_i(k) は i<ℓ、k<r のみ。親・祖先は Pos(A) 上の関係。
Lean: Arr r は len : ℕ と全域の col : ℕ→ℕ→ℕ を持ち、r は構造体では一切使われない幽霊パラメータ。parentRel/anc/cand/parent は len も r も参照せず、ℕ×ℕ 上の関係になっている。
具体例: E 0 では col 0 k=0, col 1 k=1 なので Lean 上は parent (E 0) 0 0 1 が成立するが、論文では r=0 の配列に第 0 行は存在しない。
実害は無い（expand 側は LastHasParent の ∃k<r と m₀≤r-1 で行を制限し、Label.lean の Stable も k<r・j<len を課す）が、論文の定義そのままではない。
副作用として、関係が len を参照しないため補題 3.1（接頭部分の不変性）が Lean では定義上自明になる（Defs.lean:11-13 のコメントもそう述べている）。形式化の都合による迂回と判断し U とした。

### U-3. 論文の BM4 は全ての r にわたる和集合だが、Lean の Reachable / R は r を固定した Arr r 上でしか定義されていない。

**節**: 01 §1, §2, §5 の定義

#### 差異

##### 原文

定義 1.1: 「BM4 とは、いずれかの E_r から有限回の一段展開によって得られる配列全体である」。定理 1.2 も「A を BM4 に属する任意の配列」とし、整礎性も BM4 全体上の一段関係について主張する。

##### lean

Defs.lean:90-92 Reachable、100-101 R

#### 判定

論文: BM4 = ∪_r {E_r から有限回の展開で得られる配列}。§22 の一段関係 R も BM4 全体（行数を跨ぐ集合）上の関係として定義される。
Lean: Arr r が r ごとに別の型なので、Reachable r と R r は r を固定して定義される（R r A B := Reachable r B ∧ 0 < B.len ∧ ∃ n, A = expand B n）。行数を跨ぐ BM4 全体に対応する単一の型・関係は存在しない。
展開は行数を変えないので R-降下列は一つの r に留まり、r ごとの整礎性から全体の整礎性は従う。よって強さの差は実質的に無いが、論文の主張そのままの形にはなっていない。型の都合による迂回として U とした。
なお Lean の R は Reachable r A を課さない（A = expand B n と Reachable B から従う）点、および BM4 外の A にも関係を定義している点で論文より広いが、これは整礎性を弱めない。

### U-4. 論文が課す k < r と i,j ∈ Pos(A) の制限が Lean には無く、全ての k : ℕ・全ての位置 i : ℕ について主張されている。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

定義 2.1 と §2 冒頭：「r 行、長さ ℓ の配列」「Pos(A) := {0,…,ℓ−1}」「親・祖先関係は Pos(A) 上に定義する」「i ∈ Pos(A)、k < r とする」。補題 2.2/3.1/4.1 はこの範囲での主張。

##### lean

lean/Bm4/Basic.lean 全体（例: 22 anc_lt_and_val_lt, 109 anc_mono, 176 ancEq_single_finite_chain, 343 convex）。土台は lean/Bm4/Defs.lean:14-40 の Arr / anc / cand / parent。

#### 判定

論文: 行番号は k < r、列番号は i ∈ Pos(A) = {0,…,ℓ−1} に限る。
Lean: Arr は len : ℕ と col : ℕ → ℕ → ℕ を持ち、col は全域。anc/cand/parent は k と i,j に何の上限も課さない。
そのため補題 2.2(1)(2)(3)(4)、3.1、4.1 は k ≥ r や i ≥ len でも成立する形で述べられている。
k ≥ r の行や i ≥ len の位置は論文には存在しないので、Lean の主張は論文より真に一般。
（col が全域なのでそこに現れる値はゴミだが、主張自体は不整合を起こさない。）
形式化の都合による迂回（Pos(A) の部分型化を避けている）と読めるので U に分類した。

### U-5. Lean の anc_prefix は符号化により自明（col が同一なので rfl）で、論文の仮定 m ≤ ℓ も「二位置が接頭部分に属する」も落ちており、内容は別の（より強い）補題 anc_congr が担っている。

**節**: 02 §2–§4 の補題

#### 差異

##### 原文

補題 3.1：A = (A_0,…,A_{ℓ−1})、m ≤ ℓ。接頭部分 A↾m = (A_0,…,A_{m−1}) に属する二位置について、k-親および k-祖先関係は接頭部分で計算しても全配列 A で計算しても同じ。証明は k についての帰納法。

##### lean

lean/Bm4/Basic.lean:291-294 anc_prefix（本体は anc_congr_iff (fun _ _ _ => rfl)）。実質を担うのは 219-260 anc_congr と 262-289 anc_congr_iff / cand_congr_iff / parent_congr_iff / ancEq_congr_iff。

#### 判定

Lean の Arr では anc/cand/parent が len を一切見ないので、⟨m, A.col⟩ と A は同じ関係を定める。
よって anc_prefix は m や j,i に何の条件も無しに、fun _ _ _ => rfl で終わる。論文が k に関する帰納法で示す内容は消えている。
代わりに anc_congr は「位置 x ≤ i の全成分が一致する二配列 A,B は i の k-祖先が一致」という、同一配列の接頭部分ではなく別配列同士の主張で、k についての帰納法で証明されている。
これは論文の補題 3.1 より強い（論文 3.1 は A↾m と A、すなわち同一の col を持つ対しか扱わない）。
論文は §19 等で A[N] の接頭部分 G⌢B_0 = A↾c と A を「補題 3.1 により」同一視しており、そこで必要なのは実は anc_congr の側。
形式化の都合で原文の主張が空になり、迂回して強い補題を置いているので U に分類した。

### U-8. Lean の tA はコピーを無限個持ち長さを 0 と置いた配列で、論文の Ã=A[N]（コピー N+1 個、長さ p+(N+1)s）そのものではない。

**節**: 03 §5, §6 の設定

#### 差異

##### 原文

定義 5.1「q = 0,…,N に対しコピー B_q を …で定め、A[N] = G⌢B_0⌢…⌢B_N とする」、§6 冒頭「Ã = A[N] = G⌢B_0⌢…⌢B_N」。

##### lean

lean/Bm4/Copy.lean:44-46 BadRoot.tA（noncomputable def tA : Arr r := ⟨0, tildeCol A b.p b.m b.s⟩）

#### 判定

論文: Ã のコピーは q = 0,…,N の N+1 個、長さは p+(N+1)s。
Lean: BadRoot に N が現れず、tildeCol は i ≥ p の全ての i に対し q=(i-p)/s のコピーを与える（コピー無限個）。さらに tA.len := 0 で、Ã の長さは表現していない。
正当化は Defs.lean:11-13 の設計（parent/anc/cand は len を参照しない）と、位置 < pos q j の情報しか使わないこと。論文側では補題 3.1（接頭部分の不変性）に対応する。
A[N] との接続は Expand.lean:43-54（expand_eq / expand_len）で別に与えられており、そこで初めて長さ p+(N+1)s が入る。
矛盾ではなく形式化上の迂回なので U。

### U-9. 定義 6.1 の区間 I は Lean では左端 lo のみで表現され、Cand^I_k（有効でない内部候補全体）に対応する定義は置かれていない。

**節**: 03 §5, §6 の設定

#### 差異

##### 原文

定義 6.1（p.5）「I を連続した列区間、X ∈ I をその中の列の出現とする。I 内にある X の構造的 k-候補全体を Cand^I_k(X)、有効候補全体を ECand^I_k(X) と書く。ECand^I_k(X) ≠ ∅ なら、その最後の位置を X の I-内部 k-親と呼び IntPar^I_k(X) と書く。」

##### lean

lean/Bm4/CopyPaper/Interval.lean:25-27 IntECand、62-63 IntPar（Copy.lean には対応物なし）

#### 判定

論文: I は連続区間、Cand^I_k(X) と ECand^I_k(X) の二つを定義し、IntPar は ECand の「最後の位置」。
Lean: IntECand A lo k j i := lo ≤ j ∧ cand A k j i ∧ A.col j k < A.col i k、IntPar A lo k y i := IntECand … ∧ ∀ j, IntECand … → j ≤ y。
差は (a) I の右端が落ちて条件が lo ≤ · だけになっている（Interval.lean:21-22 に理由が書かれている: 候補は常に対象より左、対象は I 内なので同値）、(b) 有効性を外した Cand^I_k に相当する定義が無い（IntECand は有効候補だけ）、(c) 「ECand≠∅ のとき最後の位置」という部分定義を、「自分が有効内部候補かつ全ての有効内部候補以上」という全体定義に置き換えている（一意性は intPar_unique で別途証明）。
いずれも同値な言い換えで矛盾はないので U。担当 Lean ファイル（Copy.lean）自体には定義 6.1 の対応物は無い。

### U-10. Copy.lean は論文に対応物のない「親・祖先の閉形式」ParForm / AncForm を定義に格上げしている。

**節**: 03 §5, §6 の設定

#### 差異

##### 原文

§5–§6 の範囲には、Ã における親関係・祖先関係の閉じた形を述べた番号付き主張は無い（AncForm の第 2 選択肢に相当するのは §6.9 の系 6.12 のみで、しかも一方向）。

##### lean

lean/Bm4/Copy.lean:250-261 ParForm / AncForm

#### 判定

AncForm k y q j は 3 択（同一コピー内の anc A k (p+i) (p+j)／k<m ∧ p ≼_k (p+j) ∧ ∃a<q, anc A k (p+i) c／y<p かつ anc A k y (p+j)）で、内容は (C1)・(C2)・系 6.12 を合わせて同値形にしたもの。系 6.12 は論文では「⇒」のみだが Lean は iff（Char.lean の anc_tA_iff）として使う。
ParForm（直接親の閉形式）に対応する主張は論文 §5–§6 に無い。これは Standard.lean（命題 7.1）で「親が存在すること」を言うための道具として使われており、論文はそこを (C1),(C2),(C3) を直接使って議論している。
矛盾ではなく形式化の都合による補助定義なので U。担当外の節（§6.9, §7, §19）に関係するため、その節の担当と重複しうる。

### U-11. Lean の A[N] は列関数が長さを越えて無限個のコピー B_{N+1}, B_{N+2}, … を持ち続け、anc_expand_iff は q ≤ N の制限なしに述べられている。

**節**: 04 §5 展開との接続

#### 差異

##### 原文

定義 5.1 末尾: A[N] = G ⌢ B_0 ⌢ … ⌢ B_N。コピーはちょうど q = 0,…,N の N+1 個で、A[N] は有限配列である。

##### lean

Bm4/Defs.lean:52-59 `tildeCol`、Defs.lean:76-83 `expand`、Bm4/Copy.lean:46 `BadRoot.tA`、Bm4/Expand.lean:49 `expand_col`、Expand.lean:72-75 `anc_expand_iff`

#### 判定

`Arr r` の `col : ℕ → ℕ → ℕ` は全域で、`tildeCol A p m s` は i ≥ p の全ての i に対し q = (i-p)/s, j = (i-p)%s でコピー成分を返す。よって位置 p+(N+1)s 以降にも B_{N+1}, B_{N+2}, … が定義されたままである。`expand A N` の `len` は p+(N+1)s なので「見える」範囲は論文どおりだが、`anc` は `len` を参照しないため、`anc_expand_iff` (Expand.lean:72) は q に上限を課さず、論文の A[N] には存在しない列 pos q j (q > N) についても祖先関係を主張している。`BadRoot.tA` に至っては len = 0 の配列である。補題 3.1（接頭部分の不変性）を型レベルで自動化した代償の迂回で、len 未満の位置では論文の A[N] と一致するので矛盾はない。

### U-13. 論文の添字範囲（コピーは 0..N、行は k<r）が Lean には無く、無限コピー・全行で主張されている。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

§5「A[N] = G ⌢ B0 ⌢ ⋯ ⌢ BN」、定理 6.3「存在する全ての添字について次が成り立つ」、(C6)「0 ≤ a < b < N なら」。定義 2.1 は k < r に限る。

##### lean

Bm4/Copy.lean:46 `noncomputable def tA : Arr r := ⟨0, tildeCol A b.p b.m b.s⟩`（コピー無限個、len は無視）。Bm4/CopyPaper/Interval.lean:124-150 の Claim1–Claim6 は q, a, c', k をすべて ℕ 全体で量化。

#### 判定

論文の Ã = A[N] はコピー B0..BN の N+1 個で、(C6) の b+1 が存在するために b < N が要る。Lean の tA は tildeCol を全位置に延長した無限コピー配列で、Claim6 は a < c' のみを課し上限が無い。
行についても Arr r の col は全 k ∈ ℕ で定義され、Claim には k < r が無い。
有限版 A[N] との接続は担当範囲外の Bm4/Expand.lean:73 `anc_expand_iff` で別に行われている。
論文より強い主張になるが、これは長さを見ない親・祖先の定義（接頭部分不変性を自動にするための設計）に伴う形式化上の迂回なので U とした。

### U-14. 論文の一般の連続区間 I が、Lean では左端 lo だけ（I = [lo, ∞) に相当）で表されている。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

定義 6.1「I を連続した列区間、X ∈ I をその中の列の出現とする。I 内にある X の構造的 k-候補全体を Cand^I_k(X)、有効候補全体を ECand^I_k(X) と書く」。

##### lean

Bm4/CopyPaper/Interval.lean:25 `def IntECand (A : Arr r) (lo k j i : ℕ) : Prop := lo ≤ j ∧ cand A k j i ∧ A.col j k < A.col i k`、:62 `def IntPar (A) (lo k y i)`。

#### 判定

Lean は I を左端 lo のみで符号化し、「I 内にある」を `lo ≤ ·` と定義する（ファイル冒頭にその旨の注記あり）。構造的候補は必ず対象より左にあり、対象は I に属するので、使用場面では論文の定義と一致する。
しかし論文の Cand^I_k(X)（有効性を課さない内部候補）に対応する定義は Lean に無く、ECand に当たる IntECand だけがある。両端を持つ一般の区間に対する定義 6.1 そのものは形式化されていない。
意味の食い違いではなく符号化の簡略化なので U。

### U-15. Lean には §6 に対応物の無い「祖先関係の閉形式 AncForm による完全な特徴付け」が追加されている。

**節**: 06 §6 コピー補題

#### 差異

##### 原文

§6 は (C1)–(C6)（定理 6.3）と系 6.12 までしか述べない。展開後配列の祖先関係の閉形式による特徴付けに当たる主張は §6 に無い。

##### lean

Bm4/CopyPaper/Char.lean:72 `anc_tA_iff : anc b.tA k y (b.pos q j) ↔ b.AncForm k y q j`、:140 `anc_tA_cases`、:154 `hasParent_tA_of_parForm`。`AncForm`/`ParForm` の定義は Bm4/Copy.lean:251,257。

#### 判定

Char.lean は (C1)–(C6)（stage1/stage3/claim4）と passing・lead_bridge・anc_lead_of_anc_lead から、`y ≺_k D_j^(q)` の三分岐（同一コピー内／より前のコピー（k<m0 かつ D_i ≺^A_k C かつ P ≼^A_k D_j）／G 内）を必要十分として導く。
論文にはこの同値の主張は無く、下流（§7 命題 7.1、§17–§19）では系 6.12 と (C1),(C2) だけが使われている。
Lean 側では Bm4/Standard.lean:37,84、Bm4/Label.lean:255,271、Bm4/Expand.lean:73 がこの閉形式を使うため、形式化の都合で追加された中間主張である。U。

### U-16. 論文では r が配列のパラメータだが、Lean では先頭列の長さから r を推定し、G 部分は元の列をそのまま連結するため、列長が不揃いな入力では行数が保たれない。

**節**: 07 論文外・試験用

#### 差異

##### 原文

定義 5.1 直前「A = (A_0,…,A_{ℓ−1}) を r 行配列、N ∈ ℕ とする」および定義 5.1 末尾「この操作では行数 r を変更しない」。r は配列に与えられたパラメータ。

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Compute.lean:10 `abbrev Mat := List (List ℕ)`、:41 `let r := (A.headD []).length`、:52-54

#### 判定

論文は A を「r 行配列」と与え、r は外から固定される。Lean の Mat = List (List ℕ) には r が無く、expandC は r := (A.headD []).length と先頭列の長さで代用する。
さらに出力は A.take p（G は元の列をそのまま複製）++ B（各列を (List.range r).map で長さちょうど r に作り直す）なので、入力の列長が不揃いだと出力も不揃いになり、定義 5.1 の「行数 r を変更しない」が字義どおりには成立しない。また列が r より長い入力では超過行が B 側で切り捨てられ、r より短い列は colv の既定値 0 で埋められる。
全列が長さ r の正規な入力（論文が扱う唯一の対象）では両者は完全に一致するので、これは形式化の都合による迂回であり、数学的内容の相違ではない。Bm4/Defs.lean 側は Arr r として r をパラメータに持っており、そちらには同じ問題は無い。

### U-18. Form(e) の存在量化の有界化が ω ではなく追加パラメータ h（= L ω）になり、また「最後の項」ではなく「どこかの項」になっている。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

(8.2)：Form(e) :⇔ ∃s ∈ ω (s は e の整式導出列)。導出列は「最後の項が対象コード」であるもの。

##### lean

Code.lean:143-148 `IsCodeW h w e := ∃ s ∈ h, DerSeqW w s ∧ ∃ k, pair k e ∈ s`

#### 判定

論文は「s ∈ ω」と有界化し、§8 冒頭で構文述語のパラメータは w = ω ただ一つと宣言している。
Lean はコードが自然数でなく HF 集合なので、導出列 s を h（実際には L ω = HF）で有界化し、
SatCode/IsAsn/NotFreeW まで h が伝播する（SatCode.lean:330-352, 1035）。結果として ∆0 式のパラメータは ω と L ω の二つ。
また論文の「最後の項が e」に対し Lean は「ある位置 k で pair k e ∈ s」。外延的には一致（Code.lean:544 isCodeW_iff）。
いずれも符号化媒体を変えたことによる迂回なので U。

### U-19. (8.3) の DerSeq/Trace という一般スキーマは Lean に無く、述語ごとに専用の導出列述語を手書きしている。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

(8.3)：各構文操作 F のグラフを G_F(ē,d) :⇔ ∃s∈ω ∃τ∈ω (DerSeq(ē,s) ∧ Trace_F(ē,d,s,τ)) という一般スキーマで与え、自由変数・構文深さ・部分式・改名・代入・相対化・padding 等をまとめて ∆0 と結論する。

##### lean

CodeFV.lean:29-51 `NFAtomicW` / `NFDerSeqW` / `NotFreeW`（担当範囲では Free のみ）

#### 判定

Lean には Trace_F に当たる計算列 τ が無い。CodeFV.lean は「x が自由でない」コードを認識する専用の導出列
NFDerSeqW を直接定義し、∀x φ の場合だけ通常の IsCodeW を呼ぶという個別設計になっている（ファイル冒頭コメント参照）。
したがって (8.3) の「全ての構文述語が同一スキーマで ∆0」という一般命題そのものには Lean 側の対応物が無く、
必要な述語（Free、および担当外の Vars/Body 等）が個別に ∆0 と証明されているだけである。
形式化の都合による迂回なので U。

### U-20. Lean の Val は定義域外で既定値 ∅ を返す全域版で、論文の Val（定義域内に限る）と外延が異なる。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

§8.1：Val(a,i,x) は「i ∈ dom(a) かつ a(i) = x」を表す。

##### lean

SatCode.lean:21-22 `ValAt a i x := pair i x ∈ a ∨ ((∀ y, pair i y ∉ a) ∧ x = ∅)`

#### 判定

論文の Val(a,i,x) は i ∉ dom(a) のとき常に偽。Lean の ValAt は i ∉ dom(a) のとき x = ∅ で真になる。
定義 8.3 の原子節では Lean 側に InDomZ a i, InDomZ a j のガードが付く（SatCode.lean:337-345）ので、
使用箇所での外延は一致する。SeqVal（SatCode.lean:95）も同じ既定値 ∅ の規約。
∅ ∈ A が保証されない場合に既定値が A の外に出るため、seqVal_mem（157）は別途 ∅ ∈ A を仮定している。
論文には無い規約なので U。

### U-21. Lean の Asn_A は Form(e) の連言項を持たず、Free(e,i) を ¬NotFree(e,i) で置き換えている。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

(8.4)：Asn_A(e,a) :⇔ Form(e) ∧ Seq(a) ∧ ∀i∈ω(Free(e,i) → i∈dom(a)) ∧ ∀i∈dom(a)(a(i)∈A)。

##### lean

SatCode.lean:34-35 `IsAsn h w A e a := IsSeqA w A a ∧ ∀ i ∈ w, ¬ NotFreeW h w i e → InDomZ a i`

#### 判定

論文は Free を肯定形の ∆0 グラフとして使うので、整式でない e に対しては Free(e,i) が全 i で偽になり、
第 3 連言が空虚に成立する。だから Form(e) の連言項が必要である。
Lean は ¬NotFreeW を使うため、整式でない e では全 i で ¬NotFreeW が真になり dom(a) ⊇ ω を要求して矛盾し、
IsAsn 自体が e のコード性を含意する。よって Form(e) を落としても外延は一致する。
ただし定義の連言項の構成は論文と異なり、定義 8.3 の条件 2（SatCode.lean:341 proper）では IsCodeW を明示的に別途課している。
肯定形 Free を導出列で書くには各項にフラグが要るための迂回（CodeFV.lean 冒頭コメント）なので U。

### U-23. Lean の正しさ命題は論文に無い仮定 a ∈ U を追加で置いている。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

(8.6)：「任意のそのようなコードに対し、式コード e と適切な A-値有限代入 a について ⟨e,a⟩∈T ⟺ (A,∈)⊨φ_e[a]」。a ∈ U は主張に書かれていない（補題 8.2 から従う）。

##### lean

SatCode.lean:560-561 `satCode_correct`：… → (∀ k ∈ fv φ, InDomZ a (natZ k)) → a ∈ U → (⟨φ.code,a⟩∈T ↔ SatIn A (SeqVal a) φ)

#### 判定

論文は「適切な A-値有限代入 a」とだけ言い、a ∈ U は補題 8.2 で自動的に得られるとしている。
Lean でも同じ導出は可能で（seq_mem_of_puCl, 441）、実際 satCode_unique（637）や satCode_truthSet_of（1122）では
その補題で a ∈ U を作ってから satCode_correct に渡している。
したがって Lean の命題は論文より仮定が一つ強い（主張として弱い）だけで、内容の食い違いではない。
分類に迷ったが「論文どおりに述べていない」という形式化上の選択なので U とした。

### U-24. Lean は論文に対応物の無い代入の padding 述語を §8 のファイルで定義し、∆0 性まで示している。

**節**: 08 §8 符号と充足コード

#### 差異

##### 原文

§8 に代入の padding（空所を ⟨k,∅⟩ で埋める操作）に相当する概念は無い。§8 の一覧にある「padding」は式に対する操作。

##### lean

SatCode.lean:91-92 `IsPadOf a b := a ⊆ b ∧ ∀ p ∈ b, p ∈ a ∨ ∃ k, p = pair k ∅`、131 `seqVal_of_isPadOf`、720 `delta0_isPadOf`

#### 判定

IsPadOf は代入 a を ⟨k,∅⟩ の対で水増しした b を表し、値（SeqVal）を変えないことが示されている。
§8 内では使われず、Truth.lean:21,37 の ∆0 真理述語（§13 相当）や BaseOK.lean:58,121、Blk.lean:386 で使われる。
論文の §13 の定式化（Asn_A(e,a), a ∈ U, SatCode, ⟨e,a⟩∈T）には padding は現れない。
代入を必要な集合の中に取り直すための形式化上の装置と見て U とした。
なお §8 の「padding」は式の padding であり、これとは別物である。

### U-25. 論文は式コードを自然数 (e ∈ ω) とするが Lean は遺伝的有限集合 (e ∈ L ω) とし、LCode は ω に加え L ω も固定パラメータに持つ。

**節**: 09 §9 L のコード

#### 差異

##### 原文

定義 9.1 条件 4：D(ξ) の定義域は {⟨e,a⟩ : e ∈ ω, a ∈ U, DefInp_{H(ξ)}(e,a)}。系 10.6 証明：「LCode は ω を固定パラメータとする ∆0 公式であり、Lθ は推移的かつ外部と同じ ω を持つ」。

##### lean

lean/Bm4/SetTheory/LCode.lean:327 IsDefEnum（`∃ e ∈ h`）、833 LCode（h, w の 2 個のパラメータ）、845 delta0_lcode（Delta0Def {h,w,η,M,c}）。h は常に L ω で具体化される。

#### 判定

論文 §8 は式コードを π 対符号化により自然数として与え（(8.1)）、定義 9.1 条件 4 は e ∈ ω と書く。Lean の Fm.code（Code.lean:14）は Kuratowski 対からなる HF 集合で、IsCodeW h w e の h は L ω。よって条件 4 の域は `∃ e ∈ h, ∃ a ∈ U, ...` となる。
結果として Lean の LCode は LCode h w η M c の 5 引数で、固定パラメータが ω と L ω の 2 個ある。系 10.6 の絶対性の論拠は論文では ω 一つで足りるとされるが、Lean の符号化では L ω も内外で一致することが要る（実際 L ω は絶対なので成立する）。
数学的内容は変わらないが、定義 9.1 の字面（e ∈ ω）と Lean の字面（e ∈ h）は一致しない。形式化の都合による迂回と判断した。

### U-26. Lean には「KP が証明する」形の補題 10.5(2) が無く、L θ（θ 許容）内の版だけがあり、証明の筋も論文と異なる。

**節**: 09 §9 L のコード

#### 差異

##### 原文

補題 10.5(2)：「KP は次を証明する。任意の順序数 η に対し、ある M, c が LCode(η,M,c) を満たす。」証明は補題 10.3 の Σ1 再帰で H を作り、S・D を系 10.2 で集め、最後に補題 10.4(3) で {η,M,H,S,D,ω} を含む推移的 PUCl な U を取る。

##### lean

lean/Bm4/SetTheory/LCodeEx.lean:553 lcode_exists_in_L（仮定 IsAdmissible θ と ξ < θ、結論 ∃ M ∈ L θ, ∃ c ∈ L θ, LCode …）。18 LTr, 24 LFn, 67 AStep, 76 LStep, 332 graphBelow_LFn_mem_L。

#### 判定

Lean の主張は θ 許容・ξ < θ の下で M, c ∈ L θ を作るもので、論文の 10.5(2)（任意の順序数について KP 内で）ではなく実質的に系 10.6 に対応する。KP 可証性そのものは形式化されていない（意味論的に IsAdmissible = ω<θ ∧ 極限 ∧ L θ の ∆0-Collection で置き換えている）。
証明の筋も違う。論文は H のみを Σ1 再帰し S, D を系 10.2 で後から集めるが、Lean は対 ⟨L ζ, TruthSet(L ζ)⟩ を再帰し（LFn, LStep）、S = graphBelow LTr ξ と D = graphBelow (defEnum …) ξ を ∆0 分出（graphBelow_mem_L_of_sep, sep_mem_L_of_limit）で L θ に落とす。
また補題 10.4(3) の TC 反復による U の構成は使わず、極限 ρ < θ を取って U = L ρ とする（δ, δ₂, ρ の三段）。AStep は論文の H(0)=∅ と極限節を一つの選言にまとめている。

### U-27. 論文が定義された項として無束縛に書く a_x を、Lean は定義の中で `∃ ax ∈ U` と U で束縛した形に書き換えている。

**節**: 09 §9 L のコード

#### 差異

##### 原文

式 (9.1)：x ∈ b ⟺ ⟨e, a_x⟩ ∈ S(ξ)。a_x は §9.1 で「x と a から v0 へ x、残りへ a の値を割り当てた連結代入」として導入された項で、量化の枠は書かれていない（∆0 性は「補題 8.2 により ax も U に属する」で後から正当化される）。

##### lean

lean/Bm4/SetTheory/LCode.lean:327 IsDefEnum の値節 `∀ x ∈ Hx, (x ∈ b ↔ ∃ ax ∈ U, IsConsSeq x a ax ∧ ⟨e,ax⟩ ∈ Sx)`、1155 IsDefPow、1358 IsDefEntry も同形。

#### 判定

a_x は関数記号ではないので、literal に書くと `∃ ax (Cons(x,a,ax) ∧ ⟨e,ax⟩ ∈ S(ξ))` という非有界存在になり、そのままでは ∆0 でない。論文はこれを条件 1・2 と補題 8.2 から得られる意味論的事実（ax ∈ U）で正当化する。
Lean は定義の段階で枠 U を入れてしまうので、LCode の他の条件が成り立たない U に対しては Lean の IsDefEnum と論文の (9.1) が一致しない可能性がある。LCode 全体の下では eq_defBit（LCode.lean:1261）が両者の一致を与える。
論文どおりに書けないための迂回なので U とした。補題 10.5(2) の b_{e,a} = {x ∈ X : ⟨e,a_x⟩ ∈ T_X} を「∆0-Separation で一意に存在する」と書く箇所も同じ事情を含む。

### U-28. Lean の補題 9.2 は η が外部の順序数 η.toZFSet の形をしている場合に限られ、IsOrdinal な ZFSet 一般には述べられていない。

**節**: 09 §9 L のコード

#### 差異

##### 原文

補題 9.2：「LCode(η, M, c) なら M = Lη。」η は定義 9.1 条件 1 の「η は順序数」を満たす任意の集合でよい。

##### lean

lean/Bm4/SetTheory/LCode.lean:1085 lcode_sound — 仮定は `LCode (L ω) ωZ η.toZFSet M c`（η : Ordinal）で、結論は M = L η。

#### 判定

Lean は Ordinal.toZFSet で与えられた η についてのみ主張する。IsOrdinal を満たす ZFSet が必ず toZFSet の像であるという橋渡し補題は Bm4 内に見当たらず（grep で該当なし）、LCode の条件 1 に含まれる η.IsOrdinal は lcode_sound の証明で捨てられている（1087 行の destructuring で `-`）。
周囲の宇宙は整礎なので数学的には同値だが、Lean の主張は論文より狭い形になっている。実際の用途（系 10.6、補題 11.2、StKP/AdmP）では常に外部順序数が渡されるので、開発上の穴にはなっていない。
一般性の差というより形式化上の限定なので U とした。

### U-29. 系 10.2 の「関数 F の存在」に対応する定理は Lean に無く、像の有界性だけが用意されている

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

系 10.2：∀x∈a ∃!y ψ(x,y,p̄) なら dom(F) = a かつ ψ(x,F(x),p̄) を満たす関数 F が存在する

##### lean

SatInL.lean:40 `image_bound`（結論は `∃ b ∈ L θ, ∀ x ∈ v 1, G x ∈ b` のみ）

#### 判定

Lean 側で系 10.2 に対応するのは `image_bound` だけで、これは像 {G(x) : x ∈ a} が L θ のある集合 b に含まれることしか言わない。dom(F) = a なる関数 F を作る定理は担当 4 ファイルにも他ファイルにも無い。論文が 10.2 を使う箇所は Lean では別筋になっている：補題 10.3 の極限段階（論文は G(ξ) = s_ξ なる関数 G を 10.2 で集め s = ⋃ran(G)）は、Recursion.lean:527–600 で Σ1-Collection で B を得たのち ∆0-Separation で G = {g ∈ B : g はある ξ₀<λ の部分近似} を切り出し ⋃G を取る形に置き換えられている。補題 10.4(1)・10.5(1) でも同様。

### U-30. 補題 10.3 の ω-再帰に対応する Lean の定理が無く、論文が ω-再帰を使う箇所はすべて ℕ 上の外部帰納法＋Σ1-Collection に置き換わっている

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.3 の付帯主張「同様に自然数段階の ω-再帰も存在する」と、その証明（規則を ω+1 まで延長して制限する）

##### lean

Recursion.lean 全体（ω-再帰に対応する定理は存在しない）

#### 判定

Recursion.lean が与えるのは順序数 Σ1-再帰 (`sigma1_recursion`) だけで、ω-再帰の系は無い。論文が ω-再帰を使う箇所は Lean では次のようになる：10.4(1) の (Q_n)_{n<ω} は SatInL.lean:304 `powSeq_mem_L`／389 `seqsLe_mem_L` として ℕ 上の外部帰納法で各段を L θ に入れ、SatInL.lean:429 `seqs_mem_L` で `image_bound` により一括収集し、412 `cum_complete`（一段拡張で閉じた集合の完備性）で埋める。10.5(1) の T = ⋃_n T_n も同様に ω-再帰を使わない（次項参照）。θ > ω の推移的モデルでは L θ の ω が真の ω なので外部帰納法で足りる、という意味論的設定ゆえの迂回。

### U-31. Lean 版は関数 F を外部から与えられたものとして仮定し、しかも仮定を ξ ≤ η ではなく全ての ξ < θ に対して要求する

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.3：各 ξ ≤ η と再帰方程式を満たす ξ 上の関数 g に対し ∃!y Φ(ξ,g,y,p̄) が成り立つなら、ただ一つの F が存在して dom(F)=η+1, Φ(ξ,F↾ξ,F(ξ),p̄) (ξ ≤ η)

##### lean

Recursion.lean:318 `sigma1_recursion`, 643 `sigma1_recursion_existsUnique`

#### 判定

論文は ∃!y Φ から F の存在そのものを導く。Lean は F : Ordinal → ZFSet を引数として受け取り、`hstep : ∀ ξ < θ, graphBelow F ξ ∈ L θ → F ξ ∈ L θ ∧ ExsD ...`（存在の半分）と `huniq : ∀ ξ < θ, ∀ y ∈ L θ, ... → y = F ξ`（一意性）を仮定する。すなわち (i) 存在部分は外部宇宙で選んだ大域関数として供給され、(ii) 仮定の範囲が論文の ξ ≤ η ではなく ξ < θ 全体に拡げられている。η+1 < θ の結論しか使わないので (ii) は Lean 側の余分な仮定であり、この点で Lean のほうが一般性が低い。結論部（∃! G ∈ L θ, IsFunc G ∧ IsDom G (η+1) ∧ 再帰方程式）は論文と一致。

### U-32. 補題 10.4(2)(3) に対応する定理が Lean に無く、補助集合 U は TC 再帰ではなく L 階層の極限段 L ξ で取られている

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.4(2) TC(X) の存在、10.4(3) X ⊆ U かつ U 推移的・PUCl(U) なる U の存在、および 10.5(1) の「補題 10.4(3) を {A,ω,T} に適用して U を取る」

##### lean

SatInL.lean:1668 `add_omega0_lt`, 1765 `exists_limit_between`, 1772 `satCode_exists_in_L`（U := L ξ）

#### 判定

Lean には TC(X)（推移的閉包）も 10.4(3) の閉包構成も無い。`satCode_exists_in_L` は A, TruthSet A, ωZ を含む極限順序数 ξ < θ を取り U := L ξ と置く（L ξ は推移的かつ PUCl）。そのために論文に対応物の無い補題を新たに要する：`add_omega0_lt`（θ が許容なら ζ < θ ⇒ ζ+ω < θ）と `exists_limit_between`。前者は nestZ/chainF/chainR（SatInL.lean:1553–1667、約 200 行）と `collect3` を使う独自の議論で、論文には一切現れない。論文の 10.4(3) 経由の U と Lean の L ξ は別物だが、どちらも Definition 8.3 の条件 1 を満たすので結論は同じ。

### U-33. 真理集合 T の構成筋が論文と全く異なる（構文深さの内部 ω-再帰ではなく、メタの論理式ごとの外部構造帰納＋コード上の Σ1-Collection）

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

補題 10.5(1) の証明：W_A^pr = {⟨e,a⟩ ∈ ω × A^<ω : e は整式コード ∧ Asn_A(e,a)} を ∆0-Separation で切り出し、構文深さに沿う T_n を補題 10.3 の ω-再帰で集めて T = ⋃_n T_n とする

##### lean

SatInL.lean:695 `TAtom`, 716 `TSet`, 1174 `TClause`, 1473 `truthSet_mem_L`

#### 判定

Lean は各メタ論理式 φ に対し TSet A φ（φ とその部分論理式の真理対）を Lean の帰納型 Fm 上の外部構造帰納で定義し、TSet A φ ∈ L θ を φ ごとに示す（TSet_mem_L）。次にコード e ∈ L ω 上の Σ1-Collection（collect3）で、各 e に対する部分充足構造の対 (Cs φ, TSet A φ) を一つの集合 b に集め、最後に TruthSet A を kprodZ (L ω) (Seqs A) から b を使った ∆0-Separation で切り出す。論文の T_n も ω-再帰も現れない。さらにコードの動く領域が論文の ω ではなく L ω（遺伝的有限集合）で、Lean の `SatCode h w A U T` は論文の SatCode(A,U,T) に無いパラメータ h = L ω, w = ωZ を持つ（定義 8.3 由来）。

### U-34. §10 の主張はすべて構文的（KP ⊢ …）だが Lean は意味論的（許容 θ に対する L θ 内）に置き換え、かつ許容順序数の定義自体が Lθ ⊨ KP ではない

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

§10 全体：「KP は Σ1-Collection を証明する」等、すべて理論 KP の定理としての主張。「順序数 θ を許容順序数と呼ぶとは Lθ ⊨ KP であることをいう」

##### lean

Recur.lean:155, Recursion.lean:318/643, SatInL.lean:1772（すべて `IsAdmissible θ` 仮定下の L θ 内の主張）。定義は Adm.lean:27（担当ファイル外だが 4 ファイル全ての仮定）

#### 判定

Lean の `IsAdmissible θ := ω < θ ∧ Order.IsSuccLimit θ ∧ Delta0Collection (L θ)` は、論文の定義 Lθ ⊨ KP とは別の条件で、同値性は Lean では証明されておらず doc-comment で主張されているだけ。Pairing・Union・∆0-Separation は KP の公理としてではなく「θ が極限」から導かれる（HF.lean:70/75/80、Adm.lean）。この置き換えの副作用として、10.5(1) の「二つの真理部分の一致は…KP 内でも証明できる」という論文の補足は Lean では不要になり、外部宇宙の補題 8.4（SatCode.lean:637 `satCode_unique`）がそのまま使える。

### U-35. 「KP が ω の存在を証明する」という §10 冒頭の主張に対応する Lean の証明は無く、ω は外部宇宙から与えられている

**節**: 10 §10 KP 内の再帰

#### 差異

##### 原文

§10 冒頭：「Infinity の証人である帰納的集合から有限順序数全体を ∆0-Separation で切り出し、Set Induction で全ての帰納的集合に含まれることを示せば、KP は最小帰納的集合 ω の存在を証明する」

##### lean

HF.lean:17 `natZ`, Adm.lean:14 `ωZ := Ordinal.omega0.toZFSet`, HF.lean:48 `ωZ_mem_L`

#### 判定

Lean の ωZ は外部宇宙の順序数 ω を ZFSet に写したもので、ωZ ∈ L θ は `IsAdmissible` の第一条件 ω < θ から直ちに従う（`ωZ_mem_L`）。natZ n も ℕ 上のメタ再帰で定義される。論文が述べる「Infinity の証人 + ∆0-Separation + Set Induction による最小帰納的集合の構成」は形式化されていない。意味論的設定では L θ の ω が真の ω なので構成が不要になる、という迂回。

### U-36. Lean の許容順序数は「L_θ |= KP」ではなく「w < θ かつ θ 極限かつ L_θ で Δ0-Collection」という三つ組で定義されている。

**節**: 11 §9 L の階層

#### 差異

##### 原文

§10 冒頭（p.19）「順序数 θ を許容順序数と呼ぶとは、L_θ |= KP であることをいう」。KP = Ext, EmptySet, Pairing, Union, Infinity, Δ0-Sep, Δ0-Coll, SetInduction。

##### lean

Adm.lean:27-28 `IsAdmissible`、Adm.lean:19-23 `Delta0Collection`、Adm.lean:30-33, 38-39

#### 判定

論文: θ 許容 :⟺ L_θ |= KP（8 公理すべての充足）。
Lean: IsAdmissible θ := w < θ ∧ IsSuccLimit θ ∧ Delta0Collection (L θ)。
つまり Δ0-Collection 以外の 7 公理が「w < θ ∧ θ 極限」に置き換わっている。
その結果、論文が補題 13.1 として証明する θ>w・θ 極限・w ∈ L_θ は、Lean では
IsAdmissible.omega_lt / .isSuccLimit が単なる射影、.omega_mem が toZFSet_mem_L_iff
から即座、という定義上の帰結になる。また §10 の「KP は Δ0-Separation と Set Induction
で最小帰納的集合 w の存在を証明する」に対応するものは Adm.lean には無く、w は
Adm.lean:14 で外部の Ordinal.omega0.toZFSet として与えられている。
同値性自体は担当範囲外の KPSat.satIn_kpAx / KPSat.isAdmissible_of_kpTrue および
AdmKP.omega_lt_of_kpTrue / isSuccLimit_of_kpTrue で証明されており、矛盾ではない。
形式化の都合による定義の置き換えなので U とした。

### U-37. Infinity を「帰納的集合の存在」ではなく「∈-極大元を持たない非空集合の存在」として形式化しており、論文の文より真に弱い。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

§10「KP は Extensionality, Empty Set, Pairing, Union, Infinity, ∆0-Separation, ∆0-Collection, Set Induction からなるものとする [3]」および「Infinity の証人である帰納的集合」

##### lean

KPAx.lean:474 infAx = ∃x ((∃u, u∈x) ∧ ∀u∈x ∃v∈x, u∈v)、KPSat.lean:123 satIn_infAx、AdmKP.lean:150 infAx_content, 172 omega_lt_of_kpTrue

#### 判定

論文は Infinity の証人を帰納的集合（∅∈a かつ後続閉）とし、補題 13.1 の証明でも「内部で得られる最小の帰納的集合が外部の ω と一致する」という筋を使う。Lean の infAx は ∅∈x すら要求せず「x は非空で ∈-極大元を持たない」だけで、Barwise の Infinity よりも弱い文である。そのため ω<η の導出も「最小の帰納的集合」ではなく、証人 x の元の階数が非有界であることから rank(x) ≥ ω を出す別の筋になっている。公理集合が弱くなるので補題 11.2 の ⟹ 方向は論文より強い主張を証明していることになる。

### U-38. 論文が ω に有界化する量化子を、Lean は h = L ω（遺伝的有限集合全体）に有界化し、パラメータも ω 一つでなく (L ω, ωZ) の二つになっている。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

定義 11.1「∀d ∈ ω (KPAx(d) → ⟨d,∅⟩ ∈ S)」、§8 の「式コードは自然数、構文述語は ω をパラメータとする ∆0 公式」

##### lean

AdmKP.lean:42 KPTrue h w M U S := ∀ d ∈ h, KPAxCode h w d → pair d ∅ ∈ S（h = L ω, w = ωZ）、AdmKP.lean:99 sigmaDef_admKP は {h,w,x} を台とする

#### 判定

論文は式コードを π による自然数として符号化し、定義 11.1 の量化を ∀d∈ω と書く。Lean のコードは HF 集合（Fm.code : Fm → ZFSet）なので、対応する有界化は ∀ d ∈ L ω になり、変数番号用に別途 w = ωZ を持つ。したがって KPAx・KPTrue・Adm の ∆0/Σ̂1 表示はいずれも自由変数（パラメータ）が論文より 1 個多い。符号化方式の選択に伴う迂回で、意味は対応している。

### U-39. 論文に対応物の無い第二の「定義 11.1」が Lean にあり、余分な ∆0 連言 2 つを持ち、KPTrue の代わりに ∆0-Collection だけを手書きした CollTrue を使っている。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

定義 11.1（KPTrue と Adm はこの一つだけ）

##### lean

AdmP.lean:103 IsUpd2, 110 CollTrue, 118 AdmP, 285 admP_iff、AdmPOK.lean:13 admP_iff'

#### 判定

AdmP は AdmKP と別に定義 11.1 を名乗るが、(1) 論文に無い ∆0 連言 w∈η（= ω<η）と ∀ζ∈η, ζ∪{ζ}∈η（= η は極限）を先頭に持ち、(2) 「全 KP 公理の真理」ではなく ∆0-Collection のインスタンスだけを IsUpd2 を使って直接書き下した CollTrue を用いる。すなわち論文の KPTrue とは別の述語である。§15 側（Reflect.lean）が使うのは AdmKP のみで、AdmP/AdmPOK は下流で使われていない。なお admP_iff の docstring は「LCode の存在は IsAdmissible θ だけからは出ないので仮定する」と書くが、AdmPOK.lean はそれを lcode_exists_in_L で解消しており、コメントが実態と合っていない。

### U-40. Lean は健全性に不要な側条件（x ∉ FV(φ) と 4 変数の相異性）まで課しており、認識される公理集合が論文の「全インスタンス」より真に狭い。

**節**: 12 §11 内部許容性

#### 差異

##### 原文

§11 冒頭「KP の公理または公理スキーマの一インスタンス」（全インスタンスを意図）

##### lean

KPAx.lean:699 KPAxCode（スキーマ枝で NotFreeW h w x c と a≠b, a≠x, a≠y, b≠x, b≠y, x≠y、set induction 枝で NotFreeW h w a c と a≠b を要求）

#### 判定

KPSat.lean:147/184 の satIn_sepAx / satIn_collAx は y ∉ FV(φ) しか使っておらず、x ∉ FV(φ)（x は先頭で全称化される集合変数なので自由出現は単なるパラメータ）と i,j,x,y の相異性は真理には不要。Lean の KPAxCode はこれらを要求するので、認識される「KP の公理」の集合は論文が意図する全インスタンスより小さい。KPTrue はこの小さい集合についての主張になるため Adm は論文より弱い述語だが、補題 11.2 の ⟹ 方向はそれでも通っている。

### U-41. 補題 12.2 の主張の核である「操作のグラフが Δ₀」が Lean には無く、外側の意味論的な存在言明だけが証明されている。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

補題 12.2 冒頭「固定した符号化の下で、次の操作のグラフは ∆0 である」（(1)(2)(3)(4) 全て）

##### lean

Bm4/SetTheory/Fm.lean:369 IsSigma.rename, :433 IsSigma.exists_neg, :500 IsSigma.pad, :851 IsSigma.and_exists, :914 IsSigma.or_exists, :952 IsSigma.andList_exists ほか（Fm.lean と BF.lean には Delta0Def の出現が 0 件）

#### 判定

論文は (1)-(4) を「符号上の構文操作であり、そのグラフが Δ₀」と主張する。
Lean は同じ内容を Fm/BF という Lean 側の帰納型上のメタ定理として述べる。
例: IsSigma.pad は ∃ φ', IsSigma q' φ' ∧ EquivNE φ' φ であって、φ ↦ φ' の
符号上のグラフが Δ₀ だとは一言も言っていない。実際 grep で Fm.lean と BF.lean
には Delta0Def が 1 件も無い。Δ₀ 性が示されているのは認識述語だけ
(IsDelta0CodeW, IsSigCodeW/WD, IsVarsBody, IsBlkUpd/P)。
つまり論文が「内部で使う操作」として立てた補題を、Lean は「外部で構成する」
形に置き換えている。下流で必要な範囲は満たすが、補題 12.2 の言明そのものの
形式化ではない。

### U-42. 補題 12.2 (1) の「代入」に対応する構文操作が Lean に無く、単射な変数改名と付値の更新で代替されている。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

補題 12.2 (1) の一覧のうち「代入」

##### lean

Bm4/SetTheory/Fm.lean:175 def rename（Function.Injective f を要求）。Bm4/ 全体に置換（substitution）の定義は存在しない

#### 判定

論文 (1) は「変数改名、代入、束縛変数の衝突回避、相対化、交代ブロックの付加」を挙げる。
Lean での対応は 変数改名 = IsSigma.rename/IsPi.rename (Fm.lean:369,375)、
束縛変数の衝突回避 = shiftBound (Fm.lean:528)、交代ブロックの付加 = IsSigma.succ 構成子、
相対化 = Rel.lean。「代入」だけ対応物が無い。
さらに rename は f が単射のときしか Σ̂q クラスを保たないので、
2 つの変数を同一化する代入は Lean では表現できない。
代わりに Sat D v φ の付値 v を Function.update で更新することで、
構文的な代入を回避している。

### U-43. Blk.lean の IsBlkUpd は論文の BlkUpd と違い ∅ padding を持たず、その代償として論文に無い仮定を存在定理に必要とする。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

定義 12.1 直後の BlkUpd(a,ν,t,b) の定義（dom(b) = m、残りの座標を ∅ で埋める）

##### lean

Bm4/SetTheory/Blk.lean:49 def IsBlkUpd、:337 exists_blkUpd、:441 exists_blkUpd'

#### 判定

論文の BlkUpd は dom(b) = m = max(dom a, max{ν(i)+1}) とし、
ν の値でも dom(a) でもない j に b(j) = ∅ を置く。
Blk.lean:49 の IsBlkUpd はこの第 3 分岐を持たず、b = (a の ν 値以外) ∪ {(ν(i),t(i))} である。
そのため exists_blkUpd (Blk.lean:337) には論文に無い仮定
hdom : ∀ i ∈ l, ∃ y, ⟨natZ i, y⟩ ∈ a（ブロック変数が既に a の定義域にある）が要り、
exists_blkUpd' (:441) では先に a 側を exists_padSeq で埋めて回避している。
論文どおりの padding 付き定義は BlkP.lean:23 の IsBlkUpdP。
二つの版が併存し、Truth.lean は前者、TrP.lean は後者を使う。

### U-44. BFCode.lean 側の Σ̂q/Π̂q 認識述語は「ブロック内の変数が相異なる」という定義 12.1 の条件を落としており、定義 12.1 より広い符号クラスを受理する。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

定義 12.1「各 x⃗i は相異なる変数からなる空でない有限ブロック」

##### lean

Bm4/SetTheory/BF.lean:84-90 Sig/Pi（Nodup 無し）、BFCode.lean:480 IsNeSeqW（値の単射性なし）、:599 IsSigCodeW、:746 isSigCodeW_iff。相異性を課した版は BFCodeD.lean:70 IsNeSeqWD、:110 IsSigCodeWD

#### 判定

BF.Sig/BF.Pi はブロックの非空性と極性の交代だけを記録し、l.Nodup を要求しない。
対応する符号認識述語 IsSigCodeW も IsNeSeqW（非空有限列、値は ω の元）だけを課す。
isSigCodeW_iff は「∃ b, BF.Sig q b ∧ e = b.code」で NodupBlocks を含まない。
定義 12.1 に一致するのは BFCodeD.lean の IsSigCodeWD / IsPiCodeWD で、
isSigCodeWD_iff が「Sig q b ∧ b.NodupBlocks」を与える。
つまり同じ階層に緩い版と忠実な版の 2 種が併存し、
相異性は代わりに BlkUpd 側 (BlkP.lean:23 nuInj) に押し込まれている。
コメント (BF.lean:77-82) も「Definition 12.1 asks in addition ...」とこの分離を認めている。

### U-45. IsVarsBody は先頭タグも交代ブロック式であることも検査せず、論文の Vars/Body より広い関係になっている。

**節**: 13 §12 交代ブロック階層

#### 差異

##### 原文

定義 12.1「交代ブロック式 e について、最外ブロックの変数番号列を Vars(e)、そのブロックを除いた本体コードを Body(e) と書く」

##### lean

Bm4/SetTheory/BFCode.lean:757 def IsVarsBody (e ν d) : Prop := ∃ t, e = ZFSet.pair t (ZFSet.pair ν d)

#### 判定

論文の Vars(e), Body(e) は「交代ブロック式 e」に対して定義される。
Lean の IsVarsBody は e が対の対の形であることしか要求せず、
t が ∃/∀ のタグ（natZ 1 / natZ 2）であることを検査しない。
そのため Δ₀ ブロック符号 ⟨natZ 0, ⟨sg, d⟩⟩ に対しては符号ビット sg を
「Vars」として返す。
ガード（IsSigCodeW 等）の下でしか使わないので実害は無く、
実際 BFCode.lean の外では使われず下流は対の形を直接書いているが、
定義 12.1 の Vars/Body そのものの形式化にはなっていない。

### U-46. Truth.lean と TrCorrect.lean の定義 13.4 は ∅ パディングを持たない別の更新関係を使い、その結果 定理 13.6 に「ブロック変数が既に代入の定義域にある」という論文に無い仮定が付いている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定義 13.4 と §12 の BlkUpd（dom(b) = max(dom a, {ν(i)+1})、埋まらない座標は ∅）、および定理 13.6 の仮定「e, a ∈ W、a が適切な代入」

##### lean

lean/Bm4/SetTheory/Truth.lean:191-204 (TrSigS/TrPiS)、lean/Bm4/SetTheory/TrCorrect.lean:213 (GoodAsn)、参照 lean/Bm4/SetTheory/Blk.lean:49 (IsBlkUpd)

#### 判定

§12 の BlkUpd は dom(b) を max(dom a, {ν(i)+1}) まで伸ばし、空いた座標に ∅ を入れる。Blk.lean の IsBlkUpd は b を dom(a) ∪ ran(ν) 上でしか定めず、∅ 補填の場合分けが無い。そのため TrCorrect.lean の GoodAsn に「∀ i ∈ b.blockVars, ∃ y, ⟨natZ i, y⟩ ∈ a」という節が要り、定理 13.6 (trSigS_correct/trPiS_correct) はブロック変数が全て dom(a) に入る代入についてしか成り立たない。論文の定理 13.6 の仮定は「a が e の自由変数を覆う適切な代入」だけで、束縛変数であるブロック変数については何も要求しない。TrP.lean:27-40 の IsBlkUpdP は §12 の定義どおり（第 3 の場合分け x = ∅ が k ≤ max ν(i) の座標を埋める）で、GoodAsnP (TrP.lean:230) からこの節が消えている。すなわち同じ定義 13.4 の形式化が 2 本あり、忠実なのは TrP.lean の側である。

### U-47. Lean の行列は論文と 3 点で違う：∅ ∈ A の追加、Asn_A(e,a) が IsSeqA に弱められ、代わりに a の ∅-パディング b が有界存在量化され、⟨e,a⟩ ∈ T が ⟨e,b⟩ ∈ T になっている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定義 13.2：Tr⁺_∆0(e,a) ⟺ e は ∆0 コード ∧ ∃A 推移的 ∃U,T (Asn_A(e,a) ∧ a ∈ U ∧ SatCode(A,U,T) ∧ ⟨e,a⟩ ∈ T)、Tr⁻ はその双対

##### lean

lean/Bm4/SetTheory/Truth.lean:19-37 (TrD0Mat, TrD0P, TrD0N)

#### 判定

論文の行列は Asn_A(e,a) ∧ a ∈ U ∧ SatCode(A,U,T) ∧ ⟨e,a⟩ ∈ T。Lean の TrD0Mat は A.IsTransitive ∧ ∅ ∈ A ∧ IsSeqA w A a ∧ a ∈ U ∧ SatCode ∧ ∃ b ∈ U, IsPadOf a b ∧ IsAsn h w A e b ∧ ⟨e,b⟩ ∈ T。(i) ∅ ∈ A は論文に無い連言（パディング値 ∅ を A に入れるため）。(ii) 論文の Asn_A(e,a)（a が e の自由変数を全て覆う）が a については落ち、覆う条件はパディング b の側 IsAsn h w A e b に移っている。(iii) 判定される対が ⟨e,a⟩ でなく ⟨e,b⟩。∃b ∈ U は有界なので複雑度 (13.1) は変わらない。なお Truth.lean:39 の trD0P_imp_trD0N_of_unique は論文に対応物が無く、リポジトリ内でも使われていない補助命題。

### U-48. Lean 版補題 13.3 は「a が適切な代入」と「e ∈ W」を仮定せず、論文より強い主張になっている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

補題 13.3：θ 許容、W = L_θ、e, a ∈ W、e が ∆0 式 δ を符号化し a が適切な代入なら (13.2)

##### lean

lean/Bm4/SetTheory/Truth.lean:391 (BaseCorrect)、lean/Bm4/SetTheory/BaseOK.lean:42 (baseCorrect_L)、同 107 (baseCorrect_V)

#### 判定

BaseCorrect W は「∀ φ, IsDelta0 φ → ∀ a, IsSeqA ωZ W a → a ∈ W → (TrD0P ↔ Sat) ∧ (TrD0N ↔ Sat)」で、a が φ の自由変数を覆うことを要求しない（覆わない変数は SeqVal a で ∅ と評価される）。これは前項の ∅-パディングによる一般化。また e に当たるものは外部の標準式 φ のコードで、コード側の e ∈ W は仮定されない（コードは L ω の元なので自動）。同値の内容 (13.2)（Tr⁺ ⟺ Tr⁻ ⟺ δ[a]）自体は 2 本の ↔ として一致する。

### U-49. Lean は ∆0 行列に符号ビットを付けた層（コード ⟨0,⟨sg,d⟩⟩）を挟み、負符号側では Tr は ¬TrD0N / ¬TrD0P になる。論文には符号層が無い。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定義 13.4 の基底（q = 1 で Tr⁺_∆0(d,b) / Tr⁻_∆0(d,b)）、および定義 12.1 の「行列 δ ∈ ∆0」

##### lean

lean/Bm4/SetTheory/Truth.lean:178-190 (TrMSig, TrMPi)、TrSigS/TrPiS の 0 段、参照 lean/Bm4/SetTheory/BF.lean:17 (BF.delta sign)

#### 判定

論文の Σ̂q は ∃x⃗₁…Q x⃗_q δ（δ ∈ ∆0）で行列に符号は無く、定義 13.4 の基底はそのまま Tr⁺_∆0(d,b)（Π 側は Tr⁻_∆0(d,b)）。Lean は BF に delta (sign : Bool) を置き、コードを ⟨0,⟨sg,d⟩⟩ とし、TrMSig は sg=1 なら TrD0P、sg=0 なら ¬TrD0N（TrMPi は sg=1 で TrD0N、sg=0 で ¬TrD0P）と定義する。∆0 は否定で閉じているので表現力は変わらないが、定義 13.4 の基底の形は論文と一致しない。あわせて TrSigS/TrPiS は q = 0 でも定義されており（= TrMSig/TrMPi）、論文の定義 13.4（q ≥ 1）に直接の対応物が無い段が 1 つ多い。

### U-50. Vars/Body の関数記号が、D で有界化した ∃ν∃d（Σ 側）／∀ν∀d（Π 側）と等式 e = ⟨tag,⟨ν,d⟩⟩ に置き換えられている。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定義 13.4：ν = Vars(e)、d = Body(e) という固定されたコード射影（∆0-定義可能関数記号）を項として使う

##### lean

lean/Bm4/SetTheory/Truth.lean:191-204 (TrSigS/TrPiS)、lean/Bm4/SetTheory/TrP.lean:27-40 (TrSigP/TrPiP)

#### 判定

論文は ν, d を項として書き、量化子は ∃t∃b（∀t∀b）だけ。Lean は Σ 側で ∃ν, D ν ∧ ∃d, D d ∧ ∃t ∃b (e = ⟨1,⟨ν,d⟩⟩ ∧ BlkUpd ∧ …)、Π 側で ∀ν, D ν → ∀d, D d → … (e = ⟨2,⟨ν,d⟩⟩ → BlkUpd → …) とする。コード対の分解は一意なので W が推移的で e ∈ W なら等価だが、量化子の本数と D による有界化が論文の記述と異なる。複雑度 (13.5) はどちらでも同じブロックに併合されるので影響しない。

### U-51. 定理 13.6 の Lean 版は全ブロックの変数を連結したリスト全体の Nodup を要求し、論文のブロック内相異より強い。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定義 12.1（定理 13.6 が前提とする Σ̂q の形）：「各 x⃗_i は相異なる変数からなる空でない有限ブロック」

##### lean

lean/Bm4/SetTheory/TrCorrect.lean:213 (GoodAsn)、lean/Bm4/SetTheory/TrP.lean:230 (GoodAsnP)

#### 判定

論文が要求するのは各ブロック内で変数が相異なることで、別ブロックでの再量化（∃x ∀x δ 型）は禁じられていない。コード判定側 IsSigCodeWD/IsPiCodeWD もブロック内相異（IsNeSeqWD）しか見ない。しかし GoodAsn / GoodAsnP は b.blockVars.Nodup（全ブロックを ++ で連結したリストの Nodup）を仮定し、帰納の各段でこれを分解して使っている。論文の Def 12.1 の読み方に曖昧さは残るが、少なくともコード判定より強い仮定が定理 13.6 に入っている。担当外だが StRelP.lean:34 の GoodAsnQ はこれを NodupBlocks（ブロック内のみ）に弱めた版で、そちらが論文に一致する。

### U-52. 外部宇宙版の定理 13.6 は IsBlkUpd 版（TrSigS/TrPiS）にしか無く、§12 の BlkUpd に忠実な padding 版（TrSigP/TrPiP）には無い。

**節**: 14 §13 真理述語

#### 差異

##### 原文

定理 13.6 最終文「同じ同値は外部宇宙 V でも成立する」

##### lean

lean/Bm4/SetTheory/TrP.lean（ファイル末尾、trPiP_correct:384 で終わり）／lean/Bm4/SetTheory/TrCorrect.lean:677-750 と BaseOK.lean:157-170 は TrSigS 版のみ

#### 判定

TrCorrect.lean は GoodAsnV と trSigPiS_correct_V / trSigS_correct_V / trPiS_correct_V を持ち、BaseOK.lean が baseCorrect_V と trSigS_correct_ext / trPiS_correct_ext で外部宇宙半を完成させている。しかしこれらは全て IsBlkUpd を使う TrSigS/TrPiS 版で、リポジトリ全体を検索しても TrSigP/TrPiP の V 版は無い（GoodAsnV を参照するのは TrCorrect.lean と BaseOK.lean のみ）。よって「§12 の BlkUpd に忠実な定義 13.4」については、定理 13.6 の外部宇宙版に対応物が無い。6 分類のどれにも綺麗には収まらず（論文の欠陥ではなく形式化の被覆漏れ）、便宜上 U とした。

### U-53. Lean の ElemHat は j の範囲が 0 ≤ j ≤ q で、q ≥ 1 と M, N の推移性という論文の前提を持たない。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

定義 14.1: 「推移的集合 M ⊆ N と q ≥ 1 に対し」「全ての 1 ≤ j ≤ q、全ての Σ̂_j, Π̂_j 式、全ての M-パラメータについて M と N が同じ真理値」

##### lean

Bm4/SetTheory/Elem.lean:18 `ElemHat`

#### 判定

論文は j を 1 ≤ j ≤ q に限り、M ⊆ N が推移的で q ≥ 1 であることを定義の前提に置く。
Lean は `∀ j ≤ q` と書くので j = 0（Σ̂_0 = Π̂_0 = Δ_0）の真理値一致も要求し、q = 0 でも定義される。
また推移性は定義に入れず、M ⊆ N のみを連言に含める。
したがって推移的でない M ⊆ N に対しては ElemHat q M N は論文の ≺*_q より真に強い（Δ0 一致は自動でない）。
推移的 M ⊆ N（実際の適用先 L_ξ ⊆ L_θ）では `IsDelta0.satIn_iff_satIn` により j = 0 は自動なので両者は一致し、矛盾は生じない。
この余分な j = 0 節のため `elemHat_of_downward` (Elem.lean:88) は hM, hN の推移性を仮定に持つ。

### U-54. Lean の TVq は (14.1) に無い 4 つの連言（M が推移的、M が非空、h ∈ M、w ∈ M）を含む。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

定義 14.2 / 式 (14.1): TV_q(M) は ⋀_{1≤j≤q} ∀e∈ω ∀a∈M(Form_{Σ̂j}(e) ∧ Asn_M(e,a) ∧ Tr_{Σ̂j}(e,a) → Tr^M_{Σ̂j}(e,a)) の Π̂_q 表示そのもの

##### lean

Bm4/SetTheory/TV.lean:41 `TVq`

#### 判定

論文の TV_q(M) は有限連言 (14.1) だけからなり、M への副条件は一切無い。
Lean は `M.IsTransitive ∧ (∃ x, x ∈ M) ∧ h ∈ M ∧ w ∈ M ∧ TVConj …` と定義する。
理由は `PiDef.relativize` が「Q D v ↔ TrSigS (· ∈ M) …」を M が good domain（推移的かつ非空）でパラメータ h, w ∈ M のときにしか与えないためで、TV.lean:110 付近の `key` がその側条件を消費している。
これらは Δ0 なので補題 14.3 の複雑度には影響しないが、TV_q(M) の外延は論文より真に狭い（非推移的 M では常に偽）。
同じ開発内の StRelP.lean:212 `TVqP` は「the finite conjunction (14.1) and nothing else」としてこの 4 連言を持たず、論文どおりの形になっている。

### U-55. 論文の Asn_M(e,a) が Lean では e に依存しない IsSeqA w M a に置き換わり、自由変数被覆の条件が落ちている。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

式 (14.1) の前件 Asn_M(e,a)（§13 冒頭: a が e の自由変数を全て覆い、値域が M に含まれる標準有限代入）

##### lean

Bm4/SetTheory/TV.lean:31 `TVBody`（`IsSeqA w M a`）

#### 判定

論文の Asn_M(e,a) は e の自由変数を a の定義域が覆うことを要求する。
Lean の `IsSeqA w M a` は「a が M-値の有限列である」だけで e を参照しない。
開発内には論文の Asn に対応する `IsAsn h w A e a := IsSeqA w A a ∧ ∀ i ∈ w, ¬NotFreeW h w i e → InDomZ a i`（SatCode.lean:32）があるが、TVBody では使われていない。
前件が弱いぶん Lean の TV_q は論文より強い主張になっており、自由変数を覆わない代入（未定義位置は既定値 ∅ で評価される）についても Tr → Tr^M の転送を要求する。
矛盾ではなく、Lean 側が真に強い版を採っている。

### U-56. 符号の量化域が ω ではなく M であり、論文が省略する固定パラメータ w に加えて h も明示パラメータになっている。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

式 (14.1) の外側量化 ∀e ∈ ω（§8: 式コードは自然数、構文述語は共通の自由変数 w を持ち w = ω を代入、記号の煩雑さを避けて w は省略）

##### lean

Bm4/SetTheory/TV.lean:31 `TVBody`（`∀ e ∈ M`）、TV.lean:57 `piDef_TVq`（台 {h, w, M}）

#### 判定

論文は符号 e を自然数とし ∀e ∈ ω で束縛する。Lean は符号を集合（対の入れ子）で表すため `∀ e ∈ M` と M で束縛する。
実際の適用では h = L_ω、w = ωZ で、M は推移的かつ h ∈ M なので符号は全て M に属し、範囲は一致する。
また論文は構文述語の固定パラメータを w だけとし本文では省略するが、Lean は h, w の 2 つを明示引数に持ち、補題 14.3 の台も {h, w, M}（論文の TV_q(M) はパラメータ M のみ）になっている。
いずれも符号化の実装差に由来する迂回で、主張の内容は変わらない。

### U-57. TVBody が使う符号認識子 IsSigCodeW はブロック内変数の相異性を課さず、定義 12.1 の Form より弱い。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

式 (14.1) の前件 Form_{Σ̂j}(e)（定義 12.1: 各ブロック x⃗_i は相異なる変数からなる空でない有限ブロック）

##### lean

Bm4/SetTheory/TV.lean:31 `TVBody`（`IsSigCodeW h w j e`）

#### 判定

定義 12.1 は各量化ブロックが相異なる変数からなることを要求する。
BFCode.lean:599 の `IsSigCodeW` はブロック列 ν に `IsNeSeqW`（空でない w-値有限列）しか課さず、相異性を課すのは BFCodeD.lean:110 の `IsSigCodeWD` の方である。
ただし前件の第 3 項 `TrSigS D h w j e a` は j ≥ 1 で `IsSigCodeWD h w j e` を内蔵している（Truth.lean:191-197）ため、TVBody 全体の前件は結果として IsSigCodeWD を含み、述語の外延は論文と一致する。
連言の一項としては論文より弱い述語が置かれている、という文面上の差である。
この弱い認識子を選んだ影響は StRelP.lean のヘッダに記録されており、そこでは IsSigCodeWD 版 TVqP が別途導入されている。

### U-58. （取り下げ）監査ミス

**節**: 15 §14 初等性

この指摘は誤りだった。(14.2) は q = k+2 の場合だけでなく q ≥ 1 一般について成立しており、
その一般版が実際にツリーにある。U としては取り下げる。

### U-59. Π̂ の場合、論文は双対化だけで両方向を片付けるが、Lean は片方向のみ双対化を使い、逆方向は論文に無い別補題 sat_alls_anti で処理する。

**節**: 15 §14 有限段 TV

#### 差異

##### 原文

定理 14.4 の証明: 「Π̂_j 式 χ については、補題 12.2 の双対化により ¬χ と同値な Σ̂_j 式 χ⊥ を一意に作れる。既に示した Σ̂_j 真理の一致を χ⊥ に適用すれば、χ の真理値も一致する」

##### lean

Bm4/SetTheory/Elem.lean:66 `sat_alls_anti`、Elem.lean:120-132（`elemHat_of_downward` の Π̂_{j+1} の場合）

#### 判定

論文は段 j で Σ̂_j の一致を示した後、Π̂_j 式 χ の双対 χ⊥ ∈ Σ̂_j に Σ̂_j の一致を適用して χ の同値を一挙に得る。
Lean は Σ̂ の場合と Π̂ の場合を独立した 2 つの分岐として証明するため、Π̂_{j+1} の上向き（M ⊨ φ ⇒ N ⊨ φ）だけを `IsPi.exists_neg` の双対 + `hdown` で処理し、下向き（N ⊨ φ ⇒ M ⊨ φ）は ∀ ブロックの下向き保存補題 `sat_alls_anti` と Σ̂_j の帰納法仮定で別に処理している。
数学的内容は同じ（sat_alls_anti は Σ̂ 側の sat_exs_mono を双対に展開したもの）だが、論文の議論の分割の仕方とは一致しない。

### U-60. 論文の相対化 St_k(ξ)^M が、Lean では M の推移性・非空性・パラメータ所属を前提とする含意の下に置かれている。

**節**: 16 §15 St と Rel

#### 差異

##### 原文

定義 15.4 / 式 (15.4)：Rel_k(ξ,η) :⟺ Ord(ξ) ∧ Ord(η) ∧ ξ<η ∧ ∃M ∃c (LCode(η,M,c) ∧ St_k(ξ)^M)。ここで St_k(ξ)^M は「固定公式 St_k(ξ) の全非有界量化を M に制限した相対化」で、無条件の Δ_0 式。

##### lean

lean/Bm4/SetTheory/StKP.lean:36 `RelKP`（および 235 `relKP_iff`, 252 `relKP_iff_ext` の証明）

#### 判定

論文: ∃M ∃c (LCode(η,M,c) ∧ St_k(ξ)^M)。第2連言は無条件。
Lean: ∃M, D M ∧ ∃c, D c ∧ LCode h w η M c ∧ (M.IsTransitive → (∃z∈M) → h∈M → w∈M → ξ∈M → StKP (·∈M) h w k ξ)。
すなわち相対化を意味論的に読み替え（StKP (·∈M)）、構文的相対化と一致する条件を仮定として明示的に付けている。
Lean 側のコメントもこの点を認めており、PiDef.relativize が GoodDom (·∈M) と ValD の下でしか構文相対化と一致しないことが理由。
LCode が M=L_η を強制し L_η が条件を満たすので外延は一致するが、論文の式そのものではない。
分類は「形式化の都合の迂回」と見て U とした。

### U-61. StRel.lean 版の補題 15.5 が使う `RelK` には (15.4) の Ord(ξ)∧Ord(η)∧ξ<η が無く、代わりに論文に無い良領域条件が連言として入っている。

**節**: 16 §15 St と Rel

#### 差異

##### 原文

定義 15.4 / 式 (15.4) の三つの先頭連言 Ord(ξ) ∧ Ord(η) ∧ ξ<η。および補題 15.5(2)。

##### lean

lean/Bm4/SetTheory/StRel.lean:357 `relK_iff`（用いる述語 `RelK` の定義は TV.lean:51）

#### 判定

論文 (15.4): Ord(ξ) ∧ Ord(η) ∧ ξ<η ∧ ∃M∃c(LCode ∧ St_k(ξ)^M)。
Lean `RelK`: ∃M, D M ∧ (M.IsTransitive ∧ (∃z∈M) ∧ h∈M ∧ w∈M ∧ ξ∈M) ∧ ∃c, D c ∧ LCode h w η M c ∧ StK (·∈M) h w k ξ。
順序数条件 Ord(ξ), Ord(η), ξ<η が丸ごと欠けている一方、論文に無い M の推移性等が連言として主張されている。
同じ §15 の StKP.lean:36 `RelKP` では三つの連言が復活しており、二つの形式化で (15.4) の読みが食い違う。
補題 15.5(2) の同値そのものは両版とも成立するため矛盾ではない。分類は U（RelK は TV.lean 定義だが、補題 15.5 の主張として現れるのは担当範囲の StRel.lean）。

### U-62. 論文は補題 10.5(2) を外部宇宙 V へそのまま適用するが、Lean は 10.5(2) を再利用できず V 用の別構成を新たに与えている。

**節**: 16 §15 St と Rel

#### 差異

##### 原文

補題 15.5(1) の証明：「ZFC は KP を含むので、補題 10.5(2) を外部宇宙で適用し、LCode(η,M,c) を満たすコードを取れる」。

##### lean

lean/Bm4/SetTheory/StKP.lean:138 `lcode_exists_ext`

#### 判定

論文の筋: 補題 10.5(2)（KP が証明する命題）を V ⊨ ZFC ⊇ KP に適用してコードを得る。
Lean: 形式化された 10.5(2) は `lcode_exists_in_L`（L θ の内部、θ が許容）であり、V に対する版が存在しない。
そのため `lcode_exists_ext` は、H, S, D を graphBelow で直接与え、定義 9.1 の条件 1 の補助集合 U をランク初期切片 V_γ（γ = rank Z + ω）として取る別構成になっている。
ファイル冒頭のコメントも「η の上の許容順序数は不要であり一般には存在しない」と述べている。
結論の主張は同じなので矛盾ではなく、形式化上そのまま書けなかった迂回。

### U-63. 補題 15.3 が二重に形式化されており、StRel.lean 版は論文と別の筋（真理述語の Lévy 複雑度による移送）で証明している。

**節**: 16 §15 St と Rel

#### 差異

##### 原文

補題 15.3 と、その依拠する定理 14.4 の証明筋（定理 13.6 を L_θ と L_ξ で二度使う）。

##### lean

lean/Bm4/SetTheory/StRel.lean:248 `trSigS_transfer`, 287 `tvq_iff_elemHat`, 344 `stK_iff` ／ lean/Bm4/SetTheory/StRelP.lean:201 `TVBodyP`, 212 `TVqP`, 329 `tvqP_iff_elemHat_general` ／ StKP.lean:25 `StKP`

#### 判定

論文の定理 14.4 の証明は、定理 13.6（真理述語の正しさ）を L_θ と L_ξ で使って (14.3) を出す一本の議論。
StRel.lean の `tvq_iff_elemHat` は「elementarity ⟹ TV」方向を `trSigS_transfer`（Tr_{Σ̂j} が Σ̂_j 定義可能であることと ≺* の直接適用）で通しており、定理 13.6 を使っていない。
理由はファイル冒頭に明記: `TVq` はブロック内変数の相異を要求しないコードまで量化するため、定理 13.6 が適用できない。
そこで StRelP.lean が `IsSigCodeWD` に制限した `TVqP` を導入し直し、StKP.lean の `StKP`/`RelKP`/`stKP_iff` で論文どおりの筋を回復している。
結果として、定義 15.2・15.4 と補題 15.3・15.5 が Lean には二組（StK/RelK 系と StKP/RelKP 系）存在する。論文には対応する二重化は無い。

### U-68. 補題 12.2 は「符号上の構文操作のグラフが ∆0」という主張だが、Lean はそれを意味論的な定義可能性クラスの閉包に置き換え、同値性の成立範囲も非空推移的領域に狭めている。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

補題 12.2（p.24）「固定した符号化の下で、次の操作のグラフは ∆0 である」および「同値性は全ての非空な一階構造で成立する」

##### lean

Defin.lean:18 GoodDom / Defin.lean:39,44,49 Delta0Def, SigmaDef, PiDef / Defin.lean:142 Delta0Def.absolute

#### 判定

論文は改名・代入・衝突回避・相対化・ブロック付加・双対化・padding・有限結合という「符号上の操作」のグラフが ∆0 であると主張し、同値性は全ての非空一階構造で成立するとする。
Lean は Delta0Def s P := ∃φ, IsDelta0 φ ∧ fv φ ⊆ s ∧ ∀ D v, GoodDom D → ValD D s v → (Sat D v φ ↔ P D v) という述語を定義し、操作はメタレベルの補題（imp/and/or/ex/all/pad/not …）として与える。符号上の ∆0 グラフは Defin/Rel では一切証明していない。
GoodDom = 非空 ∧ 推移的 なので、成立範囲は「非空推移的 ZFSet 領域」に限定され、さらに ValD D s v（パラメータが領域に属する）という論文に無い仮定が全補題に付く。
∆0-絶対性も Fm.lean:261 の sat_iff_satV は TransDom だけで足りるのに、Defin.lean:142 の Delta0Def.absolute は GoodDom（非空性込み）を要求する。
分類は形式化の都合による迂回 (U)。ただしこの迂回の結果、補題 12.2 が本来主張している「符号上のグラフが ∆0」は Defin/Rel では再現されていない。

### U-69. 原文は非有界量化だけを M に制限するが、Lean は全ての量化子を制限するため、∆0 行列の中の有界量化 ∀u ∈ a も ∀u ∈ M (u ∈ a → …) に書き換わる。

**節**: 19 全体の道具（Lévy 階層）

#### 差異

##### 原文

§14 p.27 および §15 p.28「全ての非有界量化を M に制限した相対化」

##### lean

Rel.lean:14-19 Fm.relTo（`| all i φ => ball i m (relTo m φ)`）

#### 判定

Lean の Fm は原始子として all しか持たず、ball i j φ は all i (imp (mem i j) φ) の略記なので、「非有界量化」を構文的に区別できない。relTo は all の全出現を ball i m に置き換える。
その結果、生成される式は原文の φ^M と字面が異なる（原文は ∀u ∈ a φ^M、Lean は ∀u ∈ M (u ∈ a → φ^M)）。
意味は一致する。Rel.lean:60 の sat_relTo が Sat D v (relTo m φ) ↔ Sat (· ∈ v m) v φ を与えており、これは相対化の標準的な意味論そのもの。∆0 性も保たれる（Rel.lean:21 relTo_delta0）。
分類 U（形式化の都合による迂回。実質的な差は無い）。

### U-71. 場合 (b) で論文は補題 3.1 と位置による自明な二分だけで済ませるが、Lean は §6 の完全な祖先特徴づけ `anc_tA_cases` を呼んで同じ二分を得ている。

**節**: 20 §18, §19 安定ラベルと降下

#### 差異

##### 原文

§19 場合 (b)「対象とその全ての祖先は接頭部分 A_q に属し…従って補題 3.1 により」、および備考 19.2 の表（対象が B_q の行は無く、コピー補題を使わない）

##### lean

/home/koteitan/proofs/dh-bms-term/lean/Bm4/Label.lean:255 `b.anc_tA_cases k hj' h`

#### 判定

論文の場合 (b) は、対象 D_j^{(q)} とその祖先が接頭部分 A_q に属することを述べ、
補題 3.1 で祖先関係の不変性を得たうえで、祖先を「B_q より前」「B_q 内の D_i^{(q)}」に
位置だけで分ける。祖先は必ず対象より左なのでこの二分は自明であり、コピー補題は不要。
備考 19.2 の表にも「対象が B_q」の行は無い。
Lean は :255 で `anc_tA_cases` を使う。これは `anc_tA_iff` 経由で (C1)–(C6)・定理 6.3 全体に
依存する定理で、場合 (b) には過剰。Copy.lean:199 の `lt_pos_cases`（純粋に位置の三分）で足りる。
結論は同じで数学的な食い違いは無いが、備考 19.2 が主張する「どの場合にどの結果を使うか」の
対応が場合 (b) についてはずれる。証明の筋の違いなので U と分類した（内容の差ではない）。

### U-72. Lean の R は論文に無い連言 `Reachable r B` を持つ。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

§22 の R の定義「A R B :⟺ B ≠ ∅ かつ、ある n ∈ N に対し A = B[n]」（BM4 上の関係）

##### lean

lean/Bm4/Defs.lean:100 `R r A B := Reachable r B ∧ 0 < B.len ∧ ∃ n, A = expand B n`（用いる先は Main.lean:121 `R_wf`）

#### 判定

論文の R は BM4 上の関係なので、B ∈ BM4 は関係の定義域として外側で課される。
Lean の `Arr r` は BM4 に限らない全配列の型なので、BM4 への制限を関係の中に `Reachable r B` として書き込んでいる。
結果として `WellFounded (R r)` は Arr r 全体での整礎性になり（非到達な配列は R-前者を持たないので自明に整礎）、論文の主張より広い型の上の言明になっている。
形式化上の迂回なので U。数学的な強弱の差は生じない。

### U-73. 論文は自然数符号化と最小コード選択で降下列を作るが、Lean にはその符号化も最小コード選択も無い。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

§22「各配列は有限個の自然数からなる有限データなので自然数で符号化でき、BM4 全体は集合である」および命題 22.1 の証明の「最小コードの元を選ぶ ω-再帰」

##### lean

lean/Bm4/Main.lean:121-137 `R_wf`（`wellFounded_iff_isEmpty_descending_chain` + `choose`）

#### 判定

論文は配列を自然数で符号化し、X 内で「最小コードの A」を選ぶことで ω-再帰を実行する（選択公理を避ける筋）。
Lean の `Arr r` は `len : ℕ` と `col : ℕ → ℕ → ℕ` の組で、有限データではなく自然数符号化もできない。
代わりに Mathlib の `wellFounded_iff_isEmpty_descending_chain` で「降下列が存在しない」に言い換え、`choose` で各段の存在から n : ℕ → ℕ を取り出し、降下列が `seq (B 0) n` に一致することを示して定理 1.2 に帰着している。
論文の議論が別の筋に置き換わっている（観点 4）。符号化の議論は Lean では現れない。

### U-74. 論文に対応物が無く、しかもリポジトリ内で一度も使われていない補題が Main.lean に 2 件ある。

**節**: 21 §20, §21, §22

#### 差異

##### 原文

（対応物なし）

##### lean

lean/Bm4/Main.lean:17 `E_len`、Main.lean:52 `seq_succ_shift`

#### 判定

`E_len : (E r).len = 2` は定義 1.1 の E_r ∈ (N^r)^2 に対応するが、rfl で済む自明な言明で、他所から参照されていない。
`seq_succ_shift : seq A n (t+1) = seq (expand A (n 0)) (fun t => n (t+1)) t` は論文に対応する式・補題が無く、`grep` の限り Bm4 配下・test 配下のどこからも使われていない。
観点 6（Lean にあって論文に対応物が無い主張）に該当するが、6 分類のどれにも収まりが悪いので U とした。数学的な差は生じない。
